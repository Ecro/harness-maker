---
generated_by: harness-maker
harness_maker_version: 0.7.1
generated_at: '2026-01-01T00:00:00+00:00'
source_template: rubrics/claude_md.yaml.j2
provenance: official
content_hash: 3c5e35fb0ab1ccf6bf90f5e8152b5743c868d7f92f35361fa549bbea7607105b
---
dimension: context_quality
target: CLAUDE.md
rubrics:
  - id: tech_stack_specified
    description: >
      Does CLAUDE.md specify the project's tech stack — language version,
      runtime, package manager (uv / npm / cargo / etc.), and key frameworks?
    severity: P0
    action: >
      Add a 'Tech Stack' section listing language version, package manager,
      and primary frameworks.
  - id: build_test_commands_documented
    description: >
      Are exact commands documented for build, test, lint, and type-check?
      For example, 'uv run pytest', 'cargo build --release', 'pnpm test'.
    severity: P0
    action: >
      Document explicit build / test / lint / type-check commands so Claude
      can verify its changes without guessing.
  - id: prohibited_behaviors_listed
    description: >
      Is there a 'Prohibited' or 'Don't' or 'Never' section listing critical
      things Claude must not do (e.g., shell=True, force push, secrets in code)?
    severity: P1
    action: >
      Add a 'Prohibited Behaviors' section with concrete examples; emphasize
      with IMPORTANT or MUST. CLAUDE.md is ~70% advisory — reinforce critical
      rules with hooks for deterministic enforcement.
  - id: model_selection_guidance
    description: >
      Does CLAUDE.md guide which Claude model (Opus / Sonnet / Haiku) is
      appropriate for which kinds of tasks in this project?
    severity: P2
    action: >
      Add a 'Model Selection' section mapping task types to model tiers
      (e.g., Opus for design, Sonnet for execution, Haiku for batch).
  - id: code_style_conventions
    description: >
      Are code style conventions documented (formatter, linter, naming
      conventions, comment policy)?
    severity: P1
    action: >
      Add a 'Code Style' section covering formatter, linter, naming
      conventions, and comment policy.
  - id: testing_approach
    description: >
      Does CLAUDE.md describe the testing approach (TDD vs BDD, integration
      vs unit separation, mock policy)?
    severity: P1
    action: >
      Document the testing approach so Claude knows when and how to add tests.
  - id: git_workflow_documented
    description: >
      Is the git workflow specified (commit message format, branch policy,
      whether amends or force-push are allowed)?
    severity: P2
    action: >
      Document git conventions (commit format, branch policy, push rules).
  - id: importance_emphasis_used
    description: >
      Does CLAUDE.md use IMPORTANT, MUST, or NEVER on at least one rule to
      mark non-negotiable constraints?
    severity: P2
    action: >
      Use 'IMPORTANT:' or 'MUST' / 'NEVER' on critical rules to improve
      adherence (per Anthropic best-practice guidance).
  - id: under_two_hundred_lines
    description: >
      Is CLAUDE.md within ~200 lines? Beyond this, models attend less
      reliably to mid- and late-context instructions (positional adherence drop).
    severity: P0
    action: >
      Trim CLAUDE.md to ~200 lines. Move long content to skills or use the
      @import syntax to load on demand.
