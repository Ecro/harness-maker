---
generated_by: harness-maker
harness_maker_version: 0.19.1
generated_at: '2026-01-01T00:00:00+00:00'
source_template: rubrics/workflow.yaml.j2
provenance: official
content_hash: bd46a9af3e1a0912ef132227b7b509bbc3c1a14758c9fb620dd1392f2059899f
---
dimension: workflow_clarity
target: .claude/commands/hm/*.md
rubrics:
  - id: command_purpose_clear
    description: >
      Does the workflow command have a one-line purpose statement directly
      under the title? A reader should know what the command does in one line.
    severity: P0
    action: >
      Add a one-line purpose summary under the command title.
  - id: usage_documented
    description: >
      Is there a 'Usage' section showing the command syntax, including
      any required arguments?
    severity: P1
    action: >
      Add 'Usage' section with example invocations.
  - id: stages_distinct
    description: >
      For fused workflows (commands that combine multiple stages), are
      the included stages distinct — no contradictions or redundant work
      between them?
    severity: P1
    action: >
      Review fused stages for overlap and remove redundant instructions.
  - id: argument_handling
    description: >
      Are command arguments parsed and described — $ARGUMENTS, flags
      (--no-auto-fix, --with-reviewers, etc.)?
    severity: P2
    action: >
      Document each accepted flag with an example.
  - id: outputs_declared
    description: >
      Does the command declare what it produces — files written, side
      effects, decisions logged?
    severity: P2
    action: >
      Add an 'Outputs' section listing files written, hooks fired, or state
      changes the command produces.
