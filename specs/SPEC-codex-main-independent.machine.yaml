schema_version: 3
spec_slug: codex-main-independent
verification_tier: 2
mutation_threshold: 70
mutation_threshold_rationale: Internal result classification can silently misreport
  success.
paths_to_mutate:
- src/harness_maker/codex_bootstrap.py
- src/harness_maker/claude_response.py
mutation_runner: python -m pytest tests/unit/test_codex_bootstrap.py tests/unit/test_claude_response.py
  -q
irreversible_decisions: []
ac:
- id: AC-001
  title: Resolve a stable engine release independently of a Codex cachebuster
  type: mechanical
  test_ids:
  - tests/unit/test_codex_bootstrap.py::test_s1_cachebuster_is_not_a_python_release
  executable_predicate: engine_requirement(manifest) == expected
  pending_test: false
  oracle_source: golden
  oracle_evidence: 'Fixed contract cases authored before implementation: the stable
    0.58.0 release mapping, three-layer completion truth table, and documented Claude
    result/structured_output envelope plus existing finding schema. Error examples
    must fail, independently of parser structure.'
- id: AC-002
  title: Reject unsupported or unsafe package identities
  type: mechanical
  test_ids:
  - tests/unit/test_codex_bootstrap.py::test_s2_unsupported_release_is_rejected
  - tests/unit/test_codex_bootstrap.py::test_s2_wrong_package_is_rejected
  executable_predicate: str(exc.value).startswith("unsupported release")
  pending_test: false
  oracle_source: golden
  oracle_evidence: 'Fixed contract cases authored before implementation: the stable
    0.58.0 release mapping, three-layer completion truth table, and documented Claude
    result/structured_output envelope plus existing finding schema. Error examples
    must fail, independently of parser structure.'
- id: AC-003
  title: Report completion only when package engine and project agree
  type: mechanical
  test_ids:
  - tests/unit/test_codex_bootstrap.py::test_s3_completion_requires_all_three_layers
  - tests/unit/test_codex_bootstrap.py::test_s3_local_plugin_identity_is_distinct_from_engine_version
  executable_predicate: result.state == expected
  pending_test: false
  oracle_source: golden
  oracle_evidence: 'Fixed contract cases authored before implementation: the stable
    0.58.0 release mapping, three-layer completion truth table, and documented Claude
    result/structured_output envelope plus existing finding schema. Error examples
    must fail, independently of parser structure.'
- id: AC-004
  title: Preserve valid structured findings including an empty opinion
  type: mechanical
  test_ids:
  - tests/unit/test_claude_response.py::test_s4_finding_fields_are_preserved
  - tests/unit/test_claude_response.py::test_s4_valid_empty_opinion
  - tests/unit/test_claude_response.py::test_s4_schema_integer_allows_zero_fraction
  executable_predicate: parse_response(envelope(payload), returncode=0) == payload
  pending_test: false
  oracle_source: golden
  oracle_evidence: 'Fixed contract cases authored before implementation: the stable
    0.58.0 release mapping, three-layer completion truth table, and documented Claude
    result/structured_output envelope plus existing finding schema. Error examples
    must fail, independently of parser structure.'
- id: AC-005
  title: Reject failed ambiguous or malformed provider output
  type: mechanical
  test_ids:
  - tests/unit/test_claude_response.py::test_s5_failures_never_become_clean_opinions
  - tests/unit/test_claude_response.py::test_s5_invalid_finding_types_are_rejected
  - tests/unit/test_claude_response.py::test_s5_success_markers_are_independently_required
  executable_predicate: exc.value.code == code
  pending_test: false
  oracle_source: golden
  oracle_evidence: 'Fixed contract cases authored before implementation: the stable
    0.58.0 release mapping, three-layer completion truth table, and documented Claude
    result/structured_output envelope plus existing finding schema. Error examples
    must fail, independently of parser structure.'
- id: AC-006
  title: Bound output bytes without echoing provider data
  type: mechanical
  test_ids:
  - tests/unit/test_claude_response.py::test_s6_size_limit_counts_bytes_and_does_not_echo_input
  executable_predicate: exc.value.code == "output_too_large"
  pending_test: false
  oracle_source: golden
  oracle_evidence: 'Fixed contract cases authored before implementation: the stable
    0.58.0 release mapping, three-layer completion truth table, and documented Claude
    result/structured_output envelope plus existing finding schema. Error examples
    must fail, independently of parser structure.'
