---
generated_by: harness-maker
harness_maker_version: 0.43.3
generated_at: '2026-01-01T00:00:00+00:00'
source_template: memory/failures.ko.md.j2
provenance: official
---
# Failures Log — Production preset

> 이 프로젝트에서 반복된 실수 / 함정을 기록합니다. wrapup 스테이지가 자동 추가합니다.
>
> **검색:** `rg -F "[fail:" .claude/memory/failures.md`
>
> **형식:**
> ```
> ## [fail:<category>] <slug> | <YYYY-MM-DD> | count:<N>
> <재현 조건 + 원인 + 해결책 한 단락>
> - [<YYYY-MM-DD>] <2번째 발생 한 줄 노트>
> - [<YYYY-MM-DD>] <3번째 발생 한 줄 노트>
> ```
> - `category`: import / test / render / hook / lint / type / runtime / design / other
> - `count`: 동일 실수 반복 시 헤딩만 업데이트 — 같은 slug → count++ (중복 섹션 금지). wrapup 이 기존 slug 를 먼저 검색해 일치하는 것을 재사용하므로 재발이 분산되지 않고 누적됨.
> - 반복마다 원본 본문 아래에 날짜별 발생 bullet 한 줄이 추가됨 (헤딩의 first-seen date 는 보존) — 따라서 `count:N` 엔트리는 N−1 개의 발생 근거 줄을 담음.
> - count ≥ 3 이면 wrapup 이 `.claude/memory/pending-proposals.md` 에 개선 제안 추가

---

<!-- @hm:user:entries -->
## [fail:design] producer-consumer-schema-drift-in-same-process-pipeline | 2026-05-17 | count:1
`ai_readiness.run_structural()` returned `{"structural": <int>, ...}` while `observability.dashboard.render_dashboard_markdown` and its unit tests read `.get("score")`. Both sides were unit-tested in isolation against their own assumed shape — no integration test ever exercised real producer → real renderer in the same call. The drift went undetected for the entire 0.13.0 release: every user's `dashboard.md` rendered `Structural score: 0 / 100` regardless of the real score. Root cause class: when an internal JSON contract is consumed only via the producing process's own pipeline (`harness-maker health` writes `.health.tmp.json`, `harness-maker health-finalize` reads it), the "internal" framing tempts callers to skip the contract test. Fix (PLAN-health-plugin-bugs-2026-05): producer-side key rename + ADR-002 round-trip contract test with floor + equality assertions + drift-catch meta-test that fakes the OLD shape and proves the safety net catches it. Pattern guard: any producer/consumer pair where one writes JSON the other parses MUST have an integration test that asserts both `producer_score >= MIN_FIXTURE_SCORE` AND `parsed_score == producer_score` (two failure modes, two assertions). Adjacent prior pattern: [fail:test] unit-fixture-skips-renderer-frontmatter — fixture vs production drift; this one is producer vs consumer drift in the same process. The same defense applies.

## [fail:design] rubric-signal-reads-canonical-file-only-not-rotation | 2026-05-17 | count:1
`readiness._dim_observability_setup` had two signals (`metrics_jsonl_present`, `metrics_has_samples`) that read only `.claude/observability/metrics.jsonl` even though telemetry rotates per-day to `metrics-YYYY-MM-DD.jsonl` (ADR-103 in `_metrics_io.py`). Result: every project with rotated telemetry got both signals reported as failing — `observability_setup` dimension dropped a free 50 points AND the user saw "Install the PostToolUse telemetry hook (run /hm:make)" on an already-correctly-instrumented project. The rotation reader `_candidate_files()` already existed and was used by `security_scanner.py` and `cache_diagnostics.py` since 0.7.1; this rubric was just never migrated. Fix: import and call `_candidate_files(obs, days=365)` for both signals; update diagnostic strings to reflect "telemetry present (N file(s))" instead of "metrics.jsonl missing". Pattern guard: when adding a new file-handling rubric, grep for existing `_metrics_io` / `_io_utils` helpers BEFORE writing a fresh `Path.is_file()` check — the rotation pattern is the dominant case in this repo and any single-file read is almost always wrong long-term.

## [fail:test] snapshot-regen-inside-worktree | 2026-05-15 | count:13
Running `tests/snapshot/regenerate.py` inside a git worktree embeds the worktree absolute path in all rendered template outputs (via `synthesize._HARNESS_MAKER_PKG_ROOT = Path(__file__).parent.parent.parent`). The resulting SHA-256 hashes diverge from hashes computed when tests run in the main repo. Fix: always run `regenerate.py` from the main repo root. This recurred again in the make-ux-gaps loop — the worktree squash-merge left snapshots with worktree-specific hashes. Recurred a third time in deep-interview-llm-delegation: regen ran from main repo BEFORE worktree finalize, so old template sha256s were generated; worktree must be finalized (stage-only) first so new templates are present in main before running regen. Correct sequence: (1) run unit tests from worktree, (2) finalize stage-only, (3) regen from main repo root, (4) full pytest from main.
- [2026-05-19] Running `tests/snapshot/regenerate.py` inside a git worktree embeds the worktree absolute path in all rendered template outputs (via `synthesize._HARNESS_MAKER_PKG_ROOT = Path(__file__).parent.parent.parent`). The resulting SHA-256 hashes diverge from hashes computed when tests run in the main repo. Recurred AGAIN during pre-launch-validation-strategy-2026-05-19 (count:7): the second execute cycle this same day re-tripped the trap when `uv run pytest -x` inside `execute-20260519T1214Z` mutated `tests/e2e/sandbox*/.claude/**` AGAIN. Same revert sequence (`git checkout HEAD -- tests/e2e/sandbox*/`) restored cleanliness pre-finalize. **Same-day double-recurrence** confirms the pending-proposals "post-finalize-snapshot-regen-hook" automation is now blocking: human discipline cannot reliably catch a mutation that happens silently in the e2e layer when a worktree-pytest is run. Promote pending-proposal priority. Fix sequence unchanged: (1) run unit tests from worktree excluding `tests/snapshot/` + `tests/e2e/`, (2) `worktree finalize stage-only` to merge to main, (3) **from main repo root** run `uv run python tests/snapshot/regenerate.py`, (4) full pytest from main passes.
- [2026-07-04] consolidated 2 duplicate entries (counts 4+7 = 11)

- [2026-07-26] SUPERSEDED — do not follow this entry. Verified 2026-07-26: tests/snapshot/regenerate.py:107-125 explicitly pins _HARNESS_MAKER_PKG_ROOT (HM_MAIN_CHECKOUT_PATH, default /home/noel/harness-maker) and _compute_install_ref (lambda: "$HOME/harness-maker"), so snapshots are worktree-invariant by construction. Regenerated from inside .worktrees/economics-attribution-and-carry four separate times this task and grepped every fixture for the worktree path: zero hits, and the only diffs were the intended content_hash changes. The guidance that produced count:11 is obsolete; regenerating in a worktree is the CORRECT action when template changes land there, and refusing to do so is what forces a hand-merge of generated artifacts at land time. Kept rather than deleted so the count history stays auditable.
- [2026-07-27] Instance 13, and the first one caught by a REVIEW rather than by CI or a land. New shape: not `regenerate.py`, but a brand-new test directory. PLAN Phase 3 added `tests/render/` with three committed golden renders captured by a helper inside the test module. `tests/render/` had no `conftest.py`, so it inherited the install-ref pin from neither of the two places that own one — `tests/snapshot/regenerate.py:111-128` and `tests/unit/conftest.py:33-61`. All three goldens baked the worktree absolute path (13 / 45 / 39 occurrences of `$HOME/harness-maker/.worktrees/reviewer-read-budget`), and because AC-009's `validator_invocation_points` conjunct then returned raw character offsets over the whole render, `test_verification_structure_unchanged` would have passed **only inside that worktree** and gone red in CI and in base the moment it landed. **The reasoning error is the interesting part, because I made it while reading this very entry.** This entry's 2026-07-26 SUPERSEDED note says renders are worktree-invariant "by construction". I checked that claim the right way — regenerated the snapshots in the worktree and grepped the fixtures for the worktree path, zero hits — and then generalised "the snapshot fixtures are clean" to "my goldens are clean". They are different artifacts produced by different code paths; only one of them was pinned. **Verifying X and asserting Y is the whole failure**, and a green check on X is what made it feel verified. It also survived my own leak-grep once: I grepped for `.worktrees`, got 6/14/12 hits, and those were template PROSE about the worktree workflow — the real signal was the worktree NAME, which I only checked after the reviewer named the file and line. **Fix:** `tests/render/conftest.py` mirrors the pin AND exposes it as a plain `pin_install_ref(monkeypatch)` function, because the golden-capture path never enters pytest and a pin the capture cannot reach is how this got in. **Gate this failure has never had in 13 instances:** `test_no_golden_bakes_a_machine_specific_absolute_path`, parametrized over every golden, asserting no `/home|/Users|/root` path survives and that the portable `$HOME/...` form is present. It checks the PROPERTY (no machine-specific absolute path) rather than the SYMPTOM (no `.worktrees`), so a capture from any other checkout fails too — and it was verified to bite by injecting one path and watching it go red. Prevention, generalised: any NEW test directory that renders templates needs the pin before it needs its first golden; and when a superseding note says a hazard is closed "by construction", find the construction and check it covers YOUR code path.
## [fail:design] worktree-finalize-pulls-orphan-wip-into-main | 2026-05-24 | count:3
**2026-05-24 closure** — 3rd recurrence triggered PLAN-worktree-cross-session-data-loss-defense (shipped 0.24 next-version): 5-layer defense (queue-guard, dirty-base-guard, Session UUID dirname embed, flock+O_EXCL fence, scope-guard). All 5 mechanisms shipped + wired into `_cli_create` / `_cli_finalize` / `_cli_post_commit_pop`. Memory wiki entry `cross-session-worktree-defense-5-layer` carries the full architecture. Status: counter freezes at 3 — 4th recurrence would mean a layer regressed; current `tests/integration/test_worktree_parallel_session.py` GREEN proves the integration boundary. Original 2026-05-19 entry follows.

After `harness_maker.worktree finalize <wt> stage-only`, the main repo's INDEX contained 139 staged files instead of my 4. Root cause: stale orphan worktrees from earlier interrupted sessions left WIP commits on their own branches (`execute-20260517T0146Z` had `744ec95 wip(execute): ...` with 127 files of unrelated work). The finalize-stage-only merge logic appears to have pulled content from siblings of the active worktree branch — or the index was already populated before this session's finalize ran (root cause uncertain). Resolution required: `git reset HEAD` to unstage all, then `git checkout HEAD -- <orphan-paths>` to drop orphan-WIP changes from working tree, then re-stage only my files. **Prevention**: before each `worktree create`, run a cleanup pass (`git worktree prune` + `git branch -D execute-*` for orphans) so a session starts from a known-clean state. Also add a wrapup pre-flight check: if `git diff --staged --stat` shows more files than the worktree's WIP commit diff, halt and surface the discrepancy. **2026-05-19 recurrence (count++)**: PLAN-readme-domain-packs-accuracy execute ran with main carrying unrelated WIP at session start (CLAUDE.md / second_brain.py / worktree.py / 2 test files from a parallel Second-Brain hardening workflow). `worktree create execute` proceeded silently; `worktree finalize stage-only` swept the unrelated WIP into a brand-new commit `7f00ace fix: model-routing review fixes (...)` on main HEAD — with an LLM-generated conventional-commit message wholly unrelated to the current PLAN. Between `/hm:review` (which only saw `M README.md` staged) and `/hm:wrapup`, the index *additionally* mutated to stage 5 unrelated files while de-staging README — the silent index drift was caught only because the wrapup pre-flight ran `git status --short`. Two new sub-patterns surface: (a) finalize-stage-only with dirty main = silent sweep with fabricated commit message (not a generic "WIP: snapshot" — a real-looking conventional-commit forged from the diff, which masks the contamination); (b) index state mutates between `/hm:review` and `/hm:wrapup` even with no user action, so the review's `drift_verdict: clean` cannot be trusted at wrapup time. **Required guards** (still pending implementation): (1) `worktree create` ABORT when base repo has unrelated dirty state, with escape hatch `--allow-dirty-base`; (2) `worktree finalize` scope-guard — if merge-back diff includes files not in worktree's own diff, halt and surface; (3) CLAUDE.md `## Multi-session worktree` section forbidding cross-session writes + LLM-side `git status` check before `worktree create`; (4) session-id marker per worktree so cleanup only touches own. Tracked as a separate PLAN.

## [fail:design] yaml-safe-load-on-multi-doc-harness-yaml | 2026-05-17 | count:1
`second_brain._load_config` used single-document `yaml.safe_load` against `.claude/harness.yaml`, which the renderer always wraps in a provenance frontmatter block (`generated_by`, `content_hash`, …). The resulting file is a multi-document YAML stream, so every Second Brain CLI invocation crashed with `yaml.composer.ComposerError: expected a single document in the stream`. The user reported "Obsidian에 저장이 안 되던데" — the connector had been 100% non-functional since whenever the provenance frontmatter contract landed. Fix: `harness_maker.io_utils.load_harness_yaml(path)` central helper (`safe_load_all`, filter `generated_by: harness-maker` docs, return last non-provenance mapping). Pattern guard: any new `harness.yaml` reader MUST import the helper instead of rolling its own — CLAUDE.md §"외부 소비자의 파서 정합성 확인" now lists `.claude/harness.yaml` explicitly with the provenance-frontmatter contract called out, and `docs/followups/io-utils-migration.md` tracks the staged migration of pre-existing direct readers (`verify.py`, `worktree.py`).

## [fail:test] unit-fixture-skips-renderer-frontmatter | 2026-05-17 | count:1
`tests/unit/test_second_brain.py:_write_harness_yaml` built fixture harness.yaml via `yaml.safe_dump(payload)` — single-document, no provenance frontmatter. Production reads renderer output which always carries the frontmatter block. Result: unit suite passed for months while production crashed on every CLI call. Fixture-vs-production drift is a recurring class — the same defect would catch any new file type the renderer writes (frontmatter-wrapped) and any new consumer test that fakes the file shape. Fix (defense-in-depth pair): (a) update `_write_harness_yaml` to inject the provenance frontmatter, (b) add `tests/integration/test_second_brain_e2e.py` that invokes `harness_maker.render.render` LIVE without snapshot-pinning so future renderer-vs-consumer drift fails in CI. Snapshot-pinning the rendered bytes would defeat the test's purpose — the security/code review's W8 finding explicitly documents this trap.

## [fail:design] yaml-key-value-name-mismatch-llm-footgun | 2026-05-15 | count:1
harness.yaml's snake_case YAML key `work_docs:` has a hyphenated value `work-docs/` — LLMs reading the config in an ad-hoc session (no stage template loaded) wrote `work_docs/PLAN-...md` (snake_case directory) instead of `work-docs/PLAN-...md`. Observed in `~/edge_testfarm_os/work_docs/PLAN-daily-architecture-review-20260514.md` with frontmatter `validator_outcome: SELF_REVIEW` (NOT a /hm:plan standard value) + no `harness_maker_version` — confirms ad-hoc-LLM not /hm:plan-pipeline origin. **No literal `work_docs/` directory reference exists anywhere in src/.** Pattern: one-character name asymmetry between YAML key and value is an LLM footgun, regardless of which convention is "right" (snake_case vs kebab-case). Fix landed in 0.11.6: two-layer guardrail — preventive warning paragraph in 4 stage templates' `## Outputs` (Layer 1) + non-blocking bash probe in `verify.md.j2` stage body (Layer 2, NOT in the `verify-before-completion` SKILL — preserving "6 checks" doc-truth invariant). Pattern guard: when designing a new harness.yaml key whose value is a path, prefer aligning key name to value name to eliminate the disambiguation cost entirely (`plans_dir: plans/` over `plans-dir: plans/`).

## [fail:design] prompt-builder-prose-without-data | 2026-05-11 | count:1
`build_pass2_prompt(diff: str, findings, full_context)` accepted `diff` as a parameter but never appended it to the `parts` list assembled into the returned prompt string. The instruction in the prompt body ("Validate each finding against the full context") had no `## Diff` section to reference — Pass 2 reviewers received only metadata + finding summaries. Caught by code-reviewer in `/hm:review llm-code-review-2026` as P1 single-source finding; verified by direct file inspection (parts list at lines 120-137 had title/desc/author/findings + invariant prose, no diff fence). Fix: explicit `f"## Diff\n```\n{diff}\n```"` block added before the Pass 1 Findings heading. Pattern guard: when a prompt builder accepts a data parameter, the parameter must appear in the body assembly OR be removed from the signature; mismatched signatures vs body assembly are a class of latent bug.

## [fail:test] typer-cli-runner-mix-stderr | 2026-05-09 | count:1
`CliRunner(mix_stderr=True)` raises `TypeError` — typer's `CliRunner.__init__()` does not accept `mix_stderr`. Only `unittest.mock`'s Click-based TestCase variant accepts that kwarg. Fix: remove `mix_stderr=True`; `result.output` in typer's CliRunner already captures both stdout and stderr by default.

## [fail:runtime] subprocess-missing-timeout | 2026-05-09 | count:1
`_run()` in `worktree.py` was written without `timeout=` on `subprocess.run()`. A hung git command (SSH auth prompt, NFS stall) would block the CLI indefinitely. Found by security-reviewer + concurrency-reviewer consensus (P1). Fix: `_GIT_TIMEOUT = 60` constant + `timeout=_GIT_TIMEOUT` in `_run()` + `except subprocess.TimeoutExpired as e: raise RuntimeError(f"timed out after {_GIT_TIMEOUT}s: ...") from e`. CLAUDE.md §외부 명령 호출 explicitly requires `timeout=N` — the rule was in CLAUDE.md from the start but missed during Phase 2 implementation.

## [fail:design] return-type-change-breaks-callers | 2026-05-09 | count:1
`worktree.create()` return type changed from `Path` to `list[Path]`. The e2e test at `tests/e2e/test_dogfood_sandbox.py:143` called `.exists()` on the returned value — which now returns a list, causing `AttributeError: 'list' object has no attribute 'exists'`. Caught by code-reviewer (finding E). Fix: `worktree.create("dev", repo)[0]`. Pattern: when changing a function's return type from scalar to container, grep ALL callers (unit tests, e2e tests, CLI entrypoints) before finalizing the change. The unit tests were updated but e2e was missed.

## [fail:test] boundary-test-no-sentinel | 2026-05-09 | count:1
`test_git_as_file_stops_walk` asserted `_find_marker(subdir) is None` but planted no marker above the boundary. The test passed regardless of whether the boundary guard worked, because no marker happened to exist above `tmp_path` in the test environment. Fix: always plant a marker ABOVE the boundary in a `try/finally` block so the test fails if the walk ignores the boundary. Pattern: boundary tests must prove both "find it when it should be found" AND "don't find it when the boundary blocks."

## [fail:test] class-methods-orphaned-by-partial-edit | 2026-05-10 | count:1
`TestCheckErrorCap` had 8 methods. The `Edit` tool's `old_string` captured only the first 3 methods (through `test_syntax_over_cap`), leaving the remaining 5 (`test_logical_under_cap`, `test_logical_at_cap`, `test_unknown_under_cap`, `test_unknown_at_cap`, `test_empty_counts_safe`) as un-indented dead defs. After the worktree merge, those 5 methods orphaned as nested function definitions inside the next test function (`test_s8_loop_context_backward_compat`). They compiled fine but were unreachable by pytest. Fix: always read the full class body before writing `old_string`; when editing a class, the `old_string` must include the full class through its last method (or use a marker at the class close). Grep for all `def test_` under a class before committing to the `old_string`.

## [fail:review] abbreviated-diff-causes-reviewer-false-positives | 2026-05-10 | count:1
When the review prompt for a large diff abbreviates template content with inline placeholders like `[...same 4-axis check, plan-specific dimension labels...]` or `[... Full gate text: 53 lines ...]`, reviewers interpret those placeholders as literal file content (stub/placeholder text in the template). In this case, code-reviewer raised P1 findings against plan.md.j2 for "Layer 1 placeholder stub" and "Full gate text stub" — both false positives. Fix: verify against actual file content with grep before applying single-source findings; drop false positives in the consensus step. Prevention: never abbreviate template body content in the diff context passed to reviewers; if the diff is too large, pass the full file via Read instead of an inline summary.

## [fail:render] toml-section-header-variable-injection | 2026-05-10 | count:1
Jinja2 template used `[mcp_servers.{{ server_name }}]` in `config.toml.j2`. TOML interprets unquoted dotted keys as nested table hierarchies — a server name like `server.name` creates `mcp_servers["server"]["name"]` (wrong nesting) instead of `mcp_servers["server.name"]` (intended). The `_render_pure_toml()` validator passes because the output IS valid TOML, just with unexpected structure. Caught by security-reviewer in review. Fix: `[mcp_servers."{{ server_name }}"]` — TOML quoted-key syntax safely handles dots, spaces, and special characters. Rule: any Jinja2 variable injected into a TOML section header key MUST use `"{{ var }}"` quoting.

## [fail:render] yaml-colon-in-unquoted-frontmatter-description | 2026-05-10 | count:1
`skills/conditional-router/SKILL.md.j2` description contained `` `routing: conditional` `` unquoted in YAML frontmatter. `yaml.safe_load()` in `_split_template_frontmatter()` raised `YAMLError` (mapping values not allowed), function returned `({}, rendered)` — frontmatter not extracted. Renderer prepended provenance block on top of the still-present template frontmatter, producing two `---` blocks. Codex reports "missing field description" since it only reads the first block. Fix: double-quote any description that contains `: ` sequences. The silent `except yaml.YAMLError: return {}, rendered` path makes this failure non-obvious — no CLI warning, no verify error, only an IDE-level symptom.

## [fail:design] codex-helpers-ignore-user-config | 2026-05-11 | count:1
`_codex_stage_skills()` and `_codex_target_files()` each constructed `HarnessConfig()` locally, so rendered Codex skill bodies always embedded factory-default paths and workflow names regardless of the user's `harness.yaml`. Root cause: both functions were implemented as standalone helpers with no access to the live `config_dump` from `synthesize()`; `synthesize()` also called them BEFORE computing `config_dump`. Review caught this as two independent 2/2 consensus-passed P1 findings. Fix: move `config = HarnessConfig(...)` and `config_dump = config.model_dump()` to the TOP of `synthesize()` (before file_specs), add `config_dump: dict[str, object] | None = None` parameter to both helpers, thread via `_codex_target_files(fused_workflows, config_dump=config_dump)`. Added config-threading verification tests (`test_codex_stage_skills_uses_passed_config_dump`, `test_codex_target_files_threads_config_dump_to_stage_skills`).

## [fail:review] reviewer-subagent-model-unsupported | 2026-05-11 | count:3
**2026-05-31 recurrence (count++, Claude side, now structurally fixed):** the same symptom — reviewer/validator subagents failing to launch (0 tool uses) — recurred in `claude-opus-4-8` sessions because the rendered `.claude/agents/*.md` `model:` frontmatter carried the stale **Cursor concrete id** `claude-4-7-opus`. Root cause: the dispatcher templates preferred `{{ cursor_model }}` over `{{ claude_model }}`, harmless while Claude Code ignored the field (#43869) but load-bearing once upstream fixed it. Fix (PLAN-agent-model-version-agnostic): agent frontmatter now renders the Claude **alias** via a shared partial; `default_model` floor → `opus`; regression guard `test_agent_model_alias_rendering` fails if any concrete id reaches an agent `model:` line. This is the structural prevention the Codex-side resolution (below) only half-covered — both IDE namespaces now avoid pinning. Original 2026-05-11 entry follows.
During `/hm:review`, configured reviewer agents failed to spawn because their fixed `o4-mini` role model is not supported for this Codex account. The stage recovered by recording reviewer unavailability and completing a local orchestrator review plus full test verification, but true cross-reviewer consensus was unavailable. Same root cause hit `/hm:plan` Step 4 plan-validator dispatch (the "o4" string also rejected on ChatGPT-tier). **Resolution (2026-05-11)**: dropped per-agent `model = "..."` from rendered `.codex/agents/*.toml` (ADR-001 / PLAN-codex-plan-validator-model-unavailable). Codex now inherits the user's `~/.codex/config.toml` profile default. Live probes confirmed `gpt-5.5` works on this account while `o4`, `o4-mini`, `gpt-5-codex`, `gpt-5.5-codex` all return HTTP 400. Template gate `{% if model_codex %}` preserved for a deferred opt-in HarnessConfig knob.

## [fail:render] yaml-empty-list-renders-null | 2026-05-11 | count:1
Jinja2 rendering `folders:` with no list items produced YAML null instead of `[]`, so `SecondBrainConfig` strict validation rejected the default rendered config and `answers_from_harness_yaml` silently fell back to disabled defaults. Fix: branch in YAML templates and render `folders: []` when empty; add a round-trip test that default `second_brain` reuses without warning. Pattern: every optional rendered YAML list needs an explicit empty-list representation, not a bare key.

## [fail:workflow] worktree-merge-stale-base | 2026-05-11 | count:1
`worktree finalize stage-only` failed with `error: Your local changes to the following files would be overwritten by merge` because the worktree's branch was created from `bc19c26` but main advanced to `dbfdf24` (another stage's commit) before this stage tried to merge back. `git merge --squash` from the older base detects "conflicts" with the new main commits on the same files even when the modified regions don't overlap. From the worktree the symptom looked like main had 53 modified files staged/unstaged (the entire post-`bc19c26` delta showing as "uncommitted" because the worktree didn't have those commits in its base). Resolution: `cd <WT> && git rebase main` (main repo is local — no remote needed), then retry `worktree finalize stage-only`. Rebase is clean when the new main commits don't touch the worktree's modified regions; conflicting regions require manual resolution. Diagnostic: run `git status` inside the **main repo** path (not the worktree) — if main itself shows clean, the "WIP" you see from the worktree is the new main commits the worktree hasn't caught up to.

## [fail:lint] wrapup-ruff-preexisting-e501 | 2026-05-12 | count:1
The wrapup full lint gate failed on a pre-existing `src/harness_maker/cli.py` E501 line in the Second Brain override path, outside the staged onboarding UX diff. Fix: split the conditional expression across multiple lines before proceeding, then rerun `uv run ruff check src/ tests/` and full pytest. Pattern: wrapup verification can expose unrelated lint drift; when the fix is formatting-only and required for the gate, apply the smallest change and log it as advisory drift.

## [fail:hook] sessionstart-additionalcontext-invisible | 2026-05-13 | count:1
SessionStart drift hook emitted `hookSpecificOutput.additionalContext` but the user reported "전혀 뭘 알려주지 않음" — never saw any drift notification. Root cause: `additionalContext` is fed into Claude's internal context but is NOT rendered to the user's terminal. To produce a visible notification, the hook must also emit `systemMessage` in the same `hookSpecificOutput` block. Fix: split formatter into `_format_context` (Claude-facing, long) + `_format_system_message` (user-facing, short banner with ⚠ prefix). Pattern: before claiming a hook "works", verify which output channel actually surfaces to the user vs which feeds Claude silently.

## [fail:design] install-ref-editable-only-check | 2026-05-13 | count:1
`_compute_install_ref()` assumed `editable=true` was the only signal of a local install. Claude Code's `/plugin install` produces an install with `editable=false` AND `direct_url.url = "file://..."` (the plugin cache). The editable-only branch returned `"harness-maker"`, which `uv run --with harness-maker` failed to resolve since harness-maker is not on PyPI. Cascade: every SessionStart drift hook in every project that installed via `/plugin install` silently failed with "No solution found when resolving --with dependencies". Fix: check `direct_url.url.startswith("file://")` instead of `dir_info.editable`. Pattern: when a function disambiguates "local vs remote" install, branch on URL scheme (file:// vs https://) rather than mode flags — the mode flag captures one local variant, the URL scheme captures all of them.

## [fail:design] expanduser-after-path-join-silent-noop | 2026-05-13 | count:1
`refdocs_index._build_block` did `(harness_root / rf.path).expanduser().resolve()`. When `rf.path = "~/edge_bsp_foundation"`, the join produces `<harness_root>/~/edge_bsp_foundation` — `~` is no longer at position 0, so the subsequent `.expanduser()` is a silent no-op. Downstream resolve() yielded a bogus path under `harness_root`, the existence check failed, and the index emitted `ref_folder not found or not a directory` with the `~`-prefixed resolved path in the warning — visually confusing because the warning text contained `~` (showing both the input AND the broken resolution result). Found during end-to-end sanity check immediately after shipping the `denormalize_home_to_tilde` fix: storage normalization succeeded, but the indexer rejected the stored value. Fix: expand FIRST (`raw = Path(rf.path).expanduser()`), then conditionally join only if still relative. Pattern: `.expanduser()` belongs at the boundary where a user-supplied path is first lifted into Pathlib — never AFTER a join. Co-discovered with the parent shell-expansion bug because both halves of the round-trip must work for tilde paths to be usable.

## [fail:design] cli-path-tilde-expansion-shell-layer | 2026-05-13 | count:1
ref_folders / second_brain_vault_path entered as `~/foo` got stored as `/home/alice/foo` (absolute) in harness.yaml. The Python code (`_parse_ref_folders_flag`, `RefFolder` validator, `refdocs_index._build_block`) does NOT expand `~`. Actual culprit was bash: `VAR=~/foo` is unquoted at assignment, so the shell expands `~` to `$HOME` before Python ever sees the value. Fix lives at the CLI boundary (`denormalize_home_to_tilde` in `io_utils.py`) — re-prefix `$HOME`-rooted absolutes with `~` AFTER the shell has clobbered them. Pattern: when a path transformation is observed in CLI-received input but the Python code shows no such logic, blame the shell quoting layer first; the simplest test is to call the function directly from pytest and confirm the bug only reproduces via the shell.

## [fail:render] wrapup-eof-append-outside-marker | 2026-06-20 | count:3 | previous_count:2
**2026-06-20 recurrence (count++→3, crosses proposal threshold)**: parent commit `7ac9c30 feat(autonomy): never-auto autopilot_guard hook (phase 4 of 9)` — the P4 wrapup wiki append wrote the `never-auto-guard-marker-gated-not-static-deny` entry directly OVER the `<!-- @hm:/user:entries -->` close-marker line, deleting it entirely (same as the 2026-05-20 form). All 132 wiki entries went invisible to `memory_retrieve` (returns `[]` on missing close marker), surfaced only by `test_cli_real_repo_memory_surfaces_recent_entry` failing on base HEAD. Detection was delayed because the red was first mis-triaged as a "brittle top-K test" until `grep -c "@hm:/user:entries" wiki.md` = 0 + `git show 477fe40:...|grep -c` = 1 pinned it to the P4 wrapup. Fixed in separate commit `5d5ea1e fix(memory)` (restored marker, no test weakened — the test was correctly catching real corruption). Count now 3 → pending-proposal written. The standing prose prevention (name the marker explicitly + insert ABOVE it) has now failed 3×, so the proposal is for a MECHANICAL guard (post-wrapup `grep -c "@hm:/user:entries"` ≥1 assertion, or a `parse_entries` stderr warning naming the file) rather than another prose reminder.

**2026-05-20 recurrence (count++)**: parent commit `ef79688 feat(worktree): transparent stash isolation in finalize+wrapup` appended two new wiki entries (`worktree-finalize-stash-isolation`, `interview-mid-round-reframe-pattern`) directly OVER the `<!-- @hm:/user:entries -->` close marker — same EOF-append footgun, this time the close marker was deleted entirely. Detection delayed because the visible failure surfaces only at the memory_retrieve CLI / `test_cli_real_repo_memory_surfaces_recent_entry` test which is INTEGRATION-tier — local wrapup verification ran `uv run pytest tests/ -q --ignore=tests/integration` and missed it. CI `uv run pytest -x` caught it. Two new prevention surfaces this round: (a) wrapup verification command MUST include integration suite when `.claude/memory/wiki.md` or `.claude/memory/failures.md` is touched in the diff (not just `tests/integration/*` paths); (b) `harness_maker.memory_retrieve.parse_entries` returns `[]` when close marker missing — should at minimum emit a stderr warning naming the file so a regression surfaces louder than a silent zero-result. Fix this time: simple `git diff` showed the deletion, restored marker, all 5 tests in `test_memory_retrieve_cli.py` green. Original 2026-05-17 entry follows.

`/hm:wrapup` Step 5 instruction said "Append (or update) one entry under `.claude/memory/wiki.md`" with no positional constraint. LLM naturally interpreted "append" as EOF-append → wiki entries landed AFTER the `<!-- @hm:/user:entries -->` closing marker. `block_merge.merge()` (block_merge.py:17-18 contract) treats outside-marker content as template-owned → silent REPLACE on next `/hm:make --update`. Compound footgun: CLI emitted `MERGE_BLOCK: ... preserved 1 user block(s): entries` — technically true (empty block survived) but masked the 33-line orphan drop entirely. 4 wiki entries dated 2026-05-17 (communication-variant-pre-render-extractor, codex-render-bypasses-dispatcher-source, frontmatter-source-vs-output-separation, workflow-optimization-inverted-env) vanished across 7 commits before detection. Three-layer fix: (1) wrapup.md.j2 5.1/5.2 now name `<!-- @hm:user:entries -->` explicitly + cite this regression in prose. (2) `MergeReport.orphan_outside_content` populated via `Counter` subtraction; cli.py surfaces dropped-line count without echoing content (the preview itself would leak user-controlled bytes to Bash stdout → LLM next-turn context, opening a stored-prompt-injection vector — the very feature added to "make the bug visible" almost reintroduced a different bug). (3) `merge()` walk gained fence tracking (was diverging from parse_segments — validator passed, executor misbehaved on fenced literal markers). Pattern: any wrapup-stage prompt that touches a marker-bearing file MUST name the marker; "append" is a footgun in this codebase.

## [fail:test] template-owned-key-as-user-example-broken | 2026-05-19 | count:1
`test_render.py::test_render_harness_yaml_preserves_user_added_top_level_key` used `memory:` as the exemplar "user-added top-level key the template doesn't emit". Phase 1 of PLAN-fresh-install-health-baseline made `memory:` template-owned (ADR-002 baseline). Per `_preserve_yaml_user_keys` docstring at render.py:640-643 ("Template-emitted keys always win on overlap"), the next render writes the template's `memory:` block, the test's `body["memory"]["wiki"] == ".claude/memory/wiki.md"` KeyErrors because the new dict has no `wiki` field. Fix: switch the user-extension example to `project_notes:` (template-never-emit). Pattern: any test verifying user-extension preservation MUST use a key the template provably never claims — if a future ADR makes that key template-owned, the test silently breaks. Sniff-check: `grep -rn "memory" tests/unit/` before introducing a new top-level template key.

## [fail:design] phantom-key-on-rerender-breaks-idempotency | 2026-05-19 | count:1
`render._merge_permissions` (render.py:180-209) emitted `out[key] = []` for every key in `_PERMISSIONS_LIST_KEYS = ("allow", "deny", "ask")` regardless of whether the key was present in `new_perms` or `existing_perms`. First render: only `allow + deny` present (templates don't emit `ask`). Second render: file from first run has no `ask` key either, but the function still wrote `"ask": []` because the loop unconditionally assigned. Byte-identical rerender assertion in `tests/integration/test_fresh_install_readiness.py::test_render_idempotent_byte_identical` caught it. Fix: guard with `if not new_list and not existing_list and key not in new_perms and key not in existing_perms: continue`. Pattern: when a function "deep-merges" with a fixed key list, the loop MUST NOT emit phantom keys for slots absent in BOTH sides. Idempotency invariant: `merge(out, out) == out` (fixed point).

## [fail:design] readme-prompt-embeds-built-in-slash-as-runbook | 2026-05-19 | count:1
README "Universal Bootstrap Prompt" instructed the AI to "run `/plugin marketplace add ...`" + "`/plugin install ...`". These are **built-in Claude Code slash commands**, NOT skills — they can be typed by the user only, and an assistant cannot fire them via `Skill` tool (built-in CLI commands explicitly excluded per the tool description) nor via `Bash` (subshell can't reach the parent session). Result: the AI reading the prompt redirected typing back to the user — the exact opposite of the "one-paste, AI does everything" promise. Symptom: user sees "Please run `/plugin install harness-maker@harness-maker`" in chat after pasting the bootstrap prompt. Fix: prefix every install line with `Bash:` so the AI calls `claude plugin install` (and `git clone`, `codex plugin marketplace add`) as Bash tool_use. The irreducible user-typed step that DOES remain is `/reload-plugins` after install — that one really must be user-typed. Pattern: when designing AI runbooks, distinguish `Skill(name)` (assistant-callable), `Bash: <cmd>` (assistant-callable via Bash tool), and `/builtin-command` (user-only). Don't mix the last category into instructions addressed to the AI.

## [fail:design] worktree-finalize-stage-only-merges-against-shifted-main | 2026-05-19 | count:1
`harness_maker.worktree finalize <WT> stage-only` does `git merge --squash <wt-branch>` into main. If main was modified during the worktree's lifetime by another session (sandbox-plugin-test fixtures regenerated, CHANGELOG release entry committed, ...), the squash-merge produces conflicts on files the worktree never touched — observed 118 unmerged files in a single execute stage exit. The CLI message was "merge failed, preserving worktree" but no resolution guidance. Resolution playbook: (1) `git checkout --ours -- <fixture-dirs>; git add <fixture-dirs>` for paths the worktree didn't intend to edit; (2) Manually edit conflicted files where intent crossed (CHANGELOG [Unreleased] vs new release section); (3) After all UU paths resolved + staged, manually clean up: `git worktree remove --force <WT>; git branch -D <wt-branch>`. The finalize CLI does NOT retry-after-resolution — clean up by hand. Pattern: long execute stages (multi-round-interview → multi-phase implementation → multi-pass review) widen the window for parallel main edits; conflict probability rises with execute wall-clock time.

## [fail:design] llm-cannot-verify-action-sha-at-write-time | 2026-05-19 | count:1
Symptom: PLAN-oss-readiness-audit Phase 1 required restoring PR CI with all GitHub Actions SHA-pinned (matching the existing release.yml convention). Executing LLM wrote `actions/dependency-review-action@<FABRICATED_SHA> # v4.3.4` — the SHA was hallucinated and would have either (a) failed CI with "ref not found" or (b) silently resolved to an unintended commit. Caught only because the LLM self-flagged it ("I cannot verify SHAs without network access") and replaced the fabrication with `@v4 # TODO(maintainer)`. Security + code reviewers (2/3 consensus, P1) then surfaced the leftover mutable tag as a supply-chain risk because the job carries `pull-requests: write`. Cause: any prompt that asks an LLM to "SHA-pin a GitHub Action" while the LLM has no network to fetch the actual SHA puts the LLM into a hallucinate-or-defer corner. Hallucinate produces an unverifiable artifact; defer leaves a supply-chain hole. Fix pattern: when LLMs author CI workflows, leave a structured TODO marker (`# UNVERIFIED-SHA: actions/x@v4`) and emit a separate hand-off task to the human for SHA lookup. NEVER let the LLM invent a SHA. Adjacent pattern: any `image:`, `digest:`, or content-hash field that requires upstream-state verification has the same failure mode. Update `code-reviewer` and `security-reviewer` prompts to specifically flag invented hashes/SHAs as P1 supply-chain.

## [fail:design] tee-pipe-loses-pytest-exit-code | 2026-05-19 | count:1
A GitHub Actions workflow step ran `uv run pytest ... 2>&1 | tee /tmp/out.txt` followed by `echo "exit_code=$?" >> "$GITHUB_OUTPUT"`. In bash without `set -o pipefail`, `$?` after a pipeline reflects only the rightmost command (tee), which exits 0 on any successful disk write regardless of pytest's outcome. The captured exit_code was always 0, and the downstream status label always wrote ✅ PASS even when pytest had failures. Silent. Caught by code-reviewer + security-reviewer in REVIEW-test-fidelity-gap (consensus-passed P1) before the workflow ever ran on a real release. Fix: replace `$?` with `${PIPESTATUS[0]}` to grab pytest's exit code specifically. Pattern guard: any workflow step that pipes a fallible command through `tee` (or any other rightmost-always-0 wrapper) for log capture MUST use `${PIPESTATUS[0]}` or `set -o pipefail` — `$?` after a pipeline is a footgun. Audit all CI scripts for the pattern: `grep -rn '| tee.*$' .github/workflows/` and verify each instance uses PIPESTATUS or pipefail.

## [fail:design] gh-release-view-failure-overwrites-notes | 2026-05-19 | count:1
A workflow step appended advisory test results to a GitHub Release body via the sequence: `gh release view "$TAG" --json body -q .body > "$NEW_NOTES"`, then append, then `gh release edit "$TAG" --notes-file "$NEW_NOTES"`. With no error guard on the fetch, a transient `gh` API failure or a partial `github-release` job leaves `$NEW_NOTES` empty — and the subsequent edit overwrites the CHANGELOG-derived release notes with only the appended advisory section. Catastrophic blast radius (release notes destroyed) for a low-probability fault. Caught by security-reviewer as manual-only P1 in REVIEW-test-fidelity-gap. Fix: `set -euo pipefail` at the top of the step + `if ! gh release view ... > "$NEW_NOTES"; then echo WARNING...; exit 0; fi` — skip the append on fetch failure (the existing release notes remain untouched). Pattern guard: any workflow step that reads existing remote state into a local file and then writes it back to that same remote state must guard against the fetch failing — without the guard, the write step overwrites with whatever the fetch happened to produce, including empty.

## [fail:lint] wrapup-final-verify-skips-ruff-format-check | 2026-05-20 | count:3 | previous_count:2
**2026-05-20 recurrence (count++ → 3)**: PLAN-worktree-stash-phase4 wrapup verification ran `ruff check src/harness_maker/worktree.py` + `mypy --strict` (both clean) but skipped `ruff format --check` — push of 5ed9dcd triggered CI fail in 10 seconds on the format step (4 files would-reformat). Same exact pattern as the 2× prior occurrences. User pushed back: "ci 가 fail 안되도록 먼저 확인하고 올릴 수는 없나???" The miss was structural: wrapup's bash gate block ran ruff/mypy/pytest but the wrapup template's verification example list does NOT include `ruff format --check`, so the LLM mirrors the visible list. **Resolution path that worked this round**: rerun all 4 CI gates locally in exact order (`ruff check . && ruff format --check . && mypy --strict src && pytest -x --tb=short`) BEFORE the next push. Lesson re-confirmed for the 3rd time: any new code+tests commit needs the **same command CI runs**, copy-pasted, before push — not a paraphrased subset. Wrapup template at `templates/stages/wrapup.md.j2` still needs the format-check added (deferred 3 times now; promote to active PLAN). Original 2026-05-19 entry follows.

Symptom: wrapup Step 2 final verification ran `uv run ruff check` (lint only) and called it green; subsequent push triggered CI failure 13 seconds in — `Ruff format check` step diff-failed on 4 newly-added files (auto-format-on-save was OFF during execute). Required a follow-up `chore: apply ruff format` commit (e061bd2 in 2026-05-19; same pattern as 0.15.0's b572061 — confirming this is recurring, not first-time). Cause: the wrapup stage template's "## Step 2 — Final verification pass" example block lists `uv run ruff check src/ tests/` but does NOT include `uv run ruff format --check .` alongside it, so LLMs running wrapup copy the visible commands and skip format. CI's `quality-gate` job runs BOTH (release.yml/ci.yml/nightly.yml all carry `ruff format --check .` as a separate step), so push-time fail is guaranteed. Fix at the wrapup level: run BOTH commands during Step 2 — `uv run ruff check src/ tests/ && uv run ruff format --check .`. The format-check is fast (<1s) and catches what `ruff check` doesn't (whitespace, line-break placement, import grouping). Pattern guard: whenever a project's CI has a `ruff format --check` step distinct from `ruff check`, the wrapup verification MUST mirror both — checking only one creates the "wrapup green, CI red" surprise. Permanent fix needs `templates/stages/wrapup.md.j2` edit (a separate PLAN) to add `ruff format --check` to the example commands block; this memory entry serves as the interim guard. Adjacent: any tool with both a `check` and a `--check` variant (`ruff check` vs `ruff format --check`) is a footgun — they look interchangeable but enforce different invariants.

## [fail:design] pydantic-model-copy-bypasses-field-validators | 2026-05-19 | count:1
Pydantic v2 `base.model_copy(update={...})` does NOT run field validators on the updated keys (this is documented Pydantic behavior, not a bug). In `interview.answers_from_harness_yaml` the user-provided `default_model` (or deprecated `recommended_model`) was assigned to `update["default_model"]` then passed through `model_copy(update=update)` — the `_validate_default_model_chars` field_validator declared on `HarnessConfig.default_model` is bypassed. Result: a `harness.yaml` with `default_model: "claude-opus\ntools: [Write(*)]"` survives the load, propagates as `claude_model` Jinja context, and renders as injected YAML in `.claude/agents/*.md` frontmatter — agent-permission escalation. The regex `^[a-zA-Z0-9_.:-]+$` was correctly anchored; it just never ran. Fix: validate AT THE SOURCE before the `update` dict is built — in `interview.py` add `_MODEL_ID_PATTERN.fullmatch(cleaned)` guard before `update["default_model"] = cleaned`. Surfaced by Phase 4 security-reviewer; plan-author had verified the regex in Phase 2 and marked the path "safe" (R-6) — wrong, because the validator never ran. Pattern guard: whenever Pydantic v2 `model_copy(update=...)` or `model_validate(strict=False)` is used in a load path, the field validators on touched fields must be replicated as explicit pre-checks at the assignment site. The "validator declared on the model" guarantee only holds for normal `Model(**data)` construction, not for the bypass paths.

## [fail:render] jinja-is-defined-returns-true-for-none | 2026-05-19 | count:2
**2026-06-17 recurrence (count++, the mirror-image trap):** PLAN-locale-and-command-observability `output_language.md.j2` needed an optional-with-falsy-default locale guard. First try used `is defined` alone (`config.locale is defined`) → True for present-but-None/'' → rendered `**None**`/`****` (the original bug below; caught by code-reviewer P2). The *fix attempt* then over-corrected to TRUTHINESS-only (`config is defined and config.locale`) → this **RAISES `UndefinedError`** under StrictUndefined when the `locale` key is ABSENT (a partial config dict, e.g. `test_codex_phase4`'s `_BASE_CONFIG`), because evaluating an absent `config.locale` in a boolean `and` triggers StrictUndefined's `__bool__`. The full suite caught the regression; the converged guard needs ALL THREE terms: `config is defined and config.locale is defined and config.locale` — `is defined` first (no raise on absent key), THEN truthiness (None/''→default). Rule for "optional key, may be absent OR falsy, want default": presence-gate before truthiness; neither alone suffices. Original 2026-05-19 entry follows.

Jinja2's `{% if x is defined %}` test returns True for any context key present, **regardless of its Python value** — `None` passes. The 14 agent dispatcher templates in `templates/agents/*.md.j2` guarded with `{% if claude_model is defined %}{{ claude_model }}{% else %}sonnet{% endif %}`. `synthesize._agent_files` always set `claude_model: spec.claude` in the context (key present even when value is `None`). A user override like `agent_models: {autoloop-coder: {cursor: opus}}` produces `AgentModelSpec(claude=None, cursor='opus')` → context has `claude_model=None` → template emits literal `model: None` in YAML frontmatter → agent dispatch null'd. Empirically verified via the CLI: `Environment().from_string('{% if x is defined %}{{ x }}{% else %}sonnet{% endif %}').render(x=None)` → `'None'` (literal Python `str(None)`). Fix: use both checks together — `{% if claude_model is defined and claude_model is not none %}`. Pattern guard: in Jinja2, `is defined` is a *presence* check, not a *truthiness* check. For any key the producer might set to `None`, pair `is defined` with `is not none`. Adjacent: Phase B caught a related false-RED escape from my regex `[a-zA-Z0-9_.:-]+` (matches "None" alphabetically) — so the negative assertion `model: None not in rendered` is the safer shape, OR parse YAML and check the actual value.

## [fail:test] regex-permissive-on-literal-alpha-tokens | 2026-05-19 | count:1
Phase B false-RED escape: my regex `_VALID_MODEL_LINE = re.compile(r"^model:\s+[a-zA-Z0-9_.:-]+\s*$")` was supposed to assert "model: line carries a concrete identifier, not a None-equivalent". But the alphabet `[a-zA-Z0-9_.:-]` ALSO matches the literal string `"None"` (all letters). Result: when the template emitted `model: None` (the bug under test), the regex matched it → test PASSed → RED gate falsely cleared. Test-reviewer's Round 1 critique had widened the assertion from the original narrow `"model: None" not in rendered` to a "positive concrete-ID regex" check; the rewrite traded narrow-correctness for permissive-incorrectness. Fix: YAML-parse the frontmatter and check the parsed Python value (catches `model: None`/`null`/`~`/empty/Python-None in one assertion). Pattern guard: when test-reviewer asks to widen a narrow assertion, evaluate whether the wider form admits false-pass via the alphabet of the regex/charset. If the bug value is in the wider form's alphabet, EITHER add an explicit exclusion of the bug-value OR use parsing instead of pattern-matching. `regex` is the wrong tool for "this string is not the Python repr of None".

## [fail:review] reviewer-hallucinates-tool-output-format | 2026-05-19 | count:1
code-reviewer claimed a P1 bug in `tests/integration/test_boundary_meta.py:87`: the regex `<filename>:\s*(\d+)` would never match because pytest 9.x `--collect-only -q` (per the reviewer's claim) emits only `path/to/file.py::test_name` lines, no `<filename>: <count>` summary. The claim was contrafactual — my actual test run earlier in the same execute stage showed `7 passed in 7.74s` for the meta-test module including all 5 parametrize instances of `test_meta_module_has_boundary_negative`, and the failed-test debug output literally contained `tests/integration/test_boundary_settings_json.py: 5`. The reviewer's stated trace ("pytest 9 emits one line per test, no file-summary line") was a confident hallucination — pytest 9.0.3 (locked at uv.lock:644) WITH `-q --collect-only` against a single file path does produce file-summary lines. Cause: code-reviewer ran in Pass-1-only mode (we skipped Pass-2 contextual verification because there was no PR title/desc/author metadata to redact — the anchoring concern was moot). Without contextual verification, the reviewer's confident-but-wrong claim was treated as authoritative. Saved only by the orchestrator's manual cross-check against actual execute-stage test output. Pattern guard: when running 1-pass review (no Pass 1.5 verifier + no Pass 2 contextual re-pass), orchestrator MUST verify finding claims about tool output format against ACTUAL recent runs in the diff context — do NOT auto-apply manual-only P1s about subprocess output parsing without re-running the subprocess. Adjacent risk: `subprocess.run([...pytest...]).stdout` parsing is a class of finding where reviewers can confidently hallucinate the output format; treat such findings with extra skepticism.

## [fail:design] stash-list-substring-match | 2026-05-20 | count:1
Initial `_stash_base_dirty` (worktree.py:323) resolved the just-pushed stash ref by `if message in line` substring match on `git stash list` output. With `_find_free_name`'s numeric collision suffixes (`execute-1`, `execute-10`), message `hm-finalize-execute-1` is a substring of the stash list line for `hm-finalize-execute-10` → wrong `stash@{N}` returned → subsequent `git stash pop` drains the wrong stash, silently abandoning the real user WIP. Caught simultaneously by code-reviewer (P1) and concurrency-reviewer (P0) in REVIEW Round 1; severity-resolved to P1 and auto-fixed. Fix: exact message-field comparison after `line.split(": ", 2)` (the stash list format is `stash@{N}: On <branch>: <message>` with three colons; maxsplit=2 isolates the message cleanly). Class pattern: substring `in` on structured-string parsers is a footgun whenever candidate values can share prefixes. Default to exact-field comparison or anchor with delimiters. Auto-fix loop caught this; without it the P0 (concurrency-reviewer's classification) would have shipped.

## [fail:design] gitignore-write-text-non-atomic | 2026-05-20 | count:1
`_ensure_gitignore_entry` (worktree.py:746 pre-fix) called `gitignore.write_text(...)` for the new-file branch — plain `open(path, "w")` semantics, violating CLAUDE.md's hard rule against non-atomic writes outside tempfile-owned directories. Called from `_write_loop_marker` and the new `_write_stash_ref_file`, placing it on the finalize hot path. A SIGKILL or WSL2/NTFS power-loss between file creation and content flush leaves `.gitignore` empty/truncated → harness-managed files (loop markers, stash refs) appear as tracked drift on next `git status`, can be `git add`-ed into commits by subsequent ops. Caught in REVIEW Round 1 (code-reviewer P0 + perf-reviewer P1 → consensus P0), auto-fixed by switching to `atomic_write` from `harness_maker.io_utils`. Adjacent class: anywhere in the codebase that writes to a USER-OWNED file path (tracked by git, not a tempdir scratch), the atomic-write rule applies. `Path.write_text` and `Path.open("w")` are both bare writes — only the tempfile+os.replace pattern survives interrupt. Single grep guard for the future: `git grep "write_text\|open.*'w'" src/` should be reviewed periodically for fresh violations on user-paths.

## [fail:test] pytest-collect-only-q-flag-collapses-nodeids | 2026-05-20 | count:1
`pytest --collect-only -q` outputs `<path>: <count>` per file (intentional brevity), NOT the `file::test_name` nodeid form. Code that parses the output expecting nodeids (e.g. `spec_machine._check_pytest_collect` checking whether a `test_ids[]` entry resolves) silently zeroes — 0/210 test_ids "unresolved" while every test exists. Fix: drop `-q` from the subprocess argv. The clean output is `file::test_name` form, parseable as expected. Affected: `spec_machine._check_pytest_collect` + `spec_inventory.batch_refiner._resolve_test_ids`. Validator-style unit tests for these functions used monkeypatched subprocess returns so the bug never surfaced — only manifested when a real `pytest --collect-only -q ...` ran. Pattern: when a CLI's `-q` flag is documented as "quiet output", smoke-test the actual output format against the parser before relying on it in a gate.

## [fail:runtime] cwd-inherited-from-worktree-into-main-commands | 2026-05-20 | count:2 | previous_count:1
After `/hm:loop` engages a worktree (cd into `.worktrees/<name>/`), subsequent Bash calls inherit that cwd. When the user runs `/hm:wrapup` expecting "main" context, every `!`/Bash call without an explicit `cd /path/to/main &&` prefix runs against the worktree files — but Edit tool calls (absolute path) modify main. Result: ruff/mypy/pytest/git report stale or contradictory state vs what Edit just wrote. Symptom that exposed it: `grep` finding `test_typevar_T_name` (worktree, capital T) while Read tool returns `test_typevar_t_name` (main, lowercase) for the same relative path. Detect via `readlink -f <relative-path>` + `pwd`. Fix: explicit `cd /home/noel/<project> &&` prefix on every wrapup-stage Bash, OR exit the worktree directory before invoking wrapup.
- [2026-07-27] Same mechanism, opposite direction, and a much quieter symptom. A single `cd /home/noel/harness-maker && …` inside ONE Bash call moved the cwd for every LATER call in the session (Claude Code's Bash cwd persists across calls), so a subsequent `git diff de9f3f58..HEAD` ran against the base instead of the task worktree and returned **empty**. Empty is not an error: it reads as "this phase changed nothing", which is a plausible answer, so nothing prompted a re-check — unlike the 2026-05-20 instance, where the grep/Read contradiction was visibly impossible. The base and the task worktree share almost all of their content, which is precisely what lets the misfire SUCCEED quietly rather than crash. Prevention addendum: (a) when a `git diff` / `git status` / `git log` returns EMPTY, suspect cwd before concluding "nothing changed" — `pwd` is one call; (b) prefer `git -C <abs-path> …` over a `cd <abs-path> && …` prefix, because `-C` cannot leak into the next call while a bare `cd` silently rebases the whole rest of the session.

## [fail:tooling] plugin-agent-model-pin-dispatch-fail | 2026-05-20 | count:1
모든 harness-maker plugin agent 정의 (`plan-validator`, `test-reviewer`, `code-reviewer`, `security-reviewer`, `performance-reviewer`, `ux-reviewer`, `concurrency-reviewer`) 가 frontmatter `model:` 에 옛 id (`claude-4-7-opus[1m]`, `claude-4-6-sonnet`) pin 되어 현재 Claude Code runtime 에서 Agent tool dispatch 시 "model not found" 로 즉시 실패 — 단 한 번도 reviewer 가 실제 실행되지 않음. fresh-install-p0-calibration 의 /hm:plan + /hm:exec-rev 전 과정에서 7회 발생 (plan-validator 1, test-reviewer 1, code-reviewer 1, ux-reviewer 1, + execute 중 같은 류 추가 3건). 워크어라운드: agent 호출이 실패할 때 self-review pass 로 진행하고 REVIEW report 에 명시 (single-source-documented). 진짜 fix 는 harness-maker 의 agent template frontmatter 가 user 환경의 실제 사용 가능 model 로 resolve 되도록 render time 에 변환 또는 user 의 default model 로 fallback 하는 메커니즘 (presets.PRESET_AGENT_MODELS resolve_agent_spec 가 이미 존재하지만 rendered 결과가 옛 alias 유지). Cursor target 의 model id 변환 패턴 (CURSOR_MODEL_IDS) 처럼 Claude Code rendered agents 도 user 의 current runtime 에 정렬되는 normalization 필요. 별도 PLAN 후보 (이번 fix scope X).

## [fail:render] e2e-sandbox-worktree-path-leak-on-integration-regen | 2026-05-20 | count:1
`tests/unit/conftest.py:21-40` 의 `_pin_harness_maker_pkg_root` 픽스처가 `synthesize._HARNESS_MAKER_PKG_ROOT` 를 canonical main checkout 으로 monkeypatch — unit test 안에서 renderer 가 `--with /home/noel/harness-maker` 형태로 emit 하도록. 하지만 `tests/integration/conftest.py` 에는 같은 픽스처가 없음. integration test (특히 `test_fresh_install_readiness.py`, `test_health_dashboard_roundtrip.py`) 가 full CLI 를 invoke 하면 renderer 가 cwd 기반으로 path 계산 → `.worktrees/<name>/` 경로가 e2e sandbox snapshot 에 박힘. /hm:execute 안에서 worktree-based pytest 1 회 실행 후 127개 tests/e2e/sandbox*.{md,json,yaml} 파일에 worktree-specific 경로 leak → 모두 manual `git checkout` 으로 revert 필요. 단순 fix: integration conftest 에 unit conftest 의 `_pin_harness_maker_pkg_root` fixture 동일 copy. 별도 PLAN 후보 (REVIEW M1, deferred).

## [fail:lint] ruff-format-not-in-local-verify-pass | 2026-05-20 | count:2
v0.19.2 tag 가 quality-gate `Ruff format check` 단계에서 즉시 fail (job id 76910163911, exit 1) — 5개 파일 (`src/harness_maker/{ai_readiness,improvement}.py` + 3개 신규 test 파일) 이 `ruff format` 을 거치지 않은 상태로 commit 됨. 로컬 wrapup 검증 명령은 `ruff check` 만 돌리고 `ruff format --check` 는 안 돌림 → formatting 위반 silent pass. `ruff check` 는 lint rule 위반 (unused import, undefined name 등) 만 잡고 formatting 은 안 봄. CI 의 quality-gate 가 둘 다 돌려서 잡음. 결과: v0.19.2 PyPI publish + GitHub Release 진행 안 됨 (quality-gate 는 첫 job, 다른 job 실행 전 abort). 0.19.3 으로 re-tag 필요 — fix commit + 새 patch tag (CLAUDE.md release 절차에 따라 immutable v0.19.2 tag 는 dead 로 남김). **예방**: `/hm:wrapup` Step 2 (Final verification pass) 의 toolchain 명령 set 에 `ruff format --check` 가 누락 — `ruff check` 와 함께 둘 다 명시 필요. 본 fail entry 가 wrapup template fix 의 trigger 가 됨.

**2026-05-22 fix shipped in v0.22.2**: wrapup template `src/harness_maker/templates/stages/wrapup.md.j2` Step 2 의 두 rendered branch (Claude Code + Codex) 모두에 `uv run ruff format --check src/ tests/` 추가. 8 snapshot fixtures regenerated. 다음 wrapup 부터 local verification 이 CI 의 quality-gate 와 동일 명령 set 실행 — recurrence 차단. count:2 그대로 유지 (이번 patch 는 prevention, recurrence 아님).

**2026-05-22 recurrence (count:1 → count:2)**: v0.22.0 release.yml job 26268703007 + ci.yml jobs 26268703019 + 26268774240 (chore fixup) 모두 *동일* `Ruff format check` 에서 fail (13s/27s 짧은 실행). 문제 파일: `tests/unit/test_profile.py` — Phase 2 의 11 new unit tests 작성 시 함수 시그니처를 `def test_foo(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:` 한 줄에 박았다가 E501 잡혀 multi-line 으로 split 했는데, ruff format 의 black-style 규칙은 여러 줄 시그니처도 *정규화* 필요 (parameter 들이 column-align 되지 않은 형태). local `uv run ruff check` 는 통과 (E501 안 잡힘 — 우리 한 줄 split 이 100 자 미만), 그러나 `ruff format --check` 가 strict canonical form 요구. **재발 이유 명시**: wrapup template 가 v0.19.2 사후에도 안 갱신됨 — Step 2 의 명령 set 에 `ruff format --check` 추가 PR 이 아직 안 됨. 본 round 의 fix sequence: (1) `uv run ruff format tests/unit/test_profile.py` (1 file reformatted), (2) include in v0.22.1 wrapup commit, (3) v0.22.0 tag 는 immutable 로 dead 처리 (workflow 실패 — PyPI publish 도 partial 또는 0; v0.22.0 PyPI page 확인 필요). **트리거: count==2 → wrapup template Step 2 명령 set 에 `uv run ruff format --check src/ tests/` 추가가 pending-proposals 후보** (이미 count:2 라 자동 proposal 자격은 안 되지만 — wrapup template 가 *우리 own template* 이라 self-modify 가능).

## [fail:runtime] parallel-hm-execute-stash-overlay | 2026-05-21 | count:1
세션 중 `/hm:review` 가 6개 reviewer agent 를 병렬로 long-duration 실행하는 동안, 사용자가 (또는 다른 자동화가) 별도의 `/hm:execute` 를 같은 repo 에서 시작. 두 번째 execute (timestamp `execute-20260521T0453Z`, 첫 번째 `0440Z` 보다 13분 뒤) 의 `harness_maker.worktree.finalize` 가 base 의 모든 staging 을 자동 stash 처리 (`stash@{0}: hm-finalize-execute-20260521T0453Z-...`) 한 뒤 자기 작업을 overlay. 결과: 첫 번째 execute 의 Phase 1-5a staging (locate.py + cli.py + BOOTSTRAP.md + 8 snapshot + 5 version files = 22 files) 이 stash 로 쓸려나가고 두 번째 execute 의 help-skill staging 만 base 에 남음. Round 2 auto-fix 시도가 "File has been modified since read" 로 첫 신호 — 이후 git reflog (HEAD@{0}: reset: moving to HEAD) + stash list 로 진단. 수동 복구: 현재 staging (help-skill) 을 별도 stash 로 push 보존 → `git stash pop stash@{1}` 으로 locate 작업 복원 → review Round 2 진행. **예방**: `harness_maker.worktree.finalize` 가 base 에 unrelated staged content >N 파일 있으면 abort 또는 large-warning 출력 (현재는 silent stash). 추가 신호: `git reflog | head -1` 에 `reset: moving to HEAD` 가 review 직전에 있으면 finalize 가 다녀간 것 — review/wrapup 전 pre-flight 체크에 추가 후보. 일단 single occurrence 이므로 count:1; 재발 시 proposal 작성.

## [fail:design] worktree-finalize-untracked-loss | 2026-05-22 | count:1
`/hm:exec-rev` 의 execute stage 에서 REVIEW report 를 `.worktrees/<WT>/work-docs/REVIEW-{slug}-{date}.md` 에 작성. `work-docs/` 가 `.gitignore` 에 등록된 디렉토리라 git tracked 가 아님. 이어진 `worktree.finalize <WT> stage-only` 가 worktree branch 의 변경을 base 의 staging 으로 squash-merge 한 뒤 `git worktree remove --force` 로 worktree 디렉토리 트리를 통째 삭제 — squash-merge 는 git tracked 파일만 옮기고 untracked / gitignored 파일은 무시. 결과: REVIEW 파일이 cleanup 단계에 worktree 전체와 함께 *물리적으로 삭제*. PLAN, RESEARCH 파일은 이전 turn 에 base 에서 작성됐기 때문에 살아남음 (worktree 만들기 전에 base 의 `work-docs/` 에 존재) — 이번 PLAN-harness-maker-cold-eval execute turn 만의 worktree 안 작성이 문제였음. 다행히 conversation context 에 내용이 남아 있어 base 의 `work-docs/` 에 재작성 가능. **즉시 예방** (LLM 측, no code change 필요): worktree 안에서 `work-docs/`, `docs/observability/`, 또는 다른 gitignored 디렉토리에 새 파일 작성 시 — 처음부터 base 의 해당 경로에 작성하거나, finalize 직전 `cp -r .worktrees/<WT>/<gitignored-dir>/. ./<gitignored-dir>/` 로 base 에 복사. **systemic fix** (code change): `worktree.finalize` 에 `--preserve-untracked=<glob>` 옵션 추가해서 cleanup 전에 명시된 gitignored 경로를 base 에 복사. 이 PLAN 의 `wrapup-marker-discipline-silent-loss` (33줄 wiki entry 유실) 와 같은 *gitignored-dir 안의 user-content 유실* family — 그러나 메커니즘 다름 (이쪽은 untracked 라 transfer 자체 안 됨; 그쪽은 marker 위치 misuse). 일단 single occurrence 이므로 count:1; 재발 시 finalize CLI 에 preserve-untracked 추가 proposal.

## [fail:tooling] reviewer-agents-model-unreachable | 2026-05-22 | count:1
`/hm:exec-rev` 의 audit-convergence-2026-05 turn 에서 Phase A.5 `test-reviewer` + Review stage `code-reviewer` 둘 다 `Agent` 호출에 즉시 fail: "There's an issue with the selected model (claude-4-6-sonnet). It may not exist or you may not have access to it." 두 reviewer 의 frontmatter 가 `model: claude-4-6-sonnet` (또는 그 변형) 으로 pin 되어 있는데 해당 모델이 retired/unreachable 인 듯. orchestrator (this Claude session = Opus 4.7) 가 self-review 로 대체 — 6 focus area 직접 확인, P2 finding 1건 발견 + 자동 fix + 새 regression test 추가. self-review 의 한계: cross-reviewer surface-match 가 불가능해 모든 finding 이 `manual-only` tag (consensus filter 의 single-source 룰), 그래서 auto-fix 가 자동 발화 안 됨 (`consensus-passed` 만 fix-eligible). REVIEW 보고서에 `human_review_needed: true` 명시 + infrastructure note. **즉시 예방** (no code change): 비슷한 model-unavailable error 가 다른 agent (security-auditor, plan-validator, stuck) 에서도 잠재 — 새 stage 진입 시 1차 agent invocation 이 모델 에러로 fail 하면 orchestrator self-review 로 graceful fallback. **systemic fix**: PLAN-codex-plan-validator-model-unavailable.md 의 follow-up 으로, agent frontmatter `model` 을 hard-pin 대신 (a) `model: inherit` (parent session 모델 따름) 또는 (b) `model: ${default_model}` (harness.yaml 의 default_model 참조) 도입. 또는 agent dispatcher 에 model-available probe 로 fallback chain (sonnet-4-6 → sonnet-4-5 → opus-4-7). 일단 single occurrence 로 기록; 재발 시 (B5/B6 trigger) proposal escalation.

## [fail:tooling] worktree-finalize-blocks-on-venv | 2026-05-22 | count:1
`/hm:exec-rev` 의 audit-convergence-2026-05 finalize 단계에서 `worktree.finalize <WT> stage-only` 가 WIP commit + staging merge 까지는 성공했지만 cleanup phase 의 `git worktree remove --force` 가 `error: failed to delete '.../execute-20260522T0948Z': Directory not empty` 로 실패. 원인: worktree 안에서 실행한 `uv run pytest` 가 `.worktrees/<WT>/.venv/` 를 자동 생성 → `worktree remove --force` 가 의존성 venv 디렉토리 (수백 MB) 를 정리 못 함. `git worktree list` 에서는 deregister 됐지만 디스크에는 디렉토리 잔존. `harness_maker.worktree.finalize` 의 reported state: `[finalize] succeeded: []` / `[finalize] failed: [<WT>]` / `[finalize] marker kept`. **즉시 우회**: 사용자에게 `rm -rf .worktrees/execute-*` 수동 실행 부탁 (rm 권한은 LLM 측에서 자동 거부됨). **systemic fix** (code change): `harness_maker.worktree._cleanup_worktree_dir` 에 `shutil.rmtree(<WT>, ignore_errors=False)` fallback (git worktree remove 실패 시 직접 디렉토리 제거) 또는 cleanup 전 `<WT>/.venv/` 명시 unlink. 또는 pytest 호출 시 `UV_PROJECT_ENVIRONMENT=<shared venv>` 로 venv 를 worktree 밖에 두기. 추가 우려: stale `.venv` 디렉토리는 매 execute round 마다 누적 → 디스크 압박 + 다음 finalize 도 같은 이유로 fail. single occurrence 지만 모든 long-running execute turn 에서 재발할 가능성 높음 — proposal 후보 (B3).

## [fail:design] path-traversal-via-unsanitized-slug-in-fstring | 2026-05-23 | count:1
`harness_maker.feedback.draft_writer.write()` 에 `out_path = out_dir / f"{date}-{draft.task_slug}-{dedup}.md"` 로 `task_slug` 가 파일 경로에 그대로 박혔는데 `FeedbackDraft.task_slug` 에 field_validator 없음. `file_paths` 는 validator 있고 `error_message` 는 redaction 있는데, `task_slug` 만 사각지대. 위험: in-band LLM dispatcher 가 prompt-injected telemetry (e.g. `silent-intent-miss-*.jsonl` 의 `slot` 필드에 `../../etc/cron.d/evil` 같은 문자열) 를 읽고 그걸 `task_slug` 으로 echo 하면 `atomic_write` 의 `mkdir -p` + `os.replace` 가 `.claude/observability/feedback/` 밖으로 write — 우리가 advertise 한 whitelist 계약 위반. code-reviewer round 1 P1-1 catch. **Fix**: `_TASK_SLUG_PATTERN = re.compile(r"^[A-Za-z0-9_-]{1,200}$")` + `_validate_task_slug` field_validator + 3 regression tests (`test_task_slug_path_traversal_rejected` 5 bad inputs / `test_task_slug_too_long_rejected` / `test_task_slug_kebab_case_accepted`). **Pattern lesson**: 모든 f-string 으로 파일 경로 구성 시 — 변수 하나라도 unsanitized 면 directory traversal. 다른 validator 가 있다고 안심하지 말 것; 같은 모델 안의 다른 필드도 각각 따로 검사. 향후 확장 시 새 string field 가 path 에 들어가는지 매번 확인 (whitelist discipline 의 invariant 는 "model 의 모든 path-contributing field 가 validator 있다", 아니라 "이 model 의 일부 field 가 validator 있다"). PR description 의 보안 check 에 "f-string 으로 path 구성하는 코드 변경 시 모든 변수에 validator 있나?" 항목 권장.

## [fail:design] prompt-driven-yaml-write-no-atomic | 2026-05-23 | count:1
PLAN-loop-mid-stop-and-review-skip Phase 3 (commit `63eea38`) added Gate 0 with stage_retry_counts persisted to `runtime:` block of `work-docs/loop-context/<slug>.yaml`. The persistence prose said "Persist to runtime.stage_retry_counts in the loop-context file" — pure instruction, no Python helper called via Bash. Two compounding defects: **(1)** LLM driver would hand-write YAML via Edit tool → not atomic on WSL2/NTFS → crash mid-write corrupts the file → `parse_loop_context` raises → `/compact` recovery permanently broken. **(2)** `LoopContext(extra="forbid")` had no `runtime` field declared, so even a SUCCESSFUL write produced a YAML that fails `LoopContext.model_validate` next time. The two defects mask each other: prompt-driven write produces a valid-looking YAML, then re-parse fails not because of corruption but because of schema-forbid. Concurrency-reviewer caught both as separate P0s in post-commit review. **Fix**: (a) added `RuntimeBlock` pydantic model + `runtime: RuntimeBlock | None = None` to `LoopContext`; (b) added `iter_receipts.patch_runtime_block()` + `patch-runtime` CLI subcommand callable from Bash; (c) loop.md.j2 Step 4.5 retry counter + Step 7.0 cleanup now invoke the CLI; (d) regression tests `test_loop_context_accepts_runtime_block` + `test_patch_runtime_*` (3 tests). **Pattern lesson**: any prompt instruction that says "persist X to YAML file" without naming a CLI is a latent atomicity hole + schema-drift bomb. Two-part rule for stateful files: (i) every persisted field must be in the Python model (else next read raises); (ii) every write site must call a CLI that uses `atomic_write` (else WSL2/NTFS corruption window). Prose "edit the file" is NEVER a complete instruction for stateful files in this codebase.

## [fail:design] finalize-stash-queue-no-wrapup-handshake | 2026-05-23 | count:1
PLAN-loop-mid-stop-and-review-skip 의 Phase 1-4 dogfooding 중 발생. `worktree finalize <WT> stage-only` 는 `git stash push -u` 로 WIP 을 저장하고 stash-pop 을 `/hm:wrapup` 의 `post-commit-pop` 으로 deferred (per PLAN-worktree-finalize-stash-isolation ADR-001). 사용자가 Phase 마다 `/hm:exec-rev` 만 호출하고 `/hm:wrapup` 을 끼우지 않아서 4개 stash 누적. **첫 번째 증상**: 다음 worktree 가 stale HEAD (`d92c6b3` — Phase 1-3 코드 없음) 에서 분기 → Phase 4 가 EXPECTED_STAGES table 추가 같은 Phase 3 의존 작업을 못 함. **두 번째 증상 (더 심각)**: Phase 4 `stage-only` finalize 가 base 의 stale `UU` 충돌로 `git stash push` 자체 실패 → `fail` finalize fallback → worktree 디렉토리 삭제. 결과: Phase 4 source code 가 worktree 와 함께 사라졌고, 4개 stash 도 결국 dropped (각 finalize 가 자기 stash 만 알므로 누적 race). 다행히 각 finalize 가 stash 외에 별도 WIP commit 도 branch (`execute-20260523T*`) 에 남기는 fallback 메커니즘이 있어서 `git fsck --unreachable --no-reflogs` + reflog 로 5개 phase 의 commit (4eaac70, ea2968a, 4ace849, f5ceb8c, 7f95f08) 전부 회수 가능 → cherry-pick 으로 main 에 순서대로 복원. **방어책 (즉시)**: wrapup template 에 "stage-only finalize 가 N번 누적되면 강제 halt + 명시적 알림" 추가 (`.claude/.hm-finalize-stash-*` ref 파일 개수 ≥ 2 면 새 worktree create 거부). **방어책 (장기)**: PLAN-finalize-stash-queue-guard 후보 — finalize 시 prior unpopped stash 가 있으면 명시 거부, OR multi-stash 처리 logic 추가. **Pattern lesson**: handshake-pattern API 는 handshake 가 명시적으로 강제되지 않으면 silent decay. CLAUDE.md "8. Integration 경계 한 줄 테스트" 의 정신: hand-off 시점 invariant 가 prose 로만 있고 mechanical gate 가 없으면 사용자가 한 단계 건너뛴 순간 무성의 실패.

## [fail:design] concurrent-finalize-contaminates-base-during-wrapup | 2026-05-25 | count:1
Sibling of [fail:design] finalize-stash-queue-no-wrapup-handshake. During `/hm:wrapup` for the codex-mandatory-second-opinion work (6 codex files staged on main), a **concurrent session** running a different feature (worktree-prune: render.py/worktree.py + new test_worktree_prune.py/test_manifest_compaction.py) ran `worktree finalize` against the SAME base repo. Its finalize saw my staged work as "dirty base", stashed it (named after ITS worktree `execute-85d4064c…0223Z`), and merged its own branch onto main's index — so `git status` abruptly showed a completely different changeset staged and my codex partial reverted to original. A background `pytest -x` (the wrapup verification gate) was running at the same time, which both heightened the race window and got interrupted (exit 144). **Nothing was lost** — the 5-layer defense held: my work was intact in the finalize-stash + a `wip(execute)` branch commit (`b56adb0`); the foreign work in its own `wip` commit (`3077c62`) + branch + index. Recovery recipe (no data loss to either party): (1) `git stash list` + `git reflog --all | grep wip(execute)` to map every actor's work; (2) verify the target stash's content with `git stash show -p` / `git show stash@{N}:path` BEFORE touching anything (ADR-008 no-blind-drop); (3) `git stash push -m "PRESERVED: <other session>"` to safely set aside the foreign index work (caution: foreign index was 2 lines ahead of its wip commit, so the named stash — not the wip — is the complete backup); (4) `git stash apply <my-work-stash>` to restore own work; (5) commit own work; foreign session recovers from its named stash + branch. **Prevention**: do NOT run a long full-suite `pytest -x` in the background during wrapup on a base shared with other live sessions; prefer a scoped foreground `pytest tests/unit` for the wrapup gate. Untracked artifacts (`work-docs/` is gitignored here) survive all git ops — PLAN/REVIEW were never at risk.

## [fail:design] verification-marker-check-set-drift | 2026-05-25 | count:1
The first workflow-overhead implementation let `verify` mark a fresh cache after `pytest`, `ruff check`, and `mypy`, but wrapup's full suite also required `ruff format --check`. That mismatch would let wrapup skip formatting based on a marker that had never proven formatting, recreating the "local green, CI red" class under a new cache mechanism. Fix: make `verify` run `ruff format --check src/ tests/`, write markers with `--checks lint,format,mypy,pytest`, and add a regression test that the generated verify stage covers wrapup's Python gate set. Pattern: any cache marker that authorizes skipping a later gate must encode the exact same check set as the skipped gate.

## [fail:design] manual-post-commit-pop-no-strict-mode | 2026-05-25 | count:1
The execute-stage manual no-wrapup commit instructions invoked `post-commit-pop` directly, while wrapup uses `HM_OWNED_SESSION_UUIDS="$(... owned-uuids ...)"` to force Layer 3 UUID strict mode. That meant users following the manual path could accidentally pop another live session's deferred stash refs in a shared base repo. Fix: update the manual instruction to export `HM_OWNED_SESSION_UUIDS` from `owned-uuids` before calling `post-commit-pop`, and add a rendered Codex-stage regression test. Pattern: every documented path to `post-commit-pop` must include the same strict-mode env contract, not just the happy-path wrapup template.

## [fail:design] plugin-make-direct-resolver-stale | 2026-05-25 | count:1
`/harness-maker:make` still carried a legacy inline `installed_plugins.json` resolver even after `harness_maker.cli locate` became the canonical resolver. In `~/neuroTerm`, no project-scope install matched the cwd, so the inline code fell back to `entries[0]` under `harness-maker@harness-maker-local`, selecting another project's `kairos@0.7.3` cache. A full interactive make then reused 0.7.3 and left `.claude/harness.yaml` stamped 0.7.3, so the SessionStart drift warning reappeared immediately. Fix: make `commands/make.md` bootstrap the newest cache path and call `harness_maker.cli locate --plain`; add a static guard forbidding `entries[0]` and hard-coded `harness-maker@harness-maker-local` in the plugin command. Pattern: every install resolver surface must delegate to the shared resolver, not reimplement priority logic in prompt prose.

## [fail:design] second-brain-silent-degrade-no-signal | 2026-05-27 | count:1
Second Brain was "enabled" in harness.yaml but fully non-functional — `vault_path` pointed to a non-existent subdirectory and `folders: []` was empty. All search/write calls returned empty results with zero user-visible signal. Root cause: the loader degrades gracefully (returns empty SecondBrainConfig with enabled=false) when vault doesn't exist, and empty folders just means no candidates are scanned. Fix: (1) correct vault_path to actual root, (2) add configured folder, (3) enhance empty-folders path to emit loud stderr warning with `ACTION:` remediation hint. Pattern: graceful degradation must still produce visible signal at stderr level so the user knows the feature is a no-op.

## [fail:design] orphan-sweep-skips-provenance-stripped-renders | 2026-05-28 | count:1
`reconcile._classify_orphan` consulted the render manifest only in the no-frontmatter branch. A file carrying frontmatter WITHOUT `generated_by: harness-maker` — e.g. `.cursor/rules/*.mdc`, whose provenance is intentionally stripped by `_render_pure_text` for Cursor's strict parser — short-circuited to `("theirs", KEEP)` BEFORE the manifest check. So dropping the `cursor` target leaked a stale `.cursor/rules/harness.mdc` forever, while the pure-JSON `.cursor/hooks.json`/`mcp.json` siblings were swept fine via the no-frontmatter branch — the asymmetry was the whole tell. Found in ~/spoton: the live `.mdc` full-file sha256 matched its `.hm-render-manifest.jsonl` entry exactly (= unmodified harness output) yet survived every re-render. Fix: the non-harness-provenance branch now runs the same per-path full-file-hash check the no-frontmatter branch already used (`current_hash in manifest.get(rel_key)` → ours-clean). R4 preserved by per-path scoping — locked by a regression where the byte-hash exists only under a DIFFERENT path key → KEEP. Pattern: any KEEP/DELETE classifier branch that keys on provenance MUST have a manifest-hash fallback for every render path that legitimately lacks our frontmatter (pure-text: `.mdc`, `.sh`, cursor commands). "Has our frontmatter?" is not the same question as "did we render it?"

## [fail:design] hooks-merge-identity-enumerated-paths-not-namespace | 2026-05-28 | count:1
The 0.26.x hooks.json dedup normalizer (`render._normalize_hm_managed_command`) keyed harness-hook identity on a regex that ENUMERATED one plugin path family (`harness-maker-local/harness-maker/<ver>`). It missed the GitHub-marketplace cache (`…/cache/harness-maker/harness-maker/<ver>/…`, no `-local`) and the dev-repo path (`--with /home/noel/harness-maker …`). When ~/spoton switched from the local to the GitHub marketplace (2026-05-28), the un-enumerated forms kept full-command identities, evaded the per-event dedup union (`_merge_hooks_json` keeps existing entries whose identity ∉ template set), and ACCUMULATED — every harness hook ended up TRIPLICATED (github-0.26.5 + local-0.26.4 + dev-repo), each firing per event, stale copies running old code and dangling once the old cache was pruned. This is a RECURRENCE of the exact dup bug the 05-22 normalizer was added to fix — the original just enumerated too few paths. Fix (0.26.6): key identity on the `python -m harness_maker.<invocation>` module-namespace SUFFIX (path-agnostic; module + trailing args); already-duplicated files self-heal on next render. Pattern: when an identity/ownership check must survive a VOLATILE attribute (path, version, host), key on the STABLE invariant (our module namespace) — NEVER enumerate the volatile attribute's known values, because the next unforeseen value silently reopens the bug. Same defect class as [fail:design] orphan-sweep-skips-provenance-stripped-renders above (identifier-change blindness), different surface (intra-file merge identity vs file-level sweep).

## [fail:design] reconcile-freezes-phantom-hash-as-user-modified | 2026-05-28 | count:1
Codex stage/loop skills (`hm-execute/verify/wrapup/loop`) stayed frozen at 0.26.1 across every `/hm:make --update` while sibling skills (plan/research/review/spec) reached 0.26.6 — surfaced as stale `uv run --with .../0.26.1` paths after a 0.26.2→0.26.6 update touched only 4 tracked files. Cause: pre-0.26.2 render hashed these install_ref-embedding bodies BEFORE path substitution, persisting a `content_hash` that never matches `compute_body_hash(body)` (a "phantom" hash; verified — no raw/normalized/rstrip/crlf variant of the persisted body reproduces the stored value). reconcile's ONLY edit-detection signal is `stored == recompute`, so the phantom mismatch read as "user-modified" → KEEP forever. orphan-sweep can't catch it — the files are in the blueprint, not orphans (this is why disk-15 vs written-manifest-10 looked like 5 orphans when 4 were really frozen blueprint files). Fix: `reconcile._is_healable_phantom_ours` flips KEEP→REPLACE for `generated_by: harness-maker` + `source_template ∈ {codex/stage_skill,loop_skill}.md.j2` + `harness_maker_version` < 0.26.2 floor + KEEP-reason ∈ {hash-mismatch-user-modified, hash-mismatch-malformed-markers}; pre-render `.backup-<ts>/` covers the residual. /hm:review caught the first cut (version-floor alone over-included every pre-floor ours file → could clobber genuine edits to CLAUDE.md/agents); scoping to `source_template` (STABLE) fixed it. Same defect FAMILY as the two entries above (identifier/signal blindness): the "is this ours/edited?" oracle must key on a STABLE, REPRODUCIBLE signal — a hash computed over non-persisted bytes is neither, so it can't be the sole edit oracle. Prevention: render must ALWAYS hash the exact bytes it persists (current code does — `_normalize_body` then hash then write same bytes; the bug was pre-0.26.2), and reconcile now self-heals legacy unverifiable hashes.

## [fail:render] stage-section-jinja-gated-breaks-codex-test | 2026-05-28 | count:1
Adding a new conditional section to a stage template (`templates/stages/wrapup.md.j2` Step 5.6) I gated it with `{% if config.second_brain.enabled %}`. That broke `test_codex_stage_procedures.py::test_stage_aware_second_brain_guidance[wrapup]`: the test renders each stage with `is_codex=True` and a config where second_brain is NOT enabled, asserting the SB terms (`second_brain`, `journal`, `decision`, `harness_maker.second_brain`, `untrusted reference`) are present. The Jinja gate omitted the whole block → terms vanished → fail. Cause: the SIBLING stage SB sections (research/plan/review) use PROSE gating ("If `.claude/harness.yaml` has second_brain.enabled: true …") and render UNCONDITIONALLY — the LLM does the runtime skip. Jinja-gating wrapup made it the lone divergent stage. Fix: revert to prose gating (a `>` blockquote runtime-skip note at the step head) for sibling consistency. Lesson: a stage template's Second-Brain (and any always-documented) section is prose-gated, not Jinja-gated — the rendered prompt always carries it and the executing LLM checks `harness.yaml` at runtime. A plan that says "Jinja-gate it" conflicts with this convention; verify against the sibling stages + `test_stage_aware_second_brain_guidance` before gating.

## [fail:design] gitignore-append-at-create-self-dirties-base | 2026-05-29 | count:1
PLAN-worktree-base-artifact-pollution. To "keep the base clean" I added `_ensure_harness_gitignore(base)` in `_cli_create` (before the guards) to append churn patterns to the user's `.gitignore`. But the append MODIFIES `.gitignore` → `M .gitignore` → the dirty-base guard (`_has_user_dirty_state` → `_is_create_guard_harness_artifact` → `_is_harness_artifact`) saw it as user dirt → `worktree create` BLOCKED with "base repo has uncommitted user changes: .gitignore". 16 existing `test_worktree*` tests went red at once. Subtler still: even past the guard, finalize's `_stash_base_dirty` would stash the `M .gitignore` (not a recognized artifact) → the migration recreates the very stash churn it removes. The existing `.hm-loop-*`/`.hm-session-uuid` appends dodge this only because they're subsumed when `.claude/` is dir-ignored; the new non-subsumable `work-docs/` patterns exposed it. Fix: recognize `.gitignore` itself as co-managed in `_is_harness_artifact` (`if path == ".gitignore": return True`) — safe because the finalize squash only carries the worktree-branch diff and never touches the base `.gitignore`, so a user's own `.gitignore` edit is left intact, not contaminated. Lesson: any "auto-edit a tracked file at create-time to keep the tree clean" move dirties that file, and that dirt flows through the SAME dirty-base/stash machinery — the edited file must be made non-dirtying in the same change, or the fix is self-defeating.

## [fail:test] finalize-wip-captures-cwd-test-pollution | 2026-05-29 | count:1
Running the full unit suite INSIDE the execute worktree (`cd <WT> && uv run pytest tests/unit`) let a test that writes a relative-path file (`new.txt` with "ok\n") drop it in the WT cwd. finalize's `_capture_pending_in_worktree` does `git add -A` to WIP-commit uncommitted work before cleanup → it swept the stray `new.txt` into the WIP commit → squash-merged into base as a stray staged `A new.txt`. This EXTENDS `[fail:test] snapshot-regen-inside-worktree` (count:7): that lesson's exclusion list (skip `tests/snapshot/` + `tests/e2e/` when running in a WT) is INSUFFICIENT — a plain unit test that writes to cwd leaks the same way via finalize WIP capture. Caught at wrapup pre-flight (`git status` showed `A new.txt` after finalize). Recovery: `git restore --staged new.txt` (unstage); the file itself is user-denied-rm cruft left for manual removal. Prevention: (a) prefer running the suite from MAIN, or run only the worktree-relevant unit files in the WT, NOT the whole `tests/unit`; (b) any test that writes files MUST use `tmp_path`, never a relative/cwd path — audit for offenders; (c) consider a finalize scope-guard extension that warns when `git add -A` would capture files outside the worktree's own diff. Same family as the cwd-from-worktree + snapshot-regen entries: NEVER run the broad suite inside an execute worktree.

## [fail:test] mock-only-helper-hides-external-tool-format-bug | 2026-05-30 | count:1
`spec_machine._check_pytest_collect` ran `pytest --collect-only` without `-q`. Modern pytest's non-`-q` collect output is an indented `<Function ...>` tree with NO `file::test` nodeids, so the parser's `if "::" in line` filter matched zero lines → the function reported EVERY test_id as unresolved → cross_validate rule-3 was silently broken for all REAL usage. It stayed green for months because every rule-3 unit test `monkeypatch.setattr(sm, "_check_pytest_collect", ...)` stubbed the helper out — the callers' branching was proven, the helper's contract against pytest's actual stdout never was. Surfaced only when `mark_tested` (PLAN-spec-test-accumulation) drove the helper against a real pytest tree and the forward write-back failed its own cross_validate. Same family as `[fail:design] producer-consumer-schema-drift` and `[fail:test] unit-fixture-skips-renderer-frontmatter`: when a helper's output is parsed from an EXTERNAL tool's stdout, mocking it everywhere hides format drift. Fix: `-q` (emits full nodeids) + return-code-aware degradation (`not collected → all-unresolved iff returncode!=0 else []`) + `--` argv fence; guarded by `test_mark_tested_real_pytest_collect_lifecycle` (UNMOCKED, real `pytest --collect-only`). Rule: any helper that parses an external command's stdout needs ≥1 unmocked test exercising the real command.

## [fail:test] enumeration-tests-not-updated-with-new-rendered-artifact | 2026-05-31 | count:1
P3 of PLAN-latency-worktree-step-preview added a new fixed command (`/hm:loop-p5-batch`) + a Codex skill (`loop_p5_batch_skill`) but did NOT update the count-assertion / invariant tests that enumerate rendered artifacts, so `pytest tests/unit` was RED on `main` — and P3's wrapup committed that red base (verification step apparently ran scoped/skipped). Four tests broke at once: `test_synthesize_fused_workflow_command_count` (fixed-command count 6→7), `test_codex_target_files_total_skill_count` (18→19), `test_synthesize_codex_target_emits_skill_paths` (+1), and `test_render_feedback_block` (loop-p5-batch lacked the `@hm:feedback:dispatcher-block` marker). The 4th was NOT a count bump: loop-p5-batch is loop-family (a niche bulk-authoring orchestrator, not an atomic stage), so like `/hm:loop` it carries no feedback block — the fix was to EXCLUDE it from the atomic-feedback invariant, not inject a block. Discovered while running the FULL unit suite during a later phase's execute Phase D; fixed in a separate commit `6ea501c`. Lesson: adding any enumerated rendered artifact (command/skill/agent) MUST update its count-assertion AND invariant-exclusion tests in the SAME change; and wrapup's final verification must run the full unscoped suite (a scoped/`-x`-early run that stops before these tests masks the red). Same family as `[fail:test] finalize-wip-captures-cwd-test-pollution` (scoped verification hides regressions).

## [fail:workflow] execute-skips-named-integration-exit-criterion | 2026-05-31 | count:1
PLAN-p6-p7-worktree-finalize P1 listed FOUR named exit criteria — three unit tests + one INTEGRATION concurrency assertion in `test_worktree_parallel_session.py` (the validator added it specifically to defeat the "existing GREEN" escape: a net-new cross-session prune-vs-finalize behavior unit tests can't catch). `/hm:execute` authored only the 3 unit tests and silently skipped the integration one; Phase D + Phase A.5 both passed because they only looked at the authored tests. The gap surfaced at `/hm:wrapup` Step 4 (mapping each named exit criterion → an actual test before marking the phase DONE) — the integration test was then authored at wrapup and run with `HM_RUN_PARALLEL_SESSION=1` to confirm GREEN. Lesson: `/hm:execute` Phase A must author a test for EVERY named exit criterion including integration/opt-in-gated ones (not just the unit subset), and wrapup must verify each named criterion has a corresponding GREEN test rather than trusting "suite green". Same family as `[fail:design] producer-consumer-schema-drift-in-same-process-pipeline` — integration coverage skipped because the cheaper unit layer felt sufficient.

## [fail:test] fence-position-test-pins-only-upper-boundary | 2026-06-01 | count:1
PLAN-p6-p7-worktree-finalize P2 moved `_stash_base_dirty` INSIDE the merge fence. My first Phase A test set pinned only the UPPER boundary (stash runs inside the fence) and left the PINNED LOWER boundary (ref-write/cleanup/both pops stay OUTSIDE/after the fence) unverified — so a CODER could widen the fence too far, engulfing cleanup or a pop, and every test stayed GREEN. The Phase A.5 test-reviewer FAILed the set on exactly this: "lower boundary unverified." Second defect in the same set: the threaded gating test used a bare `time.sleep(0.3)` then asserted "not yet stashed" — a false-GREEN window because the worker's pre-fence `_capture_pending_in_worktree` can exceed 0.3s on slow WSL2, so the assertion passes for the wrong reason (worker hadn't started, not "fence blocked it"). Fixes: (1) added a full boundary-ordering test asserting `fence-enter < stash < snapshot < fence-exit < ref-write < cleanup` (pins BOTH ends + staged_before-strictly-after-stash); extracted `_snapshot_staged_paths` so the snapshot step is spyable. (2) replaced the sleep with a POSITIVE barrier — a `signaling_fence` wrapper sets a `reached_fence` Event before delegating to the real fence; the test waits on it, then asserts. Lessons: when a test claims "operation X moved inside lock L", pin BOTH `X inside L` AND `the things that must stay outside L are outside` — a one-sided boundary test lets the opposite over-correction pass. And NEVER gate a concurrency assertion on a timed sleep; use a positive signal at the synchronization boundary (sleep-based gating only ever over-passes, so it has near-zero regression-detection value).

## [fail:design] rollback-cleanup-gated-on-mode-not-failure-state | 2026-06-01 | count:1
`_cli_finalize`'s rollback `finally` reset the conflicted/partial merge to HEAD (`git reset --hard HEAD`) only `if not auto_commit` — i.e. only for stage-only finalize. But a SUCCESS-mode (`auto_commit=True`) `git merge --squash` CONFLICT also leaves a dirty/conflicted index WITHOUT committing (HEAD unchanged), so the success-mode rollback skipped the reset and then ran `_restore_base_dirty` (stash apply) over the conflict markers → markers left in the base working tree + a spurious `[finalize] stash-pop conflict — autoloop must halt` signal. Caught by code-reviewer (CR1) during /hm:review of P2; the P2 fence-widening *raised its reachability* (success-mode merge conflict became a clean caught path instead of an uncaught crash). Fix: gate the pre-pop reset on `wt_rc != 0` (ANY failure rollback), not `not auto_commit`. `git reset --hard HEAD` is a no-op when the index is already clean, so widening the guard is safe. Root cause class: a cleanup/rollback step gated on a *mode flag* (auto_commit) when it should be gated on the *failure state* it's cleaning up after — the mode was a proxy that held until a new path (success-mode conflict) broke the correlation. When writing rollback logic, gate on "is there a mess to clean" (wt_rc != 0), not on "which mode are we in".

## [fail:test] integration-gated-test-stale-after-behavior-flip | 2026-06-01 | count:1
The permissions-opt-out work (21a6066/d5cfbc1) deliberately flipped the rendered `settings.json.permissions.deny` default from "4 dangerous patterns" to EMPTY (opt-in via `harness.yaml.permissions.deny_dangerous`). But the boundary test `test_rendered_settings_json_permissions_lists_well_formed` still asserted `perms["deny"]` is NON-EMPTY — stale relative to the new default → RED. It went undetected through BOTH permissions wrapups because it's `INTEGRATION=1`-gated and the wrapup verification ran scoped `pytest tests/unit tests/structural` (no INTEGRATION). Surfaced only at RELEASE time by the advisory `INTEGRATION=1 tests/integration/test_boundary_*.py` run (CLAUDE.md release procedure) — exactly what that advisory step exists for. Fix: the boundary test's real invariant is the TYPE (allow/deny are JSON lists); dropped the non-empty content assertion (content choice is covered by the settings-template unit tests), and corrected the now-stale "4 dangerous patterns are baseline" comments. Lesson (recurrence of the scoped-verification-hides-regressions family — cf. enumeration-tests-not-updated, finalize-wip-cwd): when a change FLIPS a rendered default, grep for INTEGRATION-gated/boundary tests asserting the OLD default in the same change — scoped wrapup `pytest` will NOT catch them; either run the boundary suite when touching rendered-artifact contracts, or keep the release-time advisory boundary run as the safety net (it worked here).

## [fail:design] subagent-frontmatter-permissions-not-enforced | 2026-06-02 | count:1
harness-maker ships per-agent `permissions.allow/deny` blocks in subagent frontmatter (all reviewers, executor, validators) and CLAUDE.md §보안/권한 treats them as the security boundary. **They are not enforced.** Claude Code's documented subagent frontmatter fields (`sub-agents.md`) are name/description/tools/disallowedTools/model/permissionMode/hooks/skills/… — `permissions` is ABSENT and silently ignored. Command-level allow/deny lives ONLY in `settings.json` (`permissions.md`), deny-first, session-wide. Found 2026-06-02 via the codex permission probe: a `code-reviewer` with frontmatter `deny: [Bash(sh:*)]` + `tools: …, Bash` ran `sh -c "echo X"` UNBLOCKED (negative control failed). Two compounding facts: the frontmatter deny was never parsed as a rule, AND the probe session was permission-bypassed (`--dangerously-skip-permissions`) so even a settings.json deny wouldn't have applied. Impact: the ONLY ever-enforced reviewer boundary was `tools:` excluding Bash (tool absence IS enforced). 0.28.5's ADR-001 added Bash to 3 codex agents' `tools:` UNCONDITIONALLY → those agents got unrestricted shell; the "scoped to codex exec via permissions" claim is fiction. executor's `Write(/etc/**)`/`Edit(~/.ssh/**)` deny is cosmetic for the same reason (it has the Write/Edit tools; nothing frontmatter-level path-scopes them). Fixes: (1) CLAUDE.md §보안/권한 annotated with enforcement reality (2026-06-02); (2) 0.28.6 makes the codex `tools: Bash` CONDITIONAL on `codex_second_opinion.enabled` (shrinks blast radius to opted-in users — accepted residual: codex users' 3 agents still get full Bash). Real per-agent command scoping needs settings.json deny / a PreToolUse hook / sandbox — none expressible in frontmatter. Prevention: never treat subagent frontmatter `permissions` as a boundary; grep the recognized-fields list before assuming a frontmatter key is honored. Cross-ref [[subagent-tools-field-hard-gates-bash-permission]].

## [fail:api] codex-output-schema-strict-mode-required-completeness | 2026-06-03 | count:1
`.claude/schemas/codex-finding.schema.json` (the `codex exec --output-schema` contract for the second-opinion reviewers) was authored as vanilla JSON-Schema: `additionalProperties: false` + a `required` array listing only the genuinely-mandatory keys (`["findings","summary"]` top-level; `["severity","message"]` per finding), leaving `confidence`/`file`/`line`/`evidence` as "optional", plus `minimum`/`minLength` constraints. OpenAI/Codex **strict** structured-output mode forbids exactly this: under `additionalProperties: false` every `properties` key MUST be in `required`, and it rejects range/length constraint keywords. Codex returned `invalid_json_schema` on EVERY call, and `plan-validator`/`code-reviewer`/`consensus-arbiter` silently fell back to a prompt-pinned shape each pass (retries + latency, eventual hard-fail risk) — looked like "codex flaky", was a deterministic schema rejection. Fix (0.28.7): every property in `required`, optionality encoded as nullable union (`["X","null"]`), constraints dropped. Lesson: a `--output-schema` for OpenAI/Codex is NOT a general JSON-Schema — validate it against strict-mode rules at author time (now guarded by `tests/unit/test_schema_strict_mode.py`). Same family as `[fail:design] producer-consumer-schema-drift-in-same-process-pipeline` (a schema contract that no test exercised against its real consumer). Cross-ref wiki [[openai-codex-strict-output-schema-rules]].

## [fail:test] schema-walker-scans-property-names-as-keywords | 2026-06-03 | count:1
The first cut of `strict_mode_violations` (the new strict-mode guard) walked the **raw dict tree** (`_walk_objects` yielding every dict) and checked `keyword in obj` for banned constraint keywords + `additionalProperties` default `True`. Two defects code-reviewer caught (probe-confirmed) in `/hm:review`: **(1)** walking the raw tree yields the `properties` MAP itself, so a schema with a field literally NAMED `pattern`/`format`/`minimum` false-positives the banned-keyword scan (a property name was treated as a schema keyword). **(2)** required-completeness was gated on `additionalProperties is False`, so an object that OMITS `additionalProperties:false` was silently passed — yet strict mode REQUIRES it on every object, i.e. the guard had a hole on the exact mistake it exists to catch. Fix: recurse **structurally** (`_iter_subschemas` descends `properties` *values*, `items`, `anyOf/allOf/oneOf` — never property names) and flag missing `additionalProperties:false` explicitly; locked by 2 new tests. Lesson: a JSON-Schema validator must recurse by schema STRUCTURE (property names are data, not schema), and a "validity guard" must assert the positive requirement (additionalProperties:false present) not just the conditional (if-present-then-complete). Cross-ref wiki [[openai-codex-strict-output-schema-rules]].

## [fail:design] rendered-fix-frozen-on-existing-installs-by-reconcile-keep | 2026-06-03 | count:1
The 0.28.7 codex-finding schema fix (correct in the source template + guarded by a unit test + committed + pushed) STILL did not work on the dogfood install: `/harness-maker:make --update` left the live `.claude/schemas/codex-finding.schema.json` at the broken shape. Root cause: pure-JSON schemas carry no provenance frontmatter (ADR-008), so `reconcile()` returned `KEEP("no-frontmatter")` and `cli.py` excluded them from the render blueprint — the rendered fix reached FRESH installs only, never EXISTING ones (the population that has the bug). The class of mistake: a "fix" verified at the source/template + unit-test layer can be silently capped at the **reconcile/distribution layer** — a passing test of the rendered output does not prove the user's existing file gets re-rendered. Surfaced only by manually re-rendering the dogfood and grepping the live file (the unit test rendered into a fresh tmp dir, so it was green while the real install stayed stale). Fix: forced-REPLACE reconcile branch for zero-user-content renders (0.28.8, PLAN-reconcile-schemas-always-replace). Prevention: when fixing a RENDERED artifact, ask "does `/hm:make --update` actually overwrite an EXISTING copy?" — add a boundary test that seeds a stale on-disk file and asserts re-render replaces it (not just a fresh-render test). Same family as `[fail:design] producer-consumer-schema-drift` (a layer the cheaper test never exercised). Cross-ref wiki [[reconcile-keeps-frontmatterless-renders-forever]].

## [fail:render] codex-exec-rejects-interactive-only-flag | 2026-06-03 | count:1
The rendered `codex exec` second-opinion recipe shipped `--ask-for-approval never` (copied from interactive-`codex` docs), but `codex exec` is the NON-interactive subcommand and rejects that flag (`unexpected argument` on codex-cli 0.133.0) → the second-opinion call errored on its FIRST line and skipped, for every user, since the feature first worked. It went unnoticed because (a) the `tools: Bash` gate had been separately broken until 0.28.6 so the call never ran at all, and (b) the **warn-and-proceed** failure policy turns any non-zero exit into a silent "skip, proceed Claude-only". Fix (0.28.9): drop the line (`--sandbox read-only` is the real isolation; exec has no approval prompt). **Meta-lesson — warn-and-proceed is a bug-hider:** this was the 3rd silent-skip cause in a week (no-Bash-tool → invalid-schema → invalid-flag), each masked by the same resilience wrapper. Any feature wrapped in warn-and-proceed needs a POSITIVE happy-path check (a real `codex exec` smoke / a `/hm:health` probe that the call actually succeeds), or breakage is invisible by design. Prevention: validate a CLI recipe's flags against the actual subcommand's `--help` (interactive vs exec differ), and smoke the real command on a recipe change. Cross-ref wiki [[codex-exec-is-noninteractive-no-approval-flag]].

## [fail:design] green-module-dead-prose-wiring | 2026-06-07 | count:1
PLAN-crossmodel-codex-gaps shipped `high_diff` + `codex_adapter` with passing unit tests, an APPROVED first REVIEW (grade A), and zero regressions — yet a second, deeper "냉정히" REVIEW found the feature was partly **inert in the real flow** because the rendered prose that drives the modules was mis-wired: `git diff --name-only` (no `HEAD`) saw nothing post-execute (staged diff) so the Side high-diff gate under-triggered; filenames-only (no `--added-lines`) made `boundary` unreachable, killing the LLM-boundary path + its INTEGRATION test; and `codex_adapter` was only *named* in prose, never invoked, so its deterministic severity-map was still LLM-applied. Cause: the first REVIEW graded modules in isolation; nothing tested the prose↔module seam. Same family as `[fail:design] rendered-fix-frozen-on-existing-installs-by-reconcile-keep` and `producer-consumer-schema-drift` — a correctness layer the cheaper test never exercises. Fix: `HEAD` + `--numstat`→`--added-lines`; new `codex_adapter adapt` CLI the prose actually pipes through. Prevention: for any "the stage LLM runs `python -m harness_maker.X`" recipe, audit that the rendered flags exercise the module's real branches (git-diff base, gate inputs) — and ask for a seam-focused review pass, not just isolated-module review. Cross-ref wiki [[llm-prose-invokes-python-module-the-wiring-is-the-bug]]. Also this unit: a P1 shell-injection (untrusted skip-reason inlined into `echo '{...}' | tool`) slipped past the same first review — see wiki [[untrusted-content-to-cli-pass-as-argv-or-stdin-never-quoted-blob]].

## [fail:runtime] git-status-porcelain-collapses-untracked-dir | 2026-06-12 | count:1
`git status --porcelain` (default) COLLAPSES a fully-untracked directory to a single `?? dir/` line instead of listing its files. PLAN-worktree-deliverable-blocks-create added a PER-LINE path filter to forgive `work-docs/PLAN-*.md` at `worktree create`, and the unit test passed `_is_deliverable_path` directly — but the integration test (`_has_user_dirty_state` via a real `git status`) failed: a fresh fixture's `work-docs/` was entirely untracked, so porcelain emitted `?? work-docs/`, which the deliverable regex (`work-docs/PLAN-...`) never matched → the guard still blocked. This also bites real users on a fresh project's FIRST `/hm:plan` (no committed deliverables yet → whole dir untracked → collapsed). Fix: `git status --porcelain -uall` in `_has_user_dirty_state` + `_list_user_dirty_files` so untracked dirs expand to individual files the per-line filter can see. **Prevention:** any code that filters/classifies `git status --porcelain` output PER PATH must use `-uall` (or `-u`) — the default's dir-collapse silently hides the very files a per-line predicate inspects; a direct-call unit test won't catch it, only an end-to-end test against a real `git status` on an untracked dir will. Caught here by checkpoint #8 (integration-boundary one-liner) in CLAUDE.md.

## [fail:design] codex-exec-recipe-compound-command-defeats-allow-rule | 2026-06-17 | count:1
PLAN-codex-second-opinion-sandbox's first impl rendered the main-loop Codex recipe as a single compound shell line: `content="<diff>"; prompt_tmp=$(mktemp); …; codex exec …`. Two latent defects the green unit suite could not see (only the k-of-3 heterogeneous Codex reviewer + a live permission probe caught them). **(1) Allow-rule shape (P1):** the paired `settings.json` allow rule `Bash(codex exec:*)` is a command-PREFIX match — a command starting with `content=` does not match, so a headless/autoloop run is denied at the permission layer (independent of the sandbox) → codex skips → the very "skipped: Bash permission gate" the PLAN set out to fix recurs under a new reason. Confirmed live: compound `codex exec` calls denied, bare `codex exec … < file` allowed. `dangerouslyDisableSandbox:true` disables the sandbox, NOT allow/deny, so it does not help. **(2) Untrusted shell expansion (P2):** the prose claimed a double-quoted `content="$diff"` assignment prevents `$(...)`/backtick expansion — false; double quotes expand. With the sandbox disabled an adversarial diff containing `$(…)` is host RCE. **Fix (one restructure):** split the recipe — build temp files in a separate step, write the untrusted diff to the prompt file via the **Write tool** (verbatim bytes, no shell), then run a BARE `codex exec … < "$prompt_tmp"` as its own command. Gate the Claude-only `dangerouslyDisableSandbox` directive on `{% if not is_codex %}` (codex-target skills embed the stage body with is_codex=True and cannot honor a Claude Bash-tool param). **Prevention:** for any allow-gated CLI call a stage prompt issues, render it as a bare command beginning with the allow-listed token AND keep untrusted input out of shell word-expansion (Write tool / argv / stdin redirect, never a double-quoted blob). Same family as `[fail:design] llm-prose-invokes-python-module-the-wiring-is-the-bug` (prose↔runtime seam untested by isolated module tests). Cross-ref wiki [[codex-exec-allow-rule-needs-bare-command]].
## [fail:type] typer-option-default-factory-positional-syntaxerror | 2026-06-20 | count:1
Writing a typer `--option` with a `default_factory` AND a flag name — `typer.Option(default_factory=Path.cwd, "--root", help=...)` — is a Python `SyntaxError: positional argument follows keyword argument` (the flag `"--root"` is a positional param_decl coming after the `default_factory=` keyword). It is NOT a typer error; the module fails to even parse/import, so the symptom surfaced as `ImportError`/SyntaxError at the test that imports the CLI app, not at the command. (The sibling `typer.Argument(default_factory=Path.cwd, help=...)` is fine because Arguments take no `--flag` positional.) Fix: for an option that defaults to cwd, use `root: Path | None = typer.Option(None, "--root", ...)` then `root = root or Path.cwd()` in the body — avoids the positional-after-keyword clash entirely and keeps cwd out of import-time evaluation. Prevention: never mix `default_factory=` with a positional `--flag` in `typer.Option`; pass `None` as the literal default and resolve in the body.
## [fail:design] agent-command-guard-adjacency-regex-and-static-deny | 2026-06-20 | count:1
PLAN-human-bottleneck-auto-advance Phase 4 `autopilot_guard` — first impl got an F at k-of-n review (2 code + security + Codex) for security holes a single pass would have shipped. **(1) static settings.json deny for "never-auto" (P0-class):** rendering the deny-list into `settings.json` is session-wide → blocks the USER's own manual `git push`/`rm` even with autonomy OFF. Fix: marker-gated PreToolUse hook (fires only while `.hm-autopilot` active) — the guard exists *because* auto-advance removes the human, so gate on exactly that. **(2) PreToolUse guard used `Path.cwd()` (P0):** during a worktree-isolated autonomous run cwd is `.worktrees/<wt>/` but the marker is at base root → guard silently no-ops in the exact mode it guards. Fix: payload-first root resolution (`workspace.current_dir`/env) + walk-up + `.worktrees/`-parent (mirror `worktree_gate`). **(3) adjacency regex `\bgit\s+push\b` (P1):** trivially bypassed by `git -c k=v push`/`git -C dir push`. Fix: word-tokenize (shlex), skip value-opts, check subcommand; on ValueError fall back to coarse split (block-biased), NOT return-None (false-NEGATIVE bypass). **(4) protected-surface only covered the Write tool (P0):** `echo > .claude/settings.json` (Bash redirect) and editing `.claude/hooks/hooks.json` (self-disable the guard!) slipped through. Fix: filename-substring Bash regex + Write-path regex covering settings + hooks.json. Prevention for ANY agent command-guard: gate on the autonomy marker (not static deny), resolve project root worktree-aware, tokenize (not adjacency-regex), cover Bash-redirect alongside the Write tool, and bias false-positives toward block. Same family as the codex-exec recipe failure (a security guard whose seam wasn't adversarially modeled). Cross-ref wiki [[never-auto-guard-marker-gated-not-static-deny]].

## [fail:design] validate-then-unrestricted-dict-update-bypasses-guard | 2026-06-20 | count:1
PLAN-human-bottleneck-auto-advance P5 `autopilot_ledger.append_event` — caught at k-of-3 review (by **Codex**, both Claude reviewers missed it; grade stayed A because single-source, but fixed voluntarily as a real contract defeat). Symptom: an ADR-009 membership guard (`event in EVENTS`, forbidding `iter_receipts.Verdict` literals on disk) was fully bypassable because the record was built `{"event": event}` THEN `record.update(caller_fields)` — `append_event(event="advanced", fields={"event": "pass"})` overwrites the validated `event` with a forbidden literal AFTER the check. Cause: validated keys applied BEFORE merging a caller-supplied dict that can contain the same keys. Fix: apply authoritative keys LAST (`record = dict(fields); record["ts"]=...; record["event"]=event`). Prevention: any "validate field X, then `dict.update(caller_data)`" is a validation-bypass whenever caller_data may contain X — set the validated keys after the merge, or reject reserved keys in the caller dict. Companion: enum-disjointness invariants should be STRUCTURAL (derive from the type via `get_args` + import-time assert) not test-only, so a future enum edit fails loudly. Cross-ref wiki [[validate-then-unrestricted-update-reopens-the-guard]].

## [fail:design] cli-side-effect-before-llm-gate | 2026-06-20 | count:1
PLAN-human-bottleneck-auto-advance P6 — caught at k-of-3 review (Codex + reviewer-B; reviewer-A judged it benign). Symptom: the `autopilot_caps boundary` CLI appended the `advanced` ledger event on its proceed path, but the stage-template prose evaluated the mandatory gate (review CHANGES_REQUESTED / wrapup push / plan arch-Q) AFTER calling the CLI. So a gate-pending stage recorded a phantom advance → step count inflated → step_cap fires early on STOPs (not advances) + double-counts on retry. Cause: a deterministic side-effecting recorder placed on a decision path the LLM may still abort. Fix: reorder the prose so the LLM gate is evaluated FIRST (absent-case=STOP, no CLI call) and the side-effecting CLI runs ONLY when the gate clears. Prevention: in any prompt+CLI hybrid, the LLM judgment gate must PRECEDE the deterministic recorder; never let an irreversible side effect run before the LLM has confirmed the action. Cross-ref wiki [[deterministic-side-effect-after-llm-gate]].

## [fail:type] str-of-str-enum-yields-repr | 2026-06-20 | count:1
PLAN-human-bottleneck-auto-advance P6 — found by the RED gate (a test flipped GREEN→RED right after a stringify was added). Symptom: `[str(s) for s in marker.pipeline]` produced `["AtomicStage.RESEARCH", ...]` not `["research", ...]`, so `args.current ("research") not in pipeline` was always True → every valid stage misclassified as "unknown_stage". Cause: `str(member)` on a `class X(str, Enum)` returns the Enum `__str__` (the `X.NAME` repr), not the value. Fix: compare/look up against the enum members DIRECTLY (`current in marker.pipeline`, `.index(current)`) — str-enum members are `==` their value, so `in`/`index` match a plain string; use `member.value` (never `str(member)`) when a value string is genuinely needed. Prevention: never stringify a str-Enum to get its value.

## [fail:design] enabled-predicate-diverges-from-clamp-fail-safe | 2026-06-20 | count:1
PLAN-human-bottleneck-auto-advance P7 `autopilot_ledger.smoke_check` — caught at k-of-3 review (reviewer-A, single-source; Codex + reviewer-B clean; grade stayed A, fixed voluntarily). Symptom: the `/hm:health` degradation probe computed `degraded = (yaml_level != "gated" and count == 0)`, so a typo'd/empty/pre-feature `autonomy.level` ("off", "") was treated as ENABLED → false "armed but never fired" alarm on an empty ledger. Cause: a NEGATIVE enablement predicate (`!= the-one-off-value`) diverges from the codebase's canonical clamp (`autopilot.effective_level` maps any unknown level → gated/off). Fix: positive allow-list `degraded = yaml_level in {"auto_safe","full"} and count == 0` + `choices=` on the CLI flag. Prevention: an "is feature on?" check must use the SAME known-good membership set the rest of the system clamps against, never `!= <off-value>`. This is the recurring "absent-case = feature black hole" meta-pattern (CLAUDE.md count:8) in predicate form. Cross-ref wiki [[enabled-predicate-must-match-the-clamp-fail-safe]].

## [fail:design] stage-only-finalize-writes-landed-marker-deletes-recovery-branch | 2026-06-20 | count:1
Discovered during PLAN-multisession-worktree-concurrency Phase 7 via the pre-existing-RED `test_create_time_prune_preserves_inflight_finalize_branch`. `_cli_finalize` (`worktree.py` ~L2657) writes the `refs/hm-landed/v1/<branch>` landed marker on `wt_rc==0` for BOTH `success` AND `stage-only`. A `stage-only` finalize stages work into the base INDEX but does NOT commit to HEAD (wrapup commits later) — yet the marker records the branch tip, so `prune_stale`'s marker-SHA-match path deletes the `wip(execute)` recovery branch BEFORE wrapup lands the content = biased-to-preserve invariant violation, data-loss-adjacent (count:3 contamination lineage). Correct fix is a one-line gate (`if auto_commit:` around the marker write — a landed marker MUST mean "content is in HEAD"). NOT fixed in Phase 7: the clean fix breaks `test_worktree_landed_marker.py::test_finalize_writes_landed_marker_at_branch_tip`, which deliberately PINS stage-only-writes-marker ("SOLE producer") — the pinned test encodes the buggy behavior. Resolution is a design decision in the data-loss core spanning PLAN-p6-p7-worktree-finalize + PLAN-worktree-deliverable-blocks-create → deferred to a dedicated `/hm:plan` (deep interview + review), not a loop-mode tacked-on fix. The experimental fix was applied then REVERTED when it broke the pinned test. Prevention: a landed/done marker must never be written by a stage that does not commit to HEAD; and when a "fix" conflicts with a deliberately-pinned test, FIRST ask whether the pin encodes the bug. Cross-ref wiki [[stage-only-finalize-must-not-write-a-landed-marker]].

## [fail:design] producer-consumer-timestamp-resolution-mismatch | 2026-06-21 | count:1
PLAN-human-bottleneck-auto-advance P8 — caught by the P8 mechanical e2e (NOT the P5-P7 units, which masked it by feeding the same explicit ts to both sides). Symptom: the autopilot step cap never fired in the real flow. Cause: the marker `created_at` was microsecond-resolution (`datetime.now(UTC).isoformat()`) but the ledger ts was second-truncated (`_utc_now_iso` used `strftime("...%SZ")`); `count_events` filters `ledger_ts >= marker.created_at` to scope steps to the session, so a same-second `advanced` event (truncated to `.000`) sorted BEFORE the marker's `.123456` and was dropped → step under-count → cap silently disarmed. Fix: align producer resolution to the consumer (`_utc_now_iso` → `isoformat()`). Prevention: (1) two timestamps that get compared must be emitted at one format/resolution from a single source — truncating one side is a latent same-instant bug; (2) a cross-component "producer writes X / consumer reads X" contract needs an e2e where BOTH sides use their PRODUCTION defaults — a unit test injecting the same fixture into both sides cannot catch the mismatch. Cross-ref wiki [[timestamp-resolution-must-match-across-producer-and-consumer]], [[e2e-acceptance-catches-what-same-input-units-mask]].

## [fail:design] task-land-squash-commits-whole-index-sweeps-concurrent-base-churn | 2026-06-21 | count:1
PLAN-multisession-worktree-concurrency, `worktree.task_land` — found by the Codex k-of-3 heterogeneous voter (BOTH Claude reviewers missed it; one even said "SAFE" on this exact axis), then EMPIRICALLY confirmed in a throwaway repo. Symptom: a task's squash-land commit on a shared base contained a concurrent session's pre-staged `.claude/memory/wiki.md` — cross-session contamination (count:3 class). Cause: `git merge --squash <branch>` + `git commit -m msg` with NO pathspec commits the ENTIRE base index; the dirty-base guard `_has_user_dirty_state` excludes `.claude/`/deliverables so it does NOT abort, and `git merge --squash` tolerates unrelated staged paths → they ride into the squash commit. Compounding (2nd Codex P1): `_squash_path_set`'s `git diff --name-only` honored `diff.renames=true` → a rename reported only the destination, so scoping the commit would miss the old path's staged deletion. Fix: `git commit -m msg -- *touched` (path-scoped, never whole-index) + `_squash_path_set` uses `--no-renames` (rename = 2 entries) + empty-touched (merge-base failure) aborts rc1-preserve instead of leaving orphan staging. Prevention: NEVER `git commit` (no pathspec) after `git merge --squash` onto a base another process can stage into; always scope to the squash's own rename-proof path set; never `git reset` a shared base index. Regression tests: `test_task_land_does_not_sweep_concurrent_staged_base_churn` (+ staged-preservation assert), `test_task_land_records_rename_under_diff_renames_config`. Heterogeneous-voter lesson reinforced: a different model surfaces a different blind spot — do NOT treat Codex as a tiebreaker footnote. Cross-ref wiki [[squash-to-shared-base-must-scope-the-commit-to-its-own-paths]].

## [fail:design] new-marker-content-field-must-update-every-reader | 2026-06-21 | count:1
PLAN-loop-marker-session-scoping P2 — adding a `claude_session_id:` CONTENT header to the per-session `.claude/.hm-loop-*` marker broke two finalize-stash integration tests because a FOURTH marker-content reader, `worktree._session_worktrees`, was not in the phase plan and still did `[Path(ln) for ln in text.splitlines() if ln.strip()]` — so it ingested the header line as a phantom worktree path, corrupting the session's worktree list at finalize. The plan-validator (W1) had warned the header-skip "must be explicit across ALL readers" and even named that 2 of the 3 KNOWN readers dropped it only by accidental existence-filtering; the regression confirmed the warning AND that there was a 4th reader nobody enumerated. Fix: route every reader through the shared `loop_marker.parse_marker_paths` (explicit `startswith("/")` rule). Prevention: when adding ANY field to a shared on-disk format, `grep` for EVERY reader of that file (not just the ones the plan lists) and make them share ONE parser — an unenumerated reader is the default failure mode. Tests: header-dropped-by-prefix-not-existence + the finalize-stash suite. Cross-ref wiki [[loop-marker-session-scoping-via-content-header]].

## [fail:design] degraded-fallback-can-reintroduce-the-very-bug-it-guards | 2026-06-21 | count:1
PLAN-loop-marker-session-scoping — the session-blind global `.hm-loop-active` (the degraded fallback for sessions with no id) was honored UNCONDITIONALLY by both the Stop-hook and the `loop-mode-active` detector. The k-of-3 re-review (cr#1 + concurrency + Codex) showed this re-opened the exact cross-session bug the feature set out to kill: a session WITH a valid id was blocked / forced into loop-mode by ANOTHER session's degraded global. Subtler still, this finding DIRECTLY CONTRADICTED the FIRST review's Codex-H2 ("a degraded loop must still block via the global even when the Stop payload has an id") — both can't hold because the Stop payload always carries an id even in degraded mode. Resolution: gate the global on `not sid` (honor it only for an id-LESS caller); a valid-id caller uses content-match exclusively. This SUPERSEDES H2 — chosen because the feature's core promise is parallel-safety (never interfere with a peer) which outranks guarding a degraded loop's own session. Lesson: a "degraded fallback" that is session-BLIND must be gated so it only governs sessions that are themselves degraded — otherwise it leaks across to healthy sessions and undoes the fix. And: a later review with new reasoning can correctly REVERSE an earlier review's finding; record the supersede explicitly. Cross-ref wiki [[loop-marker-session-scoping-via-content-header]].

## [fail:design] runtime-env-gate-dead-on-arrival | 2026-06-21 | count:1
PLAN-multisession-10-fleet-hardening Fix 4 (readiness `sessionid_envfile_live` hard-gate). The probe gated "are we in a Claude Code session?" on `os.environ.get("CLAUDE_ENV_FILE")` — but Claude Code exposes `CLAUDE_ENV_FILE` ONLY to HOOKS, not to slash-command `!`-Bash / `python -m` subprocesses (verified by a live env probe: UNSET in the command env). So `in_session` was always False → the hard-gate signal was permanently **N-A: dead-on-arrival**, silently NOT catching the degraded-loop state it exists to catch — the exact silent-degradation the feature was built to surface. ALL THREE review layers (plan 2-pass Codex+validator, execute self-review, k-of-3 review) PASSED it: every reviewer reasoned about code LOGIC, none probed the runtime env. Caught only by an actual `/hm:health`-driven `env | grep` during user verification. Fix: gate on `CLAUDECODE` (the session marker Claude Code DOES export to command Bash, alongside `CLAUDE_CODE_SESSION_ID`) — proven live (signal now emits + hard-gates). Also pin `CLAUDECODE` out in `tests/unit/conftest.py` autouse (pytest-under-Claude would otherwise leak it and floor the dimension). Lesson: any check that reads a runtime env var to gate behavior needs a LIVE probe in the TARGET execution context — code review cannot verify env propagation, only env probes can. See [[reference-claude-code-env-exposure]] (cross-project memory) and the companion wiki [[stale-o-excl-lock-reap-via-atomic-rename]].

## [fail:design] preserve-fix-deadlocks-its-own-reaper | 2026-06-21 | count:1
PLAN-worktree-prune-create-race ADR-003. The marker-strand fix made `_is_orphan_marker` PRESERVE a loop marker whenever a matching `.hm-finalize-stash-*` ref existed (so `post-commit-pop` could still find it). But the stash-ref DRAIN already skips any ref whose session-marker is still present. Result: a marker preserved by a DEAD ref (stash object gc'd OR content already in HEAD) and the dead ref MUTUALLY immortalize each other — marker kept ⇒ ref-drain skips it ⇒ marker kept … forever (plus it inflates the pending-stash queue guard). REVIEW k-of-3 Codex (single-source — both Claude reviewers missed it) caught the deadlock. Fix: the preserve-predicate must apply the SAME liveness test as the reaper it interacts with — `_marker_has_pending_stash` now preserves only for a LIVE stash (`_stash_object_exists AND NOT _stash_content_in_head`), so a dead ref no longer preserves the marker and the cycle drains. **Generalizable lesson:** when you add a "preserve X while Y exists" rule, check the rule that REAPS Y — if Y's reaper skips-when-X-present, you've built a mutual-preservation deadlock. The preserve condition must gate on Y being genuinely LIVE by the same predicate the reaper uses, not on Y's mere existence. Cross-ref [[worktree-inflight-protection-via-pre-create-reservation]].

## [fail:design] friction-looking-guard-was-load-bearing-safety | 2026-06-21 | count:1
PLAN-fleet-10-20-parallel-safety C3. RESEARCH + PLAN treated the create-time queue-guard's "≥2 live finalize-stashes blocks `worktree create`" as a pure fleet false-block to relieve, and a per-session fix excluding FOREIGN-owned stashes shipped + passed a Grade-A review. A fresh k-of-3 RE-review (the auto-fix had not been independently reviewed) then found Codex P0: it re-opens the count:3 `worktree-finalize-pulls-orphan-wip-into-main` contamination. Chain: Layer 3 (`post-commit-pop` glob over ALL `.hm-finalize-stash-*` refs, gated by `HM_OWNED_SESSION_UUIDS` from `_owned_session_uuids`) reads ALL sessions' `.hm-loop-*` markers — its own comment says it "preserves prior (vulnerable) behavior" — so a session's post-commit-pop RESTORES A PEER's deferred stash. Counting foreign stashes in `_count_pending_stashes` is the LOAD-BEARING gate compensating for that weak Layer 3. **Lesson 1:** before relaxing a guard that "just causes friction", trace what it gates at RUNTIME through the full chain (here: post-commit-pop's owned-set), not the guard's local docstring — a friction block can be the operative half of a defense whose other half is known-weak. **Lesson 2 (recurring, see [[preserve-fix-deadlocks-its-own-reaper]]):** heterogeneous k-of-3 earns its cost on worktree internals — Codex read the `_owned_session_uuids` IMPLEMENTATION (all-markers) + the vulnerability comment and flagged P0; two Claude reviewers trusted its "owned by THIS process" docstring and passed it SOUND. Fix: reverted C3 entirely + a load-bearing comment on `_count_pending_stashes`; the real fix is hardening Layer 3 (per-session `--owned-uuid` into post-commit-pop), a separate plan.

## [fail:design] per-session-key-that-unions-by-a-non-session-key | 2026-06-21 | count:2
PLAN-layer3-per-session-ownership. The Layer-3 hardening replaced the all-markers owned-set with a "per-session" **slug-keyed crumb** (`.claude/.hm-owned-uuids-<slug>`) that execute writes + wrapup reads by its `<slug>` arg. Shipped + the docstring claimed "same-slug collision is last-writer-wins → never a peer (fail-safe)". k-of-3 review (Codex + concurrency, both) found it FALSE: `_owned_crumb_add` is a read-modify-write UNION, and the key is the SLUG not the session — so TWO DIFFERENT sessions on the SAME slug accumulate BOTH uuids into one crumb, and the first wrapup's post-commit-pop then pops the OTHER session's still-live deferred stash (the exact cross-session contamination Layer 3 exists to close). **Lesson 1:** a "per-session" guarantee requires a per-SESSION key. A scheme keyed by a coarser identifier (slug, project, branch) that UNIONs/accumulates entries can authorize a peer — verify the key actually partitions by session, and that the merge op (union vs overwrite) matches the claim. **Lesson 2 (producer-gate completeness):** the fix added a render-grep gate asserting `execute.md`/`wrapup.md` source from the crumb, but Codex found a THIRD producer (`worktree-isolator/SKILL.md`) running `post-commit-pop` env-less — broken by the same guard change, missed by the gate. When you change a consumer's contract, grep EVERY producer (commands AND skills), and make the gate scan all of them. **Lesson 3:** `_write_stash_ref_file`'s `effective_uuid = … or _current_session_uuid` gave a legacy bare-timestamp ref a uuid that `wt-uuid` (empty on that name) couldn't reproduce into the crumb → stranded the owner's OWN legacy stash; two derivations of "the ref's uuid" MUST agree. Fixes: corrected docstring + empty-slug CLI guard (closes cross-UNRELATED-task collision) + dropped the `_current_session_uuid` fallback + SKILL rewired + gate extended. Residual ACCEPTED-RISK: same-slug-same-task peer-pop needs a SharedSlug guard on the flag-off crumb path (follow-up); distinct-slug fleet (the goal) is fully fixed; no worse than pre-fix. Heterogeneous review again: code-reviewer mis-cleared the same-slug case trusting the wrong docstring; Codex+concurrency read the union code + caught it.
- [2026-07-18] Same family, new surface: `guard_when=pipeline_only` keyed the guard-arming `.hm-pipeline-active` crumb on `worktree._current_session_uuid` — named "session" but PROJECT-scoped + persistent (reads back the same value every session, no TTL). So a crumb from a prior run (or a marker-less standalone stage, never reaped by a terminal `clear()`) matched every future SAME-project session → `pipeline_only` dormancy silently defeated after the first stage run (the feature stops working, guard nags again). code+security review both flagged it P1 (safe over-block direction, but functionally broken). Confirms Lesson 1 + the [[owned-session-uuids-is-all-markers-not-per-session]] rule: a helper NAMED per-session whose body reads project-stable state is NOT per-session — verify the implementation. Fix: crumb stores per-session `HM_SESSION_ID` (sanitized, like loop_marker); guard matches its own payload `session_id` → a foreign session's crumb is dormant, no clear-on-arm, parallel-safe. An interim clear-on-arm fix was tried first, then superseded.

## [fail:design] partial-guard-on-degrade-path | 2026-06-22 | count:2
A CLI/function that advertises "NEVER raises / ALWAYS exits 0" must guard EVERY untrusted-input failure mode, not just the obvious parse error — and a partial guard is worse than none because the contract makes callers (here: the wrapup template) trust it blindly. The new `spec_machine waiver-check` (a never-raises advisory over a hand-authored SPEC.machine.yaml) shipped with FIVE holes the k-of-3 review caught: (1) `except (yaml.YAMLError, OSError)` misses `UnicodeDecodeError` (a ValueError subclass, NOT OSError) from a non-UTF-8 file; (2) `(ac.get('oracle_evidence') or '').strip()` and the waiver `.strip()` run OUTSIDE the parse try, so a non-str `oracle_evidence: [x]` / `waiver: 12` raises AttributeError and escapes; (3) a non-dict AC entry (`ac: [123]`) was silently `continue`d → returned a clean `ok` instead of `check_error` (the FALSE-CLEAN-PASS, the worst outcome — same absent-case-black-hole family as count:8); (4) the receipt write (mkdir/append) ran outside any guard → an unwritable --root broke exit-0; (5) `atomic_append`'s 4096-byte POSIX atomicity precondition was unguarded for a large flagged list. THE CLEAN FIX is two-layered: make the function honest (broaden the parse except to include ValueError; on a malformed AC shape/field return `check_error`, never `ok`) AND add a belt-and-suspenders `try/except Exception → check_error` floor at the CLI boundary (`_run_waiver_check`) plus `contextlib.suppress(OSError)` around the best-effort receipt. GENERAL RULE: for any "never-raises" boundary, (a) the status enum MUST distinguish could-not-run from clean, (b) a top-level except-Exception floor at the entry point is mandatory, (c) coerce/`isinstance`-check every untrusted field before a method call, (d) honor downstream byte-limit preconditions (atomic_append <4096). Cross-model note: the non-dict-false-ok (3) was caught ONLY by the heterogeneous Codex voter; the two Claude reviewers focused on the crashes and missed the silent-ok.
## [fail:design] gate-collapses-states-into-clean-pass | 2026-06-22 | count:1
A fail-closed verification gate that COLLAPSES distinct sub-states — "pytest ran cleanly" vs "collection ERROR (rc≠0, e.g. a test-file import error)" vs "pytest unavailable" — into one boolean and then DISCARDS the boolean produces a SILENT FALSE-PASS on the dangerous branches. In nonmechanical-ac-binding's `find-unbound`: `_pytest_collect_nodeids` returned `(nodes, ran)` but `_collectable_ac_tests` did `if not ran: return set()`, so pytest-unavailable + a pending closed-type AC → empty collectable → `pending & {} == []` → CLI exit 0 (clean pass) — the exact OPPOSITE of the template's documented "pytest unavailable → fail-closed STOP". Worse, rc≠0 with partial stdout was treated as `ran=True`, so a genuinely-authored AC whose test failed to collect (broken import) was mis-classified as "no test → future work → safe-skip" → false PASS. The feature whose whole point was a fail-CLOSED gate was nearly shipped fail-OPEN. Caught by ALL THREE k-of-3 voters (2 Claude lenses + Codex) independently — strongest possible consensus signal. Fix: thread the clean-run signal up and RAISE `BindingGateUnavailableError` when there IS work to adjudicate but pytest didn't run cleanly (`rc∉{0,5}`; 5=no-tests-collected is legitimately empty); the early "no pending work" return stays a genuine exit-0 (pytest never invoked). LESSON: for any fail-closed gate, "could not evaluate" must be a DISTINCT, surfaced FAIL — never folded into "evaluated, found nothing". Test the can't-evaluate branch explicitly by monkeypatching the subprocess helper (`_pytest_collect_nodeids`), NOT the function under test — mocking the function under test hides exactly this bug (the original tests mocked `_collectable_ac_tests`, so the collapse was invisible to CI).
## [fail:design] gate-scope-all-exists-wrong-polarity | 2026-06-23 | count:1
A presence/scope gate that requires ALL declared inputs to exist before it fires INVERTS the safe default — it SKIPS (treats as out-of-scope/future) exactly the partially-present case that should BLOCK. In judgment-ac-binding, `_subject_paths_exist` used `all((cwd/p).exists() for p in subject_paths)`; a judgment AC with a multi-file subject where one file shipped this PLAN and one is genuinely future → `all(...)` False → `find_unjudged` skipped it → the already-built half rode free behind the absent sibling. This is the absent-case black hole (CLAUDE.md #6, failures.md count:8) recurring ONE LEVEL DOWN from the field-presence guard (ADR-007) it was meant to complement. Caught by code-reviewer + Codex in k-of-3. FIX: a gate's in-scope predicate must be the SAFE-default polarity — fire (block) when ANY input is present OR when the config is missing/empty (misconfigured ≠ exempt); only a FULLY-absent input is genuine future work. `_judgment_in_scope` = `not subject_paths or any(exists)`; the downstream hash then raises on the missing member → unbound. LESSON: for any "is this in scope for the gate?" check, ask "what does the AMBIGUOUS / PARTIAL / EMPTY case do?" — it must default to gated, never skipped. An `all()` scope-guard is almost always the wrong polarity for a fail-closed gate.
## [fail:runtime] leaf-symlink-check-misses-dir-escape | 2026-06-23 | count:1
A per-entry `f.is_symlink()` check during a directory walk catches ONLY a leaf symlink, missing an intermediate symlinked DIRECTORY — and on Python 3.12 `Path.rglob` FOLLOWS symlinked subdirs (the no-follow default arrives in 3.13). In judgment-ac-binding's `_iter_subject_files`, a subject dir containing a symlinked dir `evil -> /etc` made `rglob` yield `subjdir/evil/passwd`; the leaf `passwd` is not a symlink → the escape guard was bypassed and `read_bytes()` would read `/etc/passwd` into the subject hash. It failed-closed only by ACCIDENT (a bare `ValueError` from `relative_to(root)` on the out-of-root resolved path — and `mark_judged` doesn't catch `ValueError`, so it crashed with a raw traceback instead of the contract's clean error list). Caught by security-reviewer + Codex. FIX: never trust `is_symlink()` on a walked leaf as a traversal guard — (1) use `os.walk(followlinks=False)` so symlinked subdirs are NOT descended (also kills symlink-cycle infinite-recursion), and (2) `resolve()` + `is_relative_to(root)`-confine EVERY yielded file unconditionally, raising the domain error (SubjectHashError) on escape. Also add a TOTAL file-count + total-bytes cap (not just per-file) for an untrusted directory subject. LESSON: filesystem traversal over untrusted path config must resolve-and-confine every concrete file, and must pin the Python version's symlink-follow semantics (3.12 follows; 3.13+ does not) — a leaf-only symlink check is not a containment boundary.
## [fail:design] count-must-reuse-detector-scope | 2026-06-23 | count:1
A user-facing DISPLAY count (or summary) that re-derives "which items are live/fresh" with a LOOSER predicate than the authoritative gate/detector overcounts. In judgment-stale-health-display, the `/hm:health` "N judgment verdict(s) fresh" tally was computed as `sum(1 for a in acs if a.judgment_verdict == "pass")` — a raw verdict check — while the detector that actually decides fresh-vs-stale (`stale_judgment_verdicts`) first excludes out-of-scope ACs via `_judgment_in_scope` (a pass whose subject is fully ABSENT on disk is future-work, neither fresh nor stale). So a pass verdict with a deleted/renamed subject was silently counted as "fresh", inflating the number against the detector's own scope rule. Caught by k-of-3 review (BOTH Claude reviewers, consensus P1) before commit. FIX: derive the count by REUSING the exact scope predicate the detector uses (`... and _judgment_in_scope(a, project_dir)`), never a hand-rolled looser version. LESSON: when you display a count/summary that mirrors a gate or detector's decision, import and reuse that component's scope/filter predicate — re-implementing it loosely drifts from the source of truth and overcounts (this is the scope-polarity family — cf. gate-scope/absent-case failures — applied to a display surface, not just a gate).
## [fail:design] gate-field-missing-from-producer-schema | 2026-06-23 | count:1
A downstream GATE that keys on a frontmatter/output field is silently DEFEATED when the upstream PRODUCER stage's REQUIRED output schema omits that field — the agent follows the schema literally, the field is absent, and the gate's absent-case (N-A / clean-pass) lets everything through. In spec-requirement-gate, `/hm:plan` Step 1.7 computed `spec_need_verdict`, and `/hm:verify` Check 6 was designed to FAIL on `verdict∈{add,change,delete,not-evaluated} ∧ ¬operation ∧ ¬waiver` — but Step 5's required PLAN frontmatter block did NOT list `spec_need_verdict`/`spec_need_target`, so an agent writing the frontmatter omits them → Check 6 reads "no verdict" → its (correct, by-itself) absent-frontmatter N-A clean-pass fires → the entire fail-closed gate is bypassed. Each stage reviewed in isolation looked correct; only the CROSS-STAGE producer↔consumer link exposed it (caught by Codex in k-of-3 — the two Claude reviewers, scoped to Python-module and to prose respectively, each saw their half as correct). A companion test-fidelity gap: a unit test injected a `verdict:"waived"` value that NO producer ever emits, so it green-tested a path that can't occur. FIX: when a gate keys on a field, that field MUST be in the producer's REQUIRED output schema AND the producer must ASSERT its presence (Step 6 write-back assertion); and a consumer test must build its fixture from the REAL producer, never a hand-injected value the producer can't generate. LESSON: the absent-case black hole (CLAUDE.md #6) recurs at every producer/consumer boundary, ESPECIALLY across stages — review the full data-flow (who writes the field the gate reads?), not each stage alone, and bind consumer tests to real producers.
## [fail:design] pin-upper-bound-vs-documented-intent | 2026-06-24 | count:1
A dependency-pin upper bound that does not enforce its documented intent. `mutmut>=2.4,<4` carried the comment "3.x CLI dropped --paths-to-mutate; pin pending wrapper rewrite", but `<4` allows all 3.x — the intent lived in the comment, not the constraint. The wrapper hard-codes the 2.x `--paths-to-mutate` CLI, so a 3.x resolve would make the mutation gate spuriously FAIL at 0% (NOT a graceful skip: `_MUTMUT_ABSENT` only matches "command not found", not 3.x's "unrecognized arguments", so the parser yields zero counters → score 0.0 → gate FAIL). Cause: encoding a version-compatibility decision in prose instead of the version specifier. Fix: tighten to `<3` so the constraint matches the intent + a runtime `mutmut --version` major-guard that loud-skips 3.x; and check `proc.returncode != 0` so a broken install whose failing `--version` emits version-shaped stderr (e.g. a "3.12" traceback) doesn't become a false unsupported-skip. The returncode gap was caught by the Codex k-of-3 heterogeneous voter and missed by both Claude reviewers — a concrete instance of cross-model review value.
## [fail:design] cap-removal-unmasks-pipeline-loop | 2026-06-25 | count:1
Making autopilot's runaway caps nullable (`None` = unlimited) silently removed the only bound that was MASKING a latent bug: `autopilot_caps.next_stage()` resolves the next stage via `list(pipeline).index(current)` — the FIRST index — so a duplicate-stage pipeline like `[execute, execute]` returns the same stage forever. With a finite `step_cap` the chain eventually halted; with unlimited caps it loops and never reaches the wrapup merge-gate. The "pipeline finiteness is the bound" boundedness invariant silently ASSUMED unique stages. Fix: duplicate-rejection `field_validator` on BOTH `AutonomyConfig.pipeline` and `AutopilotMarker.pipeline` (the one `next_stage` actually consumes) → a duplicate pipeline can no longer be armed. Two transferable lessons: (1) when you remove a backstop (cap / timeout / retry-limit / circuit-breaker), explicitly audit what latent failure it was masking — the backstop's removal is the regression's trigger even though the bug predates it; (2) a "finite collection ⇒ terminates" argument only holds if the iterator advances MONOTONICALLY — a first-index lookup over a non-unique list does not, so the collection must be validated unique. Cross-model note: the heterogeneous Codex k-of-3 voter caught this; both Claude reviewers' boundedness tests exercised only the canonical unique pipeline and missed it (same pattern as the documented queue-guard P0 Codex caught).
## [fail:design] slash-effectful-git-ungated-unscoped | 2026-06-28 | count:1
Two distinct defects in generated slash-command bash that runs effectful git, both caught only at REVIEW (code-reviewer P1 + ux-reviewer P2 consensus, plus Codex P1 as the 3rd voter). (1) **Ungated `!`-autorun**: the git-disposition commit/`git-ignore-roots` snippets in `commands/make.md` §6.5 were written with a `!` prefix. In Claude-Code slash commands a `!`-prefixed bash line is AUTORUN by the command expander at load time, NOT gated by the surrounding choice prose — so invoking `/harness-maker:make` would apply the commit/ignore BEFORE the user picked commit-vs-gitignore (validator round-2 had flagged the same class for `make.md.j2`'s dispatch line; the fix there was applied but the meta command regressed). Fix: remove `!` from every effectful slash action so the orchestrator runs it via the Bash tool only after the user's choice; reserve `!` for read-only autorun. (2) **Unscoped `git commit`**: `for r in <roots>; do git add "$r"; done; git commit -m "…"` with no pathspec commits the ENTIRE index, so a user with unrelated pre-staged work who picks "Commit them" gets it swept into the harness commit (same class the harness's own Layer-5 scope-guard defends). Fix: scope BOTH the add and the commit — `git add $roots && git commit -m "…" -- $roots`. Lesson: any generated slash command that runs `git add`/`git commit`/a mutating CLI must (a) be a non-`!` block gated behind the user's choice, and (b) scope every commit with a `-- <pathspec>` so it never sweeps the user's pre-staged index.
## [fail:render] invocation-convention-split | 2026-06-29 | count:1
Symptom: a consumer session (`~/spoton`) repeatedly emitted `harness-maker: command not found` and `python: command not found` while trying to arm/advance autopilot from inside a worktree. Cause: the rendered slash-command templates carried THREE invocation forms — the correct inline `uv run --with <src_path> python -m harness_maker…` (105 sites) plus ~57 broken sites (26 bare `python -m`/`harness-maker <subcmd>` + 31 `uv run python -m` WITHOUT `--with`); both broken forms resolve against the consumer's environment instead of harness-maker's source, so they fail on any plugin-cache install. The first audit undercounted because a `grep -v "uv run"` filter hid the 31 `--with`-less sites (Codex caught it). Fix: normalize all ~57 to the single inline launcher (ADR-001), add a render+source gate so a 58th cannot regress (ADR-002). Lesson: when auditing invocation forms, never filter on a substring (`uv run`) that a broken variant also contains — count full-launcher matches vs bare matches per line instead.
## [fail:design] marker-ledger-root-asymmetry | 2026-06-29 | count:1
Symptom (caught by code-reviewer in REVIEW round-2, not by tests): after introducing `resolve_marker_root` and applying it to the autopilot marker read/write/clear, the autopilot_caps `boundary` + `gate-blocked` handlers resolved the MARKER to the base root but still passed the UNRESOLVED worktree root to the paired ledger ops (`count_events`/`append_event`/`record_cap_halt`/`clear`). Result: `marker.created_at` lived at base while the `advanced` events landed in the worktree-local ledger that gets torn down with the worktree — splitting the step count and making `/hm:health`'s smoke-check read an empty base ledger. Cause: a path-resolution helper was applied to one of two paired operations that MUST share a root. Fix: resolve the root ONCE at the top of each handler (`root = autopilot.resolve_marker_root(Path(args.root))`) and thread that single value to both the marker and the ledger calls. Lesson: when a function does a marker op AND a ledger/sidecar op keyed to the same root, resolve the root once and reuse it — never resolve inside only one of the pair.
## [fail:design] wrapup-memory-base-seam | 2026-06-30 | count:1
Symptom: per-task feature-branch wrapup appeared to commit memory but the human tiers never reached git. Cause: memory_md writes to the BASE repo (`_base_root` strips the worktree suffix) while wrapup Step 6 `git add` runs in the worktree — staging an unchanged tree — and task-land's squash is path-set-scoped, so the base-written memory was preserved as uncommitted base dirt, never committed (a silent non-commit, not the originally-assumed dirty-base abort). Fix: a dedicated fenced+pathspec-scoped `commit-base-memory` amend after task-land, anchored on task_land's in-fence squash SHA. Lesson: when a write target and its `git add` cwd differ across the worktree/base boundary, the add silently stages nothing — verify the commit actually contains the bytes, don't assume `git add <dir>` covers a path written elsewhere.
## [fail:design] add-parser-multiline-extraction-miss | 2026-07-01 | count:1
Extracting a module's argparse subcommands with a line-oriented `add_parser("...")` grep MISSES multiline `add_parser(` calls (the name literal sits on the next line), silently dropping real subcommands. Symptom: `spec_inventory generate-all` was omitted from the command registry SSOT even though the module registers and dispatches it. Cause: single-line regex over multiline calls. Fix: enumerate subcommands with `ast.walk` over `add_parser` Call nodes' first constant arg, and add a bidirectional registry↔source parity test (T-C2) that fails CI on any omission — not just phantoms. Meta-lesson: the cross-model reviewer (Codex) caught the miss that two Claude reviewers and the author missed — heterogeneous review earns its keep on completeness gaps.
## [fail:test] agent-body-sha-baseline-not-bumped | 2026-07-01 | count:1
Editing an agent body template (`templates/agents/<name>_body.md.j2`) breaks `tests/unit/test_agent_body_partials.py::test_full_agent_md_sha256_unchanged[<name>]`, which pins the rendered agent's SHA-256 to a hardcoded `_EXPECTED_SHA256` baseline dict. Symptom: `Agent '<name>' output changed after body partial refactor!` with expected vs actual hashes. In PLAN-review-grade-criteria the consensus-arbiter 4c hard-seal edit changed the body; `/hm:execute` Phase D ran `tests/unit` but the miss slipped through to `/hm:verify` Check 2 (regression smoke). Fix: when editing ANY `agents/*_body.md.j2`, bump the matching `_EXPECTED_SHA256` entry to the test's printed `actual` value AND document the reason inline next to it (the file already carries per-agent bump comments). Note the pin renders with a codex-disabled test config, so edits outside the `{% if codex_second_opinion %}` block still move the hash. Guard: any `agents/*_body.md.j2` diff → grep `_EXPECTED_SHA256` for that agent and re-pin in the same change, before Phase D closes.
## [fail:tooling] mark-tested-pytest-env | 2026-07-03 | count:1
`spec_machine mark-tested` (wrapup Step 3.5) reported "test_id does not resolve via pytest --collect-only" for EVERY AC even though `pytest --collect-only <node>` resolved fine standalone. Cause: mark-tested's internal `subprocess.run(["pytest", "--collect-only", ...])` calls BARE `pytest`, which under `uv run --with <plugin-path> python -m harness_maker.spec_machine ...` resolves to a global `/home/noel/.local/bin/pytest` that has no `harness_maker` installed → conftest.py `ModuleNotFoundError: harness_maker` → zero nodeids collected → all ids reported unresolved. The `--with <plugin>` ephemeral uv env has harness_maker but NOT pytest (a dev-only dep), so the subprocess `pytest` falls through to the global one on PATH. Fix: run the wrapup mark-tested / find-unbound / waiver-check commands via plain `uv run python -m harness_maker.spec_machine ...` (the PROJECT dev venv, which has both editable harness_maker AND pytest, and whose `pytest` is first on PATH) — NOT via `uv run --with <plugin-path>`. Same failure would hit any spec_machine subcommand that shells `pytest` for collection. Guard: when a collect-only-backed gate false-negatives every id at once, suspect the subprocess pytest is the wrong interpreter, not the tests.
## [fail:lint] format-gate-single-file-miss | 2026-07-03 | count:1
`ruff format --check` FAILED (FMT=1) at both /hm:verify and /hm:wrapup on `command_registry.py`, but was masked earlier because I only ran `ruff format` on the specific files I edited with the Edit tool — command_registry.py was edited via a `python3 - <<EOF` heredoc script (inserting a multi-line ModuleSpec block) whose indentation/line-wrap did not match ruff's formatter, and I never ran `ruff format` on it. Per-file lint/format checks on "the files I touched" silently miss a file mutated through a non-Edit path (heredoc script, sed, generated insert). Fix: `ruff format src/ tests/` (whole tree) before the final gate, never a hand-curated file list; the full-suite verify gate correctly caught it. Guard: after ANY heredoc/script-based source insertion, run tree-wide `ruff format` + `ruff check`, not just on the Edit-tool files — the format violation does not surface in `ruff check` (lint), only in `ruff format --check`.
## [fail:design] sibling-code-path-bypasses-shared-guard | 2026-07-04 | count:1
Symptom: adding a NEW sibling code path that writes content into a structured block silently bypassed an existing invariant guard, reintroducing the exact corruption the guard exists to prevent. In memory_md._upsert, the pre-existing heading-shape guard runs only on `body_lines`; a newly-added new-entry `--occurrence-note` seed path wrote the note as an UNPREFIXED body line, so a heading-shaped note (`## [fail:x] …`) became a phantom heading on the next `_entry_headings` scan (silent truncation / tier DoS). Two review sources (Codex + code-reviewer) caught it; a THIRD instance of the same class (marker-absent creation branch dropping the note) was found only by a Codex re-verification AFTER the first fix — proving the guard-parity gap spanned multiple branches. Cause: a guard attached to one branch's input is not automatically enforced on a sibling branch writing to the same destination. Fix: extract the seed/collapse logic into a SINGLE shared helper (`_new_entry_body` + `_collapse_note`) that every creation path routes through, so the guard + fail-closed + safe-format contract cannot be bypassed by adding a branch. Lesson: when adding a code path that writes to a shared structured target, route it through the SAME validated writer the existing paths use — never re-implement the write inline, or the next reviewer/re-verify finds the guard hole one branch at a time. Related: global learning absent-case=feature-black-hole (a feature that activates on an optional path must define that path's guarded behavior).
## [fail:design] shared-extractor-consumer-parity | 2026-07-04 | count:1
When a shared parse/transform helper runs uniformly across tiers/variants but its output is consumed by only SOME of them, the un-consuming variant silently loses whatever the helper extracted. `consolidate`'s `_parse_entries` ran `_split_body_bullets` on BOTH failures and wiki entries, peeling any trailing `- [YYYY-MM-DD]` run into `.bullets`; but only the failures merge branch re-emits `.bullets` — the wiki merge builds from `.body` alone, so a wiki dup whose body ended with a dated line (e.g. `- [2026-06-08] see MEMORY.md`, a style used throughout this repo's memory) had that line silently, irreversibly DELETED on merge. Happy-path tests (wiki bodies with no trailing dated line) and the concurrency review all missed it; the code-reviewer caught it as P0 and Codex independently converged on the same wiki asymmetry (k-of-3 heterogeneous review value). Fix: gate the helper to the tier that consumes its output — wiki now takes the whole body via `_strip_trailing_blank` (bullets always empty). Rule: when a shared extractor feeds multiple consumers, assert EACH consumer re-emits everything the extractor pulled out, or scope the extractor to only the consumers that use it — and write a test that exercises the extract-relevant shape for EVERY consumer, not just the one you were focused on.
## [fail:design] prose-refactor-removal-sweep-gaps | 2026-07-10 | count:1
A grep-based sweep to remove a concept from docs/templates misses instances two ways: (a) BILINGUAL docs — an English-only search pattern skips the localized analog (this repo: Korean `비자명한 결정` / `세션별 로그` survived an EN-only sweep); (b) REWORDED prose — the concept restated as a walkthrough (a wrapup "Step 5 — Update Memory: three files" list) does not match the canonical keyword. Fix: sweep BOTH languages with language-specific terms AND grep for the structural CONTAINER (any list that enumerates the tiers), not only the removed keyword. This refactor's self-sweep reported CLEAN, then cross-model second opinion (codex + antigravity) + code-reviewer surfaced 6 real misses — concrete cross-model review value, echoing [fail:design] pin-upper-bound-vs-documented-intent (cross-model caught what Claude self-review missed).
## [fail:other] git-checkout-wipes-worktree-edits | 2026-07-15 | count:1
During /hm:execute, ran `git checkout -- src/harness_maker/synthesize.py` in the task worktree intending to restore a file after a mutation test — but it silently discarded the stage's OWN uncommitted edits (the ADR-002 dev_mode pins). Root cause: /hm:execute never commits (wrapup owns the commit), so ALL in-progress src edits live UNCOMMITTED in the worktree, and `git checkout -- <path>` restores them to HEAD = wipes them. The pins had to be re-applied by hand. Compounding: the mutation-check heredoc used bare `python`, which is not on PATH in this env (`uv run python` is required), so the intended mutate→restore never ran and only the git checkout took effect. Prevention: NEVER `git checkout -- <path>` (or `git restore`) on uncommitted work during execute/review; for a self-restoring mutation test, back up with `cp <file> <tmp>` and restore in a `finally`, and always invoke Python via `uv run python`.
## [fail:test] assertion-pinned-defect-from-review-hint | 2026-07-17 | count:1
Phase A.5's test-reviewer flagged, as a NON-blocking note, that `test_merge_retires_group_only_when_every_command_is_harness_owned` could not distinguish "preserve the mixed group whole" from "prune our commands out of it", and added: *"command-level prune is arguably the better behavior."* I read the note as "pin the literal ADR wording" and added `assert gate in cmds`, pinning **preserve-whole** — the wrong branch of the very choice it flagged. REVIEW round 1 then found the P1 the note was pointing at (codex + code-reviewer, consensus): preserving the mixed group verbatim beside the shipped group registers our command **twice**, so the hook fires twice — the 2026-05-28 spoton-triplication class. code-reviewer named the mechanism exactly: *"its own fixture produces the P1 duplication above, so the test passes while encoding the defect."* The test was GREEN, the suite was GREEN, and the assertion was the bug. Root-cause class: a reviewer's **non-blocking** note that names an unresolved either/or is a DECISION REQUEST, not a style nit — and a hint of the form "X vs Y is untested, and Y is arguably better" must be resolved by picking a branch on the merits, never by pinning whichever branch the current prose happens to describe (the ADR wording was itself the unexamined half). Fix: `_strip_shipped_commands` — a preserved mixed group keeps the user's commands and drops the ones the template already ships; the test now asserts `cmds.count(hm_cmd) == 1`. Prevention: when a test-reviewer says "this test cannot discriminate A from B", write the test that discriminates BEFORE choosing, and treat any `assert` you add in response as a claim you must independently justify — not as compliance with the note. Related: `[fail:test] fence-position-test-pins-only-upper-boundary` (one-sided boundary) is the adjacent family — that one under-pinned; this one pinned the wrong thing.
## [fail:design] namespace-prefix-mistaken-for-authorship | 2026-07-17 | count:1
PLAN-permission-deny-and-hooks-wiring ADR-008 went through THREE mechanisms for "remove a harness hook the template no longer ships", and the first two both shipped a defect. (1) A sidecar fingerprint store (`.hm-rendered-hooks.json`) — the plan-validator found it would collide Claude↔Codex identities (same nested schema + same event names) and be per-machine (gitignored churn ⇒ a fresh clone never reaps). (2) Replaced at execute time with `ident[1].startswith("<HM>:")` — the prefix `_normalize_hm_managed_command` stamps on any `python -m harness_maker.*` command — on the reasoning "the normalizer already IS the ownership fingerprint, zero persisted state, both validator warnings dissolve." That reasoning was right about the store and **wrong about ownership**: `<HM>:` marks our NAMESPACE, not our AUTHORSHIP. security-reviewer (P1) gave the concrete, likely case: ADR-006's staged rollout deliberately does not ship `loop_gate`/`autopilot_guard`/`permission_gate`/`worktree_gate`/`spec_gate` yet, so a user who hand-wires one gets it **silently deleted** on the next render — **the staging itself creates the victim population**. codex found the same defect from the forgeable-prefix angle (a command literally starting with `<HM>:`) but rated it P2, and the consensus filter forbids bridging severity tiers, so two votes on one defect produced two `manual-only` findings and zero consensus. (3) The resolution was not a better ownership test: **Phase 1 does not need retirement at all** — it only matters once a template STOPS shipping something, which no Phase-1 template does. Benefit 0, cost = silent deletion from a user's config file. Retire was removed entirely; preservation is the invariant. Root-cause class: **a destructive operation gated on an inferred property**. Ownership is provenance ("did WE write this?") and can only be answered by a record of what we wrote — a prior-render manifest — never by pattern-matching the artifact's own text, which the user can also produce. Second lesson: before adding a delete path, ask what it buys THIS phase; if the answer is "nothing yet", the correct design is not a safer delete, it is no delete. Prevention: any new deletion of user-disk state needs (a) positive provenance and (b) a named beneficiary in the current phase. Cross-ref `[wiki:architecture] hooks-load-from-settings-not-hooksjson`, `[fail:design] stage-only-finalize-writes-landed-marker-deletes-recovery-branch` (same family: deleting on a proxy signal).
## [fail:test] absence-as-evidence-unverified-source | 2026-07-17 | count:1
Twice in one session I treated ABSENCE as evidence, and the second time it reached the user as a false "all green". (1) RESEARCH argued "hooks never fire" from `.claude/observability/metrics.jsonl` holding 2 Cursor-origin lines — the wrong file: it is the pre-0.7.1 `_LEGACY_NAME` (`_metrics_io.py:18`) and telemetry writes `metrics-YYYY-MM-DD.jsonl` (`telemetry.py:216`), which held **7,550** entries. The conclusion survived only because a controlled experiment re-proved it on different evidence; the reasoning was void. (2) Hours later, reading a background `pytest` log with `grep -c FAILED` → `0`, I reported "regression GREEN" — **the run had not finished** (6-line output, no summary line). Two tests were in fact failing (`test_render_hooks_json_valid_in_both_dev_modes`, broken by my own Phase-3 revert leaving `assert "PreToolUse" in claude["hooks"]` behind). A completed run confirmed both. Root-cause class: an empty/quiet source proves nothing until you establish that the source is (a) the one the producer writes, and (b) finished. `grep -c FAILED == 0` on a live log is indistinguishable from success — the log is quiet because the work is not done, exactly as a legacy path is quiet because nobody writes it. Prevention: (i) before citing a file's contents as evidence, grep the PRODUCER for the path it actually writes; a `_LEGACY_*` name is a reader fallback, never a writer target. (ii) Never judge a background command from its log tail — wait for the completion notification and read the EXIT CODE, or assert on a positive terminal marker (pytest's `N passed` / warnings summary), never on the absence of a failure string. (iii) Prefer a positive falsifiable signal ("X appears when I do Y") over an absence ("X isn't here, so Y never happened") — absence arguments fail silently, positive ones fail loudly. Note the irony: between (1) and (2) I promoted this exact lesson to the Second Brain as `failure-legacy-path-emptiness-as-false-evidence` — but never wrote it to the LOCAL tier, so `memory_retrieve` surfaced nothing when it recurred and the count++ dedup had no slug to increment. **Promotion is not a substitute for the local entry**: wrapup Step 5.6 promotes FROM 5.1-5.4, it does not replace them. Cross-ref [[wiki:architecture] hooks-load-from-settings-not-hooksjson] (the metrics-path trap), [[wiki:architecture] failure-recurrence-dedup].
## [fail:design] security-blacklist-narrowing-is-a-bypass | 2026-07-17 | count:1
A REVIEW finding said `permission-surface-write` (autopilot_guard's rule stopping an autonomous agent from rewriting the files that gate it) was a bare path regex, so `cat .claude/settings.json` — a READ — was blocked. I fixed the false positive by requiring a write TOKEN before the path: `(?:>>?|tee|sed -i|cp|mv|rm|truncate|dd|patch|touch)[^|>]*?<path>`. The next review graded it P0 from BOTH voters: the fix closed the false positive and **opened false negatives the old regex had caught** — `python -c "open('.claude/settings.json','w')"`, `perl -i -pe ... <path>`, `git checkout HEAD -- <path>`, `git restore <path>`, `install /dev/null <path>`, `printf | ex -sc 'wq' <path>`. Plus `echo x >| <path>`: `_SEGMENT_SPLIT` splits on `|` BEFORE the rule runs, so the noclobber-override redirect lands the token in one segment and the path in another — neither matches. Severity was P0 not P2 because the same PLAN phase wired the rule's PreToolUse path for the first time, i.e. the change that widened the hole is the change that made it reachable: an autopilot-armed agent could rewrite the file holding both `permissions` and the `hooks` that enforce them. Root-cause class: **for a security rule, a blacklist of dangerous verbs is unbounded and a fix that narrows it is a bypass**. The set of ways to write a file is open (every interpreter, every VCS restore, every coreutil); the set of ways to READ one is small and closed. security-reviewer's shape is the right one: block any segment naming the surface UNLESS it matches a narrow read-only allowlist (`cat|less|head|tail|grep|jq|git diff|git log`, path not after a `>`). Prevention: when a guard produces a false POSITIVE, do not narrow the match — invert the polarity. Narrowing trades a visible annoyance for an invisible hole, which is the wrong direction for a guard whose whole job is the invisible case. And when a rule is dormant (nothing wires it), a "cleanup" of it is not low-risk: check whether the same change makes it live. Cross-ref [[fail:design] namespace-prefix-mistaken-for-authorship] (same session, same shape: a destructive-or-protective decision inferred from text the adversary also controls).
## [fail:design] reconstructed-adr-hand-copied-oracle | 2026-07-18 | count:1
A reconstructed PLAN's ADR can carry a factual error the reviewer trusts. ADR-004 (reconstructed from memory after the original was lost) listed 9 deny literals to prune "because harness-maker shipped them all". Four had NEVER been in a settings template (`Write(~/.aws/**)`, `Edit(/etc/**)`, `Edit(~/.ssh/**)`, `Edit(~/.aws/**)`) — they reach a user's disk via /hm:health Layer 1 acceptance or by hand. Pruning by that list = silent deletion of user-authored deny rules. Caught ONLY because a test-reviewer flagged the oracle was a hand-copied list, forcing a git-history-derived oracle (`git log -S<lit> -- templates/settings/`), which then failed on the first non-shipped literal. Lesson: for any "delete from user's disk" operation, the oracle must be DERIVED from ground truth (git history / the rendered artifact), never hand-copied from the same head that wrote the code — a hand-copy stays green by being edited. And the safety line for deletion must be a property that holds without knowing the user's config: here, `is_matchable_rule(rule) is False` (dead syntax → deleting removes zero protection), because no oracle can distinguish "we shipped this string" from "the user also typed it".
## [fail:process] trust-crossmodel-over-own-probe | 2026-07-18 | count:1
Do NOT trust your OWN empirical probe over a cross-model adversarial voter on security code. During Phase 3+4 P0#2 I wrote a thorough probe (20+ write vectors, all BLOCK; 11 allow cases) and got "BAD: 0" three times — yet codex found a genuine in-scope bypass PAST each of my clean bills: my probe missed `pushd`/`CDPATH`/`--work-tree`/exec-FD, then command-substitution masking, then `less -O` (I never tested an allowlisted reader WITH a write flag). A single reviewer's clean bill (even your own, even empirical) is weak signal on an adversarial surface; the K=2 cross-model panel (security-reviewer + codex) is what actually converged the fix. Concrete practice: after your own probe passes, still run the fix through an independent adversarial voter framed DEFENSIVELY (codex refuses "attack it" framing with a cybersecurity-risk flag — reframe as "verify the guard blocks these unauthorized-write patterns; report any it fails to block"). Related: the codex second-opinion recipe is dead on the feature-branch worktree path (`.claude/schemas/…` absent) — use the base repo's absolute schema path. See [[permission-rule-matchability]], [[textual-bash-guard-cannot-be-complete]].
## [fail:render] install-ref-abs-home-path-committed | 2026-07-21 | count:1
Machine-specific absolute home path committed into rendered hooks → team flip-flop. `_compute_install_ref()` returned the `direct_url.json` `file://` path verbatim (`/home/<user>/.claude/plugins/cache/harness-maker/harness-maker/<ver>`), and that value is baked into every hook `--with` in a COMMITTED `.claude/settings.json` (plus `.cursor/hooks.json`, `.codex/hooks.json`, and all command/skill bodies). A team that commits settings.json saw the path rewritten to each developer's home on every re-render/rebase → an infinite flip-flop (surfaced in edge_testfarm_os at 0.41.1). Same root CLASS as `[fail:test] snapshot-regen-inside-worktree` (an absolute machine path leaking into rendered output via the install ref) but a DISTINCT surface — a committed, team-shared config file rather than test snapshot hashes — so it recorded separately. Fix (0.42.0): `synthesize._portablize_ref` replaces the render-machine home prefix with the literal `$HOME` at the install_ref SOURCE (all return branches), and hook-JSON `--with` was switched from single-quote / unquoted to double-quote so the shell expands `$HOME` at run time. Prevention: any value baked into a COMMITTED rendered artifact must be machine-portable — never embed an absolute home path. A render-time leak-check assert (`render._assert_portable_install_ref`) guards regressions, but it MUST cover EVERY renderer: the first implementation wired it only into the settings + merged-hooks paths and missed `_render_pure_json` (the fresh-render path for cursor/codex), so 2 of 3 IDE surfaces bypassed the guard on the common path — caught by code-reviewer in review and fixed + regression-locked.
## [fail:test] assertion-invariant-over-named-dimension | 2026-07-25 | count:4
A test whose assertions are INVARIANT over the exact dimension its name claims to cover. Caught 11 times by the Phase A.5 test-reviewer across one task (8 in the pure layer, 3 in the cache re-point), never by pytest — because every one of them PASSED. Canonical instances: (a) `assert five_m.counters == one_h.counters` to prove both cache-write TIERS are mapped — but if BOTH tiers are dropped the two dicts are still equal, so the single most likely mapping bug (TokenUsage has two tier fields, the classifier reads one differently-named key) sails through; (b) `assert counters["hit"] == 0` for a "cache-creation-only" case — true for ANY classification of a turn with no cache read, so the creation signal is untested; (c) `assert 0.0 < carry_ratio <= 1.0` — true for a hardcoded 0.5 and for an INVERTED ratio; (d) a "mixed models" test whose token values never straddle the two models' thresholds, so per-turn vs window-level threshold semantics are indistinguishable; (e) `assert far == near or far <= cap` — an or-disjunction satisfied by any gap-insensitive implementation. **Root-cause class: asserting a RELATION (symmetry, range, non-emptiness) where the contract specifies a VALUE.** The relation holds in the broken world too. **Prevention:** for every assertion, name the specific wrong implementation it is meant to reject and check the assertion actually fails against it — if you cannot name one, the assertion is decorative. Pin the discriminating value, not a bound: the boundary case (accept AT the limit, reject at the first value past it) and the inverse fixture (a window that should score 0) are what carry the information. Related: `[fail:test] fence-position-test-pins-only-upper-boundary` (same family, one-sided pin), `[fail:test] absence-as-evidence-unverified-source`.
- [2026-07-26] Recurred 3x inside one review round. Two render asserts were position-invariant: "Check 1 —" in body holds because the check definitions sit near the top of the document, so it passes even when the degraded heading points at nothing; "STOP" in body is satisfied by the pre-existing "STOP on first FAIL" header, so it passes even if the dispatch hands the gate decision to the subagent. The vault pair varied only directory-EMPTINESS (one matching note vs none), so a "vault given and any *.md present" implementation passed both — leaving the anti-fabrication check for the one promotion number CLAUDE.md relies on entirely untested. And the verify _receipt fixture defaulted to checks=() / record_path=None, which is the exact vacuous shape the finding was about, so every test built on it could not fail for its named reason. Caught by suspicion at 17 instant passes, not by a gate.
- [2026-07-27] 2026-07-27 (round-5 review): recurred inside the FIX for it. Four gates shipped this cycle each passed in the world they existed to prevent — `tools:` parsed as `str(list).split(',')` (intersection never matches, read-only check silently off), a rendered-only $N scan whose fixture leaves the guarded block unrendered, `rc != 0` where the guard returns 2, and my own vault regression test which asserted 'not accused of fabricating' — a property the OTHER half of the same fix already satisfied, so removing the half under test killed nothing. Mutation-check every new gate: delete the code, name the test that dies.
- [2026-07-27] 2026-07-27 (token-economy Phase 1): recurred AGAIN, inside a plan whose ADR-010 exists to prevent it. A gate written to pin the ORDER of the classification branches — sub-threshold must be tested before the unknown-model branch — carried cache-write tokens on BOTH fixture turns, and the branch it meant to discriminate only fires when the turn created no cache. The branch could not execute in either variant, so the assertion held for every possible ordering, including the inverted one it existed to reject.
## [fail:tooling] memory-cli-slug-cap-blocks-legacy-count | 2026-07-25 | count:1
`memory_md upsert-failure` validates `--slug` as kebab-case **≤40 chars**, but 45 of the 119 entries already in `failures.md` have slugs LONGER than 40 (up to 65: `task-land-squash-commits-whole-index-sweeps-concurrent-base-churn`). Those 38% can therefore never receive a `count++` through the CLI — the exact-slug-match increment the recurrence detector depends on is unreachable for them, so their counts are frozen at whatever they were, and the `count>=3` escalation into `pending-proposals.md` can never fire for any of them. Discovered 2026-07-25 during wrapup: two recurrences of THIS unit mapped confidently onto `cwd-inherited-from-worktree-into-main-commands` (46) and `codex-exec-recipe-compound-command-defeats-allow-rule` (53), and both were rejected — forcing new near-duplicate entries and *lowering* the very counts the search-before-write step exists to raise. Root-cause class: **a validator added to the writer after the data was written, with no migration and no read-side reconciliation** — the writer and the corpus disagree about what a valid key is, and the writer silently wins by refusing. Note the direction of the harm: the rule was presumably added to keep slugs terse, but its effect is to fragment the dedup namespace it was protecting. Fix options: raise the cap to the observed max, or normalise/alias long legacy slugs on read, or have `upsert-failure` accept an existing slug verbatim when it already appears in the file (validate only NEW slugs). Prevention: any validator tightened on a writer must be checked against the existing corpus first — `awk 'length>N'` over the current keys is a two-line pre-flight.
## [fail:render] recipe-relative-path-breaks-in-worktree | 2026-07-25 | count:1
The rendered `/hm:review` and `/hm:plan` second-opinion recipes pass `--output-schema .claude/schemas/second-opinion-finding.schema.json` as a **relative** path. Under `worktree.feature_branch_workflow` (the Production default) every `/hm:` stage runs inside `.worktrees/<slug>/`, which has no `.claude/schemas/` — only the base repo does. Result: `codex exec` exits 1 with `Failed to read output schema file ... (os error 2)` and the graceful-degrade path records `status: skipped`, so the codex vote silently vanishes on what is the harness's NORMAL operating path for Production. Verified 2026-07-25: identical invocation with an absolute path succeeded immediately and returned 9 findings, three of which were real P1s that no Claude reviewer had raised. Root-cause class: **a recipe with a cwd-relative asset reference, authored against the base repo, executed from the worktree.** The degrade is invisible because "skipped" is also the legitimate outcome for a missing CLI or an expired login — a graceful-degrade path that cannot distinguish "not installed" from "we broke our own invocation" will hide the second forever. Fix: render the schema path absolute (or resolve it against the base repo root, which the stage already computes for `task-land`). Prevention: any rendered command that references a repo asset by path must be checked from inside a worktree, not just from the base. Direct parent of `[fail:runtime] cwd-inherited-from-worktree-into-main-commands` (its slug exceeds the 40-char CLI cap, so this could not be recorded as that entry's count++ — see `[fail:tooling] memory-cli-slug-cap-blocks-legacy-count`).
## [fail:tooling] agy-print-flag-swallows-next-flag | 2026-07-25 | count:1
`agy --print` takes the prompt as its VALUE, not as a boolean flag. The rendered antigravity second-opinion recipe is `agy --print --sandbox --print-timeout 240s --model "X" < prompt_file`, so `--print` consumes the literal string `--sandbox` as the prompt and stdin is never read. Verified 2026-07-25: the first run returned *"It looks like you've only provided the text `--sandbox`. How can I help you with that?"*, exit 0. Probe `agy --sandbox --print "Reply with exactly: PONG"` → `PONG`; re-running the real review in that form returned 7 substantive findings including a critical. **Every antigravity vote this harness has ever cast was therefore vacuous** — and because the fail-closed adapter only sees "output is not JSON", it records `status: failed`, which reads as a model/parse problem rather than a broken invocation. Two compounding traps: (a) exit 0 + a fluent-looking response is the worst possible failure signature — nothing looks broken; (b) CLAUDE.md **requires** the command to begin with `agy --print --sandbox` so the scoped `Bash(agy --print --sandbox:*)` allow-rule prefix-matches it, so the correct flag order breaks the permission rule — the fix must change the recipe AND the allow rule together, and the `--sandbox` flag is meanwhile being consumed as a value rather than applied. Root-cause class: **an external CLI's flag arity assumed rather than read, with the command shape frozen by a permission-prefix constraint.** Prevention: for any shelled-out CLI, run a one-line positive smoke (`... "Reply with exactly: PONG"`) before trusting its output shape; and when an allow-rule pins a command prefix, that prefix is a contract with the tool's argv grammar, not just a string. Sibling of `[fail:render] codex-exec-recipe-compound-command-defeats-allow-rule` (slug >40 chars, so not recordable as its count++).
## [fail:test] test-pins-retired-implementation-name | 2026-07-25 | count:3
A test whose docstring states a real invariant but whose assertion pins the *implementation name* that happened to express it, so a refactor preserving the invariant turns the test RED and the test is the thing that is wrong. Instance: `test_review_codex_present_regardless_of_runner` documented "same second-opinion wiring whether run standalone or via loop" and asserted `"codex_adapter" in <rendered review stage>`. When adaptation moved out of the rendered prose into `second_opinion_invoke` (which imports `codex_adapter`), the wiring was fully intact for both runners — verified by rendering both and counting `second_opinion_invoke` ×3 plus a per-model `--model` line — but the string was gone, so the suite went red on a change that broke nothing. Two more in the same task: `test_partial_sandbox_disabled_call_is_a_bare_invoker_command` pinned `codex exec`, and a producer-gate test matched the retired shape anywhere in the file so it fired on the *prose explaining why the shape was retired*. **Root-cause class: the assertion names a symbol/string that is one contingent expression of the contract, not the contract.** This is the mirror image of `[fail:test] assertion-invariant-over-named-dimension` — that family is false GREEN (assertion too weak to fail in the broken world); this one is false RED (assertion too specific to survive a correct change). **Prevention:** before asserting on a literal, ask "if someone rewrote this correctly, would this string still be here?" If no, assert on the observable the docstring names — here, the per-model invoke line, which is also strictly stricter than a module-name grep. When a test fails, decide *first* whether the invariant or the expression broke: render/inspect the artifact and confirm the contract still holds before touching either side. Related: PLAN Phase 3 enumerated five artifacts pinning the old shape and the real count was nine — hand-enumerating a blast radius does not replace running the suite.
- [2026-07-25] Reproduced within hours of being recorded: the F6 test I wrote asserted the literal prose "not yet the operative grant"; retiring the blanket Bash(uv:*) made that sentence correctly false and the test went red. Retargeted to a cross-artifact invariant (the rule the prose cites must be present in the rendered settings allow list), which survives rewording.
- [2026-07-26] Third time, and this time inside a test written to guard this exact family: test_dry_run_says_would_drop_not_dropped asserted `"will now ask before running" not in err` AFTER I had reworded the notice to "require approval", so the pinned string existed nowhere in the tree and the negative assertion could never fail. Re-anchored on the word "dropped", which the sibling applied-branch assertion proves is really emitted.
## [fail:design] verification-cache-key-nondeterministic | 2026-07-25 | count:2
`observability.verification_cache`'s skip key is non-deterministic for every harness-rendered invocation, so the marker written by `/hm:verify` can never be fresh at `/hm:wrapup` and both stages always run the full suite (~7 min each here). Symptom: five consecutive `verification_cache key --root . --mode relevant` calls with zero working-tree changes returned five different hashes. Cause: `compute_relevant_skip_key` appends `_env_hash()`, and the rendered commands all run as `uv run --with $HOME/.claude/plugins/cache/harness-maker/<ver> python -m harness_maker...`, which makes uv create a fresh ephemeral build env per invocation at `~/.cache/uv/builds-v0/.tmpXXXXXX`. That random path lands in both `PATH` and `VIRTUAL_ENV`, neither of which matches `_ENV_IGNORE_PATTERNS` (`SSH_* / WSL_* / WT_* / CLAUDE_CODE_*`), so it feeds the hash. Proven by diffing the kept-env dump across two `--with` invocations (only `PATH` and `VIRTUAL_ENV` differ) versus two plain `uv run python` invocations (identical) — and by confirming in-process that every *other* component (root, HEAD, relevant-path diff blob, uv.lock, pyproject, tool versions, `_env_hash` itself) is stable. **Root-cause class: a cache key that fingerprints the ambient environment will be invalidated by the launcher, and the failure is silent — a permanently-cold cache looks exactly like a correctly-invalidated one.** Candidate fixes: normalise `PATH`/`VIRTUAL_ENV` out of the env hash (or restrict the hash to an explicit allowlist of build-affecting vars), which is the same conservative-default trap as `is_relevant_path`'s catch-all `return True`. Not fixed in PLAN-second-opinion-invocation-and-slug-cap — discovered during its wrapup and recorded here. Detection idea: a test asserting the key is stable across two subprocess invocations.
- [2026-07-27] Second instance, same root cause, a different variable and a wider blast radius. The 2026-07-25 fix scrubbed volatile VALUES out of `PATH`/`VIRTUAL_ENV`, which stopped the churn *within* one process — and left the ambient-environment fingerprint itself in place, which is the actual root-cause class this entry already named ("a cache key that fingerprints the ambient environment will be invalidated by the launcher"). It also declined the remedy this entry listed second: "restrict the hash to an explicit allowlist of build-affecting vars". So the cache stayed 100% cold for another two days and nobody noticed, because a permanently-cold cache is indistinguishable from a correctly-invalidated one — the same reason instance 1 survived. Measured this time: the main loop's Bash hashed 43 variables, a subagent's hashed 42, and the single difference was `CLAUDE_EFFORT`. `/hm:verify` writes the marker from one context and `/hm:wrapup` reads it from another, so the marker was structurally unreadable, not merely stale. The same policy also hashed `DISCORD_BOT_TOKEN`, `JENKINS_TOKEN`, `PIPELINE_SLACK_WEBHOOK_URL`, `ZEPHYR_BASE`, `NVM_DIR` — rotating a Slack webhook invalidated the Python test cache. **Fix (this round): flipped to an allowlist** (`_ENV_ALLOW` / `_ENV_ALLOW_PATTERNS` / `_ENV_ALLOW_EXCEPTIONS` in `observability/verification_cache.py`), enumerated from a grep of every `getenv`/`environ.get` in `tests/` and `src/`; the value-scrubber is RETAINED because `PATH`/`VIRTUAL_ENV` are allowlisted and still carry uv's throwaway build dir. **The trade-off is real and now documented rather than implied**: the old policy could not produce a stale PASS from an unknown variable, the new one can if a build-affecting variable is never enumerated — but the old guarantee was worth nothing, because the cache never returned a single hit in its lifetime. **Prevention, generalised past this module:** when instance 1 of a failure entry lists two candidate remedies and you take the cheaper one, the entry does NOT get closed — the root-cause sentence is still true and the second instance is already scheduled. Re-read the *class* line, not the symptom line, before declaring a fix complete. **Detection that was named a round early and never written:** this entry's own "Detection idea: a test asserting the key is stable across two subprocess invocations" — it now exists (`test_the_key_is_identical_across_two_subprocess_invocations`), spawning real subprocesses with a main-loop env and a subagent env, because no in-process monkeypatch models an env var that is simply absent in a child context.
## [fail:lint] gate-exit-code-lost-through-pipe | 2026-07-25 | count:2
A quality gate reported green while one of its checks was failing, because the check was piped through `tail` and the pipeline's `$?` is the *last* command's. Instance: a wrapup-style gate block ran `uv run ruff format --check src/ tests/ 2>&1 | tail -1` alongside three sibling checks that each captured their own `rc`. `ruff format --check` exited non-zero (`1 file would be reformatted` — a test file this task had just added), `tail` exited 0, the aggregate summary showed no failure, and the harness's own completion notification also said exit 0. The violation was only caught by reading the human-readable line, not by any exit code. **Root-cause class: a verification step whose exit code is consumed by a formatting/truncation stage is not a gate — it is decoration.** This is the same shape as the bugs the task itself was fixing (a mechanism reporting success while doing nothing), reproduced in the script written to verify the fix. **Prevention:** never pipe a gate command anywhere. Redirect instead (`cmd > file 2>&1; echo "rc=$?" >> file`) and assert on the recorded `rc` per check, one `rc` per gate — an aggregate `rc` from a compound block hides which member failed. CLAUDE.md's wrapup gate list (`pytest` / `ruff check` / `ruff format --check` / `mypy --strict`) is exactly where this matters, and `ruff format --check` is the member most often dropped (`[fail:lint] ruff-format-not-in-local-verify-pass`). Corollary observed the same session: this environment's background-task completion notifications reported exit 0 for a run whose own recorded `rc` was 1, four separate times — so a reported exit code is not evidence here either; read the output.
- [2026-07-26] Same root cause, different disposal channel: `uv run ruff check --fix src/ tests/ >/dev/null 2>&1` sent the rc to /dev/null instead of through a pipe, so a B023 (function defined in a loop closing over the loop variable) survived three consecutive "clean" gate reports and surfaced only in the final full run. Redirecting a gate to /dev/null is the same defect as piping it to tail: the verdict never reaches a decision. Prevention line already in this entry covers it — the rule is per-check `rc` capture, regardless of how the output is disposed of.
## [fail:test] advisory-check-fails-unseen | 2026-07-25 | count:1
A CI step marked `continue-on-error: true` failed for 10 days while the workflow reported success, because nothing surfaces an advisory failure except a GitHub annotation nobody opens. Instance: `install-cmd-regression`'s advisory codex step. On 2026-07-15 the expectation was re-truthified to `codex plugin add` SUCCEEDING (codex >= 0.144.x clones the repo), but the CI pin stayed at `@openai/codex@0.133.0` from 2026-05-23, where that command still errors `not found in marketplace`. Every run since printed `1 failed` inside a job rendered with a green check. It was found only by opening the annotation on an unrelated push. **Root-cause class: a check made deliberately non-gating still needs a gating check on its own preconditions.** The advisory decision (ADR-002 — external CLI behaviour must not block CI) was correct and is unchanged; what was never checked at all was *internal consistency* — that the version CI installs matches the version the expectation was verified against, two values this repo fully owns. Fix: `_install_helpers.CODEX_CLI_PINNED_VERSION` + `test_ci_codex_pin_matches_the_verified_version`, which is BLOCKING and needs neither the external CLI nor an opt-in env var. Also made the advisory assertion print running-vs-expected version, so a future annotation is readable at a glance. **Prevention:** for every `continue-on-error` / advisory / warn-and-proceed check, ask what a human would see if it failed *today*, and if the answer is "an annotation", split out the part that is deterministic and repo-owned and make that part blocking. Same family as `[fail:lint] gate-exit-code-lost-through-pipe` (a check whose result never reaches a decision) but a distinct mechanism: that one loses the status accidentally through a pipe, this one discards it by policy and provides no substitute signal.
## [fail:process] settled-dispute-with-off-target-evidence | 2026-07-26 | count:1
Two reviewers disagreed on whether `/hm:make --dry-run` reaches the permission merge. I "settled" it by running `render(dry_run=True)` directly, watching the notice print, and writing a code comment asserting the reviewer who called the path unreachable was wrong. The experiment was real, positive, and reproducible — and it answered a DIFFERENT question than the one in dispute. Nobody doubted the API path prints; the dispute was whether the CLI reaches it. It does not: `cli.py`'s `--dry-run` branch raises `typer.Exit(0)` before the only `render()` call site, so no shipped command can reach the merge in preview mode. A later reviewer traced `cli.py` and refuted both my comment and the CHANGELOG entry I had written from it ("Fixed — --dry-run no longer claims it deleted permission rules" documented a bug no user could hit). **Root-cause class: an experiment that exercises a DIFFERENT entry point than the claim is not evidence for the claim, and it is more dangerous than no evidence because it feels conclusive — I upgraded a guess to an assertion in a code comment on its strength.** Note this is not the absence-as-evidence trap ([[fail:test] absence-as-evidence-unverified-source]); the signal here was positive and correct, just off-target. **Prevention: when the disputed question is REACHABILITY ("does anything actually call this?"), the only valid evidence is tracing the caller chain from the real entry point — grep the call sites and read the early-return branches. Calling the function yourself proves the function works, never that anyone calls it. Before recording a dispute resolution in a comment, restate the claim and check the evidence names the same entry point the claim does.** The parameter was kept as deliberate hardening and the comment now says so; the CHANGELOG entry was re-filed as internal, not a user-visible fix.
## [fail:design] template-fix-misses-existing-users | 2026-07-26 | count:1
A security fix applied to a settings TEMPLATE reaches new installs only, because `render._merge_permissions` unions `permissions.allow` with what is already on the user's disk and the old rule is preserved as pseudo-user content. This happened TWICE in the same feature, the second time inside the change written to fix the first. (1) 0.43.0 retired the blanket `Bash(uv:*)` from both templates — correct — but `allow` had no prune, so every existing project kept the arbitrary-command grant forever and the release notes needed a post-hoc correction. (2) The follow-up added the prune and, while at it, tightened the scoped rule from `python -m harness_maker*` (no word boundary — also matched `python -m harness_maker_evil`, satisfiable by dropping a file in cwd) to `harness_maker.*`. That tightening ALSO reached new installs only: the on-disk rule text embeds the user's resolved `harness_maker_src_path`, so no exact literal in the prune set could ever name it, the union kept it, and an upgraded harness carried BOTH rules — with the stale one still pre-approving `harness_maker_evil` while a fresh install denied it. Reproduced: `old rule survived: True / evil allowed on upgrade: True / allowed on fresh install: False`. **Root-cause class: when a config file is produced by a MERGE rather than a rewrite, editing the producer is not a fix — it is only half of one, and the missing half is invisible because the template diff looks complete and every render test (which renders from scratch) stays green.** The upgrade path has no fixture unless you build one. Fix: `_HARNESS_SHIPPED_ALLOW_PATTERNS`, an anchored-both-ends regex prune for retired rules whose text contains an interpolated path, plus a regression test that seeds the stale rule on disk, re-renders, and asserts both that it is gone and that the attack it enabled is now denied. **Prevention: for any change to a merged-config template, write the test as `seed old on disk -> re-render -> assert`, never as `render fresh -> assert`. If the fix cannot reach an existing user, say so in the CHANGELOG in the same commit rather than describing it as closed.** Cross-ref [[fail:design] reconstructed-adr-hand-copied-oracle] (same prune, oracle discipline) and [[wiki:architecture] allow-literal-prune].
## [fail:test] shipped-entry-point-not-exercised | 2026-07-26 | count:2
Four times in ONE task, a change was correct in the library and wrong at the invocation the product actually runs — and every unit test was green each time. (1) `task-preflight <slug> --stage hm:plan "$(pwd)"`: the shipped parser takes "every arg not starting with `--`" as positional, so the flag placed before the base made `base_dir = Path("hm:plan")` and the stage created its worktree under a directory literally named `hm:plan`. (2) `economics._collect` built `aggregate(...)` without `spans` or `inferred`: Phase 1 wrote the join, Phase 2 wrote the emitter, and no phase wired the reader — so `economics report`, the only thing a user runs, described a world where neither the ledger nor the classifier existed. (3) `run_classify.main` never `.resolve()`d `--root`, and the rendered command is `--root .`: `Path(".")` encodes to the project-dir name `"-"`, which matches nothing, so `boundaries` returned 0 while `economics report` saw 392 on the same corpus — every unit test passed an absolute `str(tmp_path)`, which is exactly why none of them saw it. (4) `reconcile` gained a `worktree_root` parameter with two passing unit tests and NO caller passed it, so the reconciler kept resolving at base — byte-identical to the behaviour the parameter was added to fix; the security reviewer's phrasing was "closed in the library and open in the product". **Root-cause class: the test exercises the Python API, the user runs an argv / a rendered line / a call site — and nothing pins the seam between them.** A render-grep cannot observe a parse; a unit test that constructs arguments in Python never types the flag; a new parameter is invisible to every test that does not go through the caller. **Prevention: when you add a CLI flag, a parameter, or a call-site argument, add one test that drives the LITERAL shipped form** — `main(["boundaries", "--root", "."])` after a `chdir`, not `str(tmp_path)`; `worktree.main([...])` asserting the resulting worktree LOCATION, not that the function returned 0; a render-grep asserting the flag is present on the emitted command line. Ask "what string does the user's shell actually receive, and does any test contain that string?" Related: `[fail:test] assertion-invariant-over-named-dimension` (the weaker sibling — there an assertion exists but cannot fail; here no assertion exists at all).
- [2026-07-26] 2026-07-26: the two delegation CLIs (`wrapup_brief`, `wrapup_receipt`) had 68 tests between them and neither called `main()` — the `--worktree` hand-off the rendered command performs was uncovered; closed by `tests/unit/test_delegation_cli_boundary.py`.
## [fail:test] bidirectional-mapper-write-half | 2026-07-26 | count:1
CLAUDE.md checkpoint 6 says every persisted format needs a reverse mapper, and the round-trip test I wrote proved only the READ half: it hand-wrote `delegation: {stages: [wrapup]}` into a temp file and asserted `answers_from_harness_yaml` returned it. That says nothing about whether the key is ever WRITTEN. `/harness-maker:make --update` re-renders `harness.yaml` FROM the Jinja template, so a key absent from `templates/harness-yaml/{Side,Production}.yaml.j2` — or a missing `HarnessConfig` field, or a `synthesize` that does not copy `answers.<key>` — silently reverts the user's setting on the very next update, with the reader-side test green throughout. Two live instances found by following one review finding: (a) `delegation.stages`, which ADR-011 designates as the ROLLBACK switch for a medium-likelihood/high-impact quality risk — so the escape hatch would have disarmed itself; (b) `economics.span_max_turns` / `span_max_min`, which had ALREADY shipped that way one phase earlier — the config model had them, the reverse mapper read them, and the `economics:` block in both templates listed only the six pre-existing keys, so a user who tuned a cap had it reset on every re-render. **Root-cause class: a "bidirectional mapper" test that only goes one direction reads as full coverage.** The write half has three independent failure points (model field, synthesize copy, template emission) and the read half touches none of them. **Prevention: the round-trip test must close the loop through the ARTIFACT** — render a real `harness.yaml` for BOTH presets, assert the key is in the rendered file, then feed that rendered file back through `answers_from_harness_yaml` and assert it survives unchanged. A test that writes the input by hand is testing the parser, not the round trip. Related: `[fail:test] shipped-entry-point-not-exercised` (same family: the test covers the library, not the artifact the user reads).
## [fail:test] live-e2e-timeout-concurrent-sessions | 2026-07-26 | count:1
The full regression came back `rc=1` with exactly one failure: `tests/e2e/test_plugin_live.py::test_make_fresh_install_file_count`, and the traceback was `subprocess.TimeoutExpired` with `<Popen: returncode: -9 args: ['claude', '-p', '/harness-maker:make --ci ...']>` — the live test spawns a REAL `claude` process on a fixed 180s timeout and it was SIGKILLed. Not an assertion about harness contents. Three signals separated "flake" from "my diff broke render": (a) the same file run in isolation was 27/27 green; (b) the other three live-make tests passed IN THE SAME failing run, and all four go through `/harness-maker:make`, so a broken template would have failed all of them; (c) a character-level diff of the pytest progress line against two earlier green runs showed the identical prefix with `...` where this run had `F..` — i.e. exactly one newly-failing test at a known index, which is how the test was identified without a summary line. **The contention was self-inflicted:** the full suite was already running when, to diagnose, I launched `pytest tests/e2e/test_plugin_live.py tests/e2e/test_dogfood_sandbox.py` — both of which spawn their own `claude` processes. Five interactive `claude` sessions were also live on the machine, all competing for the same subscription. **Root-cause class: a live-LLM e2e test with a fixed wall-clock timeout is a shared-resource test, and its failure mode is indistinguishable from a real regression until you look at the exception type.** **Prevention: (i) read the exception before the verdict — `TimeoutExpired` on a subprocess that shells out to a model is a capacity signal, an `AssertionError` is a defect signal; (ii) never launch a second test run while a suite is in flight (this is the second methodology error of this shape in one session — the first was editing source files while a regression ran, which broke an `inspect.getsource()` line-number test); (iii) re-run cleanly with nothing competing and confirm from the `rc=` line before drawing any conclusion. Do not resolve it as "flaky, moving on" — the clean re-run is the evidence, not the assumption.
## [fail:design] written-under-one-key-read-under-another | 2026-07-26 | count:1
A value is persisted under one key and looked up under a different one, so the write succeeds, the read finds nothing, and **neither side reports an error**. The feature is inert and every test that checks "the row was written" is green.

Live instance: `delivery_metrics adjudicate --commit` stored whatever string the caller typed, but `classify_cfr` looks the verdict up with the **full 40-char sha** it read out of `git log`. The abbreviated sha that `git log --oneline` hands you — the form a human naturally copies — produced a ledger row no candidate would ever match. `compute` kept exiting 3 on the candidate that had just been adjudicated, and its output could not distinguish that from "not adjudicated yet". Two dead rows sat in the ledger for a full session before the mismatch was noticed by hand.

**Why the existing tests could not see it:** every test passed `cand["commit_sha"]` straight from the `candidates` output, so the two halves of the key were the same string *by construction*. The defect only exists when they differ, and nothing in the suite ever made them differ.

**Prevention — two rules, both needed.** (1) A key that two components share must be **normalised at the single place both halves meet**, not trusted from the caller; where the value cannot be normalised, reject it loudly instead of storing it. (2) The regression test must assert the **behavioural** half — that the same run then reaches the consumer and the consumer's verdict changes. Asserting "the row exists with the value I passed" passes in the broken world too, which is the `[fail:test] assertion-invariant-over-named-dimension` shape.

**Generalise the fix past the instance.** Normalising only the sha would have left a mistyped `--release` landing in exactly the same dead end; the pair is now validated against the pending-candidate set, with `--force` as the documented escape. Fixing the half that bit you and stopping is how the same defect survives its own fix.

Related: `[fail:test] shipped-entry-point-not-exercised` (same family — a green boundary above a wrong wiring).
## [fail:design] hardening-with-no-measured-effect | 2026-07-27 | count:1
A security or robustness change lands because the reasoning is sound, and nobody runs the case it claims to close. Round 5 produced one in each direction, from the same review.

**Adopted after measurement contradicted the stated mechanism.** `--end-of-options` was added to `git rev-parse --verify --quiet <ref>^{commit}` to keep a leading-dash `--commit` out of git's option parser. Measured: `--glob=refs/*^{commit}` exits 1 **with and without** the anchor — the appended `^{commit}` suffix already makes every option form unresolvable. The anchor changes no outcome today. It was kept, with the comment rewritten to say what it actually buys (the suffix neutralises today's option set by accident; the anchor neutralises tomorrow's by contract) and to record the cost it introduces (a first-in-codebase git >= 2.24 floor).

**Nearly shipped as a live P1 on three concurring voices.** See [[wiki:review reproduction-outranks-consensus-count]].

**The rule:** state, in the comment, what the hardening was measured to change. "Sound reasoning" and "measured effect" are different claims, and a comment that asserts the first while implying the second makes the next reader unable to tell whether the guard can be removed. If the measured effect is nothing, that is not automatically a reason to revert — but it IS the thing the comment must say, along with any new floor or dependency the change costs.
## [fail:test] fix-introduced-defect-passes-all-gates | 2026-07-27 | count:2 | previous_count:1
Three review rounds over one Phase-1 diff produced 22 findings, and **11 of them were defects introduced by the previous round's own fixes** — each of those repairs had been verified green by all four gates (`ruff check`, `ruff format --check`, `mypy --strict src`, full `pytest`) before the next round found the new hole. The worst was a `ValueError` crash that shipped past all four because no fixture ever reached the window where the `actionable` list is empty; lint cannot see it, mypy cannot see it, and the suite only sees what a fixture reaches. **Root-cause class: on a surface being actively churned, "the gates are green after the fix" is evidence about the gates' COVERAGE, not about the fix.** A repair characteristically moves the code into states the existing fixtures were never built to reach, so the post-fix green run is measured on precisely the wrong sample. The compounding form is worse: ADR-019 of PLAN-token-economy-step-pruning records rounds 1-2 growing an unknown-model apparatus (a per-turn counter tracked independently of classification, an incompleteness caveat appended to two strings, a dedicated no-primary branch, a remediation with no completion) where each round's patch generated new defects faster than it closed them, and round 3 found the structure defective in three independent, interacting ways at once. The resolution was to **delete the apparatus**, not to patch the three findings separately. Prevention: (a) after a repair, name the input window the repair newly makes reachable — an empty collection, a `None` threshold, a no-primary branch — and assert a fixture enters it, in the same commit; (b) when a third consecutive round finds defects in the same mechanism, treat the mechanism's EXISTENCE as the finding rather than its current bug; (c) a green four-gate run is a precondition for review, never a substitute for one — the finding rate here was 22 findings against 0 gate failures. Related: `[fail:test] assertion-invariant-over-named-dimension` (why the individual gates could not fail), `[fail:test] shipped-entry-point-not-exercised` (green in the library, wrong at the seam).
- [2026-07-27] **Phase 2 of the same PLAN, sharper ratio: review round 2 raised 7 findings and all 7 were defects introduced by round 1's own fixes** (7/7, against Phase 1's 11/22). Between the rounds all four gates were green AND the ADR-010 mutation check was 7 mutants / 7 killed / 0 survivors — so every mechanical signal available said the round-1 fixes were correct, including the one specifically built to detect under-tested repairs. What the mutation check cannot see is where four of the seven actually lived: **prose** (a SPEC note's numeric justification, a PLAN frontmatter status, an ADR titled "Four independent commits" that still enumerated exactly four after a fifth phase was added). Two more were intra-AC drift — the fix updated AC-010's `executable_predicate` but left the `expected_relation` field beside it still reading the float equality the validator had already rejected. Prevention addendum for this shape: (d) after editing any field of an AC, re-read the WHOLE AC block (predicate + expected_relation + note) rather than the field you touched — they restate the same contract in three places and only one is executable; (e) renumbering or adding a phase obliges a sweep of every section that ENUMERATES phases (ADR titles, frontmatter, checkbox markers), because those counts are assertions that no gate re-derives.
## [fail:design] observability-field-with-no-consumer | 2026-07-27 | count:1
A diagnostic field is written, aggregated and asserted — and nothing ever surfaces it to a user. Two live instances in one module, found in the same review round. (a) `_incompleteness_caveat()` appended an unknown-model caveat to `CacheDiagnosis.evidence` and `.remediation`, but the last reader — `improvement._extract_layer3_actions` — returns `[]` when `primary_failure is None`, which is exactly the branch the caveat was built for, so the text existed only in memory and reached no human. (b) `CacheDiagnosis.ttl_regression` / `ttl_regression_detail` are computed by `_detect_ttl_regression` and read by **no module outside `cache_diagnostics.py` at all** — verified by grepping the whole of `src/` for the field name. What made (a) invisible is the gate's placement: it asserted on `diag.remediation`, **one layer inside the last reader**, so it could observe the string being produced and structurally could not observe that the only consumer discards it. **Root-cause class: the assertion sits at the producer's output instead of at the last surface a user actually reads**, which makes "the field is populated" and "the field is delivered" indistinguishable. Prevention: for any new observability field, name the consumer and the exact call that renders it to a human, and put the assertion THERE — `improvement.build_plan(...).actions` for a diagnosis field, `report.model_dump(mode="json")` (the form the CLI emits) for a report field, the rendered dashboard text for a dashboard field. If you cannot name that call, the field is dead on arrival and should not ship; delete it rather than leaving a plausible-looking signal that nobody reads. Note the asymmetry with a plain unused variable: an unused observability field is invisible to lint, mypy and the suite, because it is populated correctly and consumed by nothing. Related: `[fail:test] shipped-entry-point-not-exercised` (writer shipped, reader never wired), `[fail:design] green-module-dead-prose-wiring` (module green, real flow inert).
## [fail:design] remediation-instructs-refused-action | 2026-07-27 | count:1
A user-facing remediation string instructed an action that the same module deliberately refuses to perform. `cache_diagnostics`' `miss_unknown_model` remediation told the user to "upgrade harness-maker … report the id above so it can be added" — while roughly 250 lines earlier, `_MIN_CACHEABLE_PREFIX` had `opus-4-5` / `sonnet-4-5` **removed on purpose**, because no release-specific cache minimum is published for either and inventing one is the circular oracle the SPEC forbids. The table will not accept the id the remediation asks the user to send. Every user of a currently-shipping model in that set was handed an errand with no completion path, and the defect compounded: because the healthy-shortcut guard read a per-turn unknown counter rather than the classification, `hit_rate >= 80` became **permanently unreachable** for exactly those users — an incomplete diagnosis on every run, with no achievable remedy. **Root-cause class: the remediation prose is written by whoever is fixing the classifier, while the policy it presumes lives in a data table that the same change just narrowed; the two halves are never diffed against each other, and no gate compares an instruction to the code's willingness to satisfy it.** Prevention: for every remediation string, name the concrete action that closes it and check the module will actually accept that action (does the table take new rows? does the flag exist? does the config key serialize?). When no action exists on the user's side, **say so plainly** — "no action is available here; whether a minimum exists to record is a property of the model's published documentation" — instead of manufacturing a remedy to make the output feel actionable. A remediation whose completion the code refuses is worse than silence: it converts a known, honest limitation into a permanent user-facing defect and trains the user to ignore the whole class of message. Residual on this instance (2026-07-27): `improvement.py:180-184` still carries a comment asserting "the only honest remediation is 'upgrade, or report the id'" — the stale half of the fix, contradicting the corrected string it feeds from.
## [fail:test] worktree-score-omits-untracked-assets | 2026-07-27 | count:1
`/hm:verify` Check 3 compares a structural/readiness score before and after the change and FAILS on a drop past −5. Recomputed inside the task worktree the score was **47 against the base's 58 (−11, a confident FAIL)**, while the true base-to-base movement for the same change was **55 → 58 (+3, an improvement)**. Nothing in the diff caused the −11: `harness_maker.readiness` scores guardrail and workflow signals by reading `.claude/commands/`, `.claude/skills/`, `.claude/hooks/` and `.claude/settings.json`, and in this repo those are harness render output left **UNTRACKED** (and partly gitignored). `git worktree add` materialises the TRACKED tree only, so those paths do not exist in a worktree at all and six signals fail for **absence rather than for defect**. Root-cause class: **a worktree is a checkout of the tracked tree, so any metric that reads untracked or gitignored assets silently measures a different, smaller project inside it** — and it errs in the alarming direction, manufacturing a red on a change that improved the score. The reason this is hard to catch is that a score is a bare number with no provenance: once written into a report, a worktree 47 and a base 47 are indistinguishable, and the threshold comparison happily subtracts two numbers produced in different trees. Prevention: (a) compute BOTH endpoints of any before/after score in the SAME tree and say which tree in the receipt — for anything reading `.claude/`, base-to-base is the only valid comparison; (b) before trusting a score drop, check whether the fallen signals' input files are even visible to a worktree — `git ls-files --error-unmatch .claude/settings.json` exits non-zero for exactly the files that vanish; (c) treat any metric that reads `.claude/` as base-only and make the stage that runs it `cd` to the base explicitly. Related: `[fail:test] snapshot-regen-inside-worktree` (count:12 — same "measured in the worktree, wrong in main" family, different mechanism: worktree path embedded in output rather than files missing from it), `[fail:render] recipe-relative-path-breaks-in-worktree` (same family on the asset-reference side).
## [fail:design] unverified-number-in-spec-justification | 2026-07-27 | count:1
Twice in one review round I wrote a NUMBER into a SPEC justification note without evaluating it, and both survived every gate because **no gate reads a note**. (1) While fixing a finding that said "this note over-claims", I justified replacing the absolute tolerance `< 1e-9` with the relative `<= 1e-9 * abs(total)` by writing that it "strengthens the contract rather than relaxing it". At the live window's magnitude ($8,960 of unattributed spend) the relative form is **8,960× LOOSER**. The fix reproduced the exact defect class it was repairing — an over-claim, replaced with a new over-claim, in the same edit. (2) The same note asserted the old bound left "roughly 1e7 ulps of slack"; `ulp(0.0175) = 3.47e-18`, so the true figure is **2.9e8** — off by ~29×, and the number had no source at all: it was invented to make the paragraph read as quantitative. **Root-cause class: a justification note is prose to every tool in the pipeline.** `ruff`, `mypy`, `pytest` and the ADR-010 mutation check all read the predicate and none of them read the sentence beside it, so a numeric claim in a note carries the visual authority of a spec while having strictly LESS verification than a code comment (a comment at least sits next to the thing it describes). The tolerance case is the sharp one: "tighter" and "looser" are **scale-dependent**, so a change that is tighter at fixture scale (1.75e-11) and looser at production scale is naturally described by whichever end the author happened to be looking at — and the author is looking at the fixture, because that is what the test runs. Prevention: (a) any number written into a note gets computed in the SAME turn, and the expression that produced it is pasted into the note (`ulp(0.0175)`, `1e-9*8960`) so the next reader can re-run it rather than trust it; (b) never write a bare comparative ("tighter", "stronger", "looser", "negligible") about a scale-dependent bound without naming the two scales and giving the value at each; (c) when the finding you are fixing is "this claim over-reaches", the replacement is at MAXIMUM risk of the identical defect — re-read it as if it were the original finding before moving on. Related: `[fail:test] fix-introduced-defect-passes-all-gates` (this was two of that entry's seven round-2 findings), `[fail:test] absence-as-evidence-unverified-source` (citing an unverified source, same family).
<!-- @hm:/user:entries -->
