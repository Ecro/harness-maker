# Session Log — 2026-07-03

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:cfr-churn-metrics] Opt-in git-only delivery metrics (CFR + churn), reflection-not-gate | 22:40 UTC | stage:wrapup
Full research→wrapup pipeline for a new opt-in `delivery_metrics` feature. Three non-obvious decisions shaped it: (1) the task-land fallback for CFR (tagless repos) had to be FIRST-PARENT commits, not parents>=2 merge commits — the plan-validator caught that a merge-only predicate contradicts SPEC AC-002 (squash-lands are linear single-parent), which the Codex-driven "don't inflate the denominator" fix had over-corrected into. (2) The whole feature is deliberately NOT a readiness dimension and NOT a gate (Goodhart guard) — the number is reflection input the LLM diagnoses, never a target; this is what makes it safe to ship on by-project opt-in. (3) The pure classification layer (`classify_cfr_full`/`classify_churn`) is split from the git adapter specifically so the metamorphic property oracles (window-outside invariance) don't need subprocess fixtures. k-of-3 review (code+security+Codex) found a real exit-4-contract gap and a prompt-injection surface (unbounded commit subjects → LLM); the re-review then caught that MY security fix had over-broadened the git-argv validator enough to reject legit monorepo tag schemes (`pkg@*`) — revspec rejection had to be scoped to the revision-context field (default_branch) only, not the fnmatch tag_pattern. Managed docs TECH_SPEC/ARCHITECTURE/HOW-IT-WORKS intentionally NOT edited — the feature is peripheral+opt-in and fully covered by the CHANGELOG [Unreleased] entry + README /hm:metrics row.
