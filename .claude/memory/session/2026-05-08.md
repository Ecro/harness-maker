# Session log — 2026-05-08

## [decision:cursor-compat-dual-render-kept] kairos forensic 으로 dual-render 가 의도된 설계임을 입증, single-source 시도 안 함 | 03:57 UTC | stage:wrapup
초기 `/hm:refresh` proposal 에서 `.cursor/hooks.json` 을 "옛 schema 잔재" 로 오진 → 삭제 권장. kairos 0.5.7 의 metrics.jsonl 에서 4개 entry 가 모두 `event: "stop"` (lowercase) + Cursor 전용 `status`/`loop_count` 페이로드 → Cursor 가 `.cursor/hooks.json` 을 native 로 읽고 있다는 강한 증거. PLAN-cursor-compat-uplift Phase 2 를 "schema bug fix" 에서 "dual-render docs + 회귀 방지 테스트" 로 reframe. 같은 forensic 이 Q-B (commands single-source) 도 해소 — kairos 에 `.cursor/commands/` 가 없는데 `/hm:*` 가 작동했음을 사용자 확인. 결론: 새 가정을 세우기 전에 실사용 환경에서 forensic 을 먼저 수집.

## [decision:drift-detector-cache-scan] `__version__` 대신 plugin cache 스캔으로 drift 비교 — 양쪽 import path 가 같은 답 받게 | 04:50 UTC | stage:wrapup
0.6.2 P6: `/hm:refresh` 슬래시 명령은 렌더 시점 `--with <pinned-path>` 로 실행되어 `__version__` 이 옛 버전. SessionStart hook 은 시스템 플러그인의 새 `__version__` 사용. 같은 `detect_version_drift` 호출이 다른 답 반환 → 사용자 신뢰 erosion. fix: `latest_installed_version()` 이 `~/.claude/plugins/cache/harness-maker-local/harness-maker/<v>/` 디렉토리 스캔으로 외부 truth 사용. trade-off: 함수가 디스크 I/O 의존 → `@functools.cache` + autouse fixture cache_clear 로 perf/test 결정성 확보. top-K cap (10) 으로 무한 cache 디렉토리 증식 방어.

## [decision:review-cross-check-with-disjoint-specialists] 비-overlap 전문 reviewer 배치 시 cross-check 가 모든 finding 을 manual-only 로 만든다 | 05:30 UTC | stage:wrapup
3 specialist reviewer (code/security/perf) 를 비-overlap scope 로 배치 → 모든 finding 이 single-source → cross-check 규칙 하에 모두 manual-only → grade 자동 A. 절차 엄격 적용 시 auto-fix 안 됨 → 9개 finding 모두 사용자 위임 필요. 사용자 명시 요청으로 전부 fix 적용 (M1-M9). cross-check 규칙은 generalist overlap 가정 — specialist 분산 reviewer set 에서는 over-permissive 결과. 0.7.x 이후 consensus rule 을 reviewer-scope-aware 로 개선 검토 (현재는 manual override 로 보완).

## [decision:m2-versiondrift-rename-breaking-but-cleaner] `VersionDrift.installed` → `stamped` API breaking 감수 | 05:50 UTC | stage:wrapup
이전 0.6.1 까지 `VersionDrift.installed` 필드는 "stamped in harness.yaml" 의미였으나 이름은 "installed package" 로 읽혀 의미적 반대. 0.6.2 REVIEW M2 가 지적. 외부 consumer 가 있을 수 있으나 본 repo 안의 모든 caller 갱신 + dataclass docstring 에 이력 기록. release notes 에 breaking change 명시 필요. 명명-정확성 vs API-stability trade-off 에서 정확성 선택 (0.x 버전이라 허용).

## [checkpoint:compaction] autocompact-20260508-070740Z | 20260508 070740Z UTC | trigger:auto
Context compaction fired. Progress snapshotted to `.claude-progress-checkpoint-20260508T070740Z.json`.
Resume: load `.claude-progress.json` and continue from `current_phase`.

## [decision:0-7-1-cleanup-shipped] 0.6.2 → 0.7.1 직접 jump, 0.7.0 internal | 19:55 UTC | stage:wrapup
0.7.0 wiring round 가 grade C 로 4 P1 carry-over → 0.7.1 cleanup 으로 묶어서 단일 patch 로 release. 16 phase autoloop (ADR-101 ~ 108), 풀 pytest 그린, ruff/mypy clean. /hm:review round-2 에서 reviewer 4명이 P0 두 개 (depth-before-flock, buffered TextIOWrapper) 추가 발견 → consensus auto-fix 적용 후 grade A. manual-only M1-M9 (single-source 디자인 판단) 는 REVIEW-0-7-1-cleanup-2026-05-08.md 에 기록, user 위임. squash-merge `b9447dc` 로 통합.
