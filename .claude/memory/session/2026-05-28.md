# Session Log — 2026-05-28

## [decision:cursor-mdc-orphan-sweep] fix scoped to the classifier gap only; spoton remediation deferred post-release | 05:30 UTC | stage:wrapup
Investigating ~/spoton's 0.26.4 install surfaced one real defect: a stale `.cursor/rules/harness.mdc` left behind after `cursor` was dropped from `targets`. Root cause was a gap in `reconcile._classify_orphan` (frontmatter-without-our-provenance short-circuited to KEEP before the manifest check). Locked decisions: (1) fix RC1 only — the classifier branch — and explicitly NOT the legacy pre-tracking `.sh` residue (no manifest fingerprint → R4 keeps them; ADR-002/003); (2) no empty-dir pruning, file-only unlink (ADR-004); (3) ship as 0.26.5 then re-render spoton (ADR-005). Two non-obvious execution constraints: the version bump forced a **snapshot regen from the main repo** (never inside the worktree — `[fail:test] snapshot-regen-inside-worktree`), and the review's two P3s (symlink-follow + fm-parse downgrade in the shared `_iter_disk_files`/parse path) were deliberately left untouched as out-of-RC1-scope rather than expanding a deletion-safety function right before an immutable release. Phase 5 (spoton re-render verification) is genuinely post-release and is NOT part of this commit.

## [decision:hooks-merge-stale-path-dedup] path-agnostic hook identity; ship regex validated against real residue | 06:40 UTC | stage:wrapup
Verifying the 0.26.5 spoton remediation surfaced a second defect: switching spoton local→GitHub marketplace left every hooks.json hook TRIPLICATED. Root cause: the 05-22 dedup normalizer's regex enumerated only the `harness-maker-local` path → github-cache + dev-repo forms evaded dedup (incomplete-coverage recurrence). Locked: (1) key identity on the `python -m harness_maker.*` module-namespace suffix, path-agnostic — NOT enumerate paths (ADR-001); the validator's W1 sharpened this to a prefix-agnostic `search` so even an intermediate `uv` flag can't defeat it. (2) hooks.json only; settings.json legacy `.sh` is a separate task (ADR-002). (3) self-heal existing dups on next render (ADR-003). (4) ship 0.26.6 (ADR-004). Non-obvious call at review: code-reviewer's P3 (greedy `.*$` captures trailing whitespace) was NOT applied — the shipped regex was validated end-to-end against spoton's ACTUAL triplicated `/tmp/hooks.json.bak` (27→9) in Phase 2, and changing it pre-release would ship a regex differing from the validated one. security P2 (user re-wrapping our namespace gets deduped) is accepted-risk by ADR-001 design. Unlike the orphan task, the 0.26.5→0.26.6 bump produced NO snapshot diff (version isn't in the body-hashed content for these fixtures).

## [decision:reconcile-phantom-hash-heal] scope heal by source_template (stable), not version alone | 10:15 UTC | stage:wrapup
A `/hm:make --update` (0.26.2→0.26.6) touched only 4 tracked files, which looked wrong for a 4-version jump. Investigation: 4 Codex skills (`hm-execute/verify/wrapup/loop`) were frozen at 0.26.1 because pre-0.26.2 render wrote a "phantom" content_hash (hashed before install_ref substitution) that reconcile's `stored == recompute` edit-oracle can never verify → permanent false "user-modified" KEEP. Deleted 5 stale `.agents/skills/` dirs (4 frozen blueprint files + 1 true renamed-workflow orphan), then root-caused and fixed reconcile to self-heal. Key decision at /hm:review: code-reviewer flagged the first cut (version-floor alone) as P1 data-loss (would clobber genuine edits to ANY pre-floor ours file), security-reviewer judged it SAFE (backup-first). Resolved by gating the heal on `source_template ∈ {codex/stage_skill,loop_skill}.md.j2` — a STABLE invariant matching the repo's "never key on volatile values" rule — so only the bug's exact file class is touched; version floor (0.26.2, FIXED) protects current/future edits; backup covers the residual. Shipped as a `fix`, NOT a release — version bump to 0.26.7 + tag is a deliberate separate step (the CHANGELOG entry sits under [Unreleased]). README/TECH_SPEC/ARCHITECTURE/HOW-IT-WORKS reviewed, no change needed (internal reconcile logic, not user-facing surface).

## [decision:second-brain-promotion-pipeline] advisory→numbered must-evaluate step; folder-by-type + longest-root resolve | 14:30 UTC | stage:wrapup
The Obsidian vault was empty for weeks despite `second_brain.enabled: true` and a working module. Root cause: the only write path was an *advisory floating preamble* in the wrapup template — not a numbered procedure step — so the LLM completed the concrete local-memory Step 5 and silently dropped the advisory every time (locked as "Advisory" by PLAN-second-brain-write-failure ADR-006). Fix (0.27.0): reverse ADR-006 — add numbered **Step 5.6** (MUST-evaluate, LLM-judged "cross-project durable" filter, no count-gate to avoid synthetic notes) + `second_brain promote` CLI as the idempotency rail (deterministic `<type>-<slug>.md` + `project_id`/`hm_source` link-back). Two review rounds found real bugs my own fix introduced: (1) `promote_note` picked the first writable folder ignoring `note_types`, and `_resolve_authorized`'s first-match could shadow a nested per-type folder → silent SecondBrainError no-op → fixed both by selecting folder-by-type AND making `_resolve_authorized` prefer the longest-root match; (2) receipt `N` denominator was undefined → LLM could zero-out the observability signal → defined N concretely. Non-obvious convention learned the hard way: stage SB sections are PROSE-gated, never Jinja-gated (`test_stage_aware_second_brain_guidance` + sibling consistency) — my Jinja gate broke the codex test, reverted. Dominant accepted residual (ADR-005): promotion fires ONLY at `/hm:wrapup`, which is rarely run here (3/40 commits) — the vault fills only as fast as wrapup runs; the receipt makes under-promotion visible but can't force the habit.
