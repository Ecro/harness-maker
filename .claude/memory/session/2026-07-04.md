# Session Log — 2026-07-04

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:failure-memory-recurrence-dedup] Repaired recurrence dedup + shared-writer guard fix | 14:30 UTC | stage:wrapup
Shipped the failure-memory recurrence fix (occurrence-log CLI + wrapup search-before-write + escalation last-mile, 7 ADRs). Two non-obvious things: (1) the plan-validator + Codex second opinion caught that the ORIGINAL plan left Step 5.3 (the count>=3 escalation, the literal reported symptom) silent-dead and that taxonomy-only couldn't reach cross-unit oscillation without a wiki anchor — both became ADR-006/007 after a 3rd interview round. (2) Review found a guard-bypass bug in the new-entry occurrence-note path (unprefixed seed → phantom heading), and a Codex RE-VERIFY found the SAME class in a second untouched branch (marker-absent creation) — fixed by routing all three creation paths through one shared `_new_entry_body`/`_collapse_note` helper. Tooling gotcha worth remembering: `codex exec` in the review stage runs with cwd=base repo, but per-task changes live on the hm/<slug> branch/worktree — the first Codex re-verify reported "fix not present" because it read base; re-running with `codex exec --cd <WT>` pointed it at the actual changed files. The k-of-3 heterogeneous panel was load-bearing: no single reviewer had the whole picture (cr#2 cleared the match path and missed new-entry; Codex caught both the initial injection and the markerless follow-up).

## [decision:failures-consolidate-cli] Built opt-in memory_md consolidate; deferred live backfill; k-of-3 review caught a P0 | 16:20 UTC | stage:wrapup
Built `consolidate` to merge exact-slug failure/wiki dups — fixes the latent `matches>1` crash from the pre-0.37 `snapshot-regen-inside-worktree` c7+c4 dup. Locked: count=sum, earliest body canonical, later→dated bullet, upsert `matches>1` raise KEPT + consolidate opt-in (never wrapup-auto). Live backfill DEFERRED out of `/hm:execute` — consolidate targets BASE memory via `_base_root`, so writing it mid-execute tangles with finalize's dirty-base handling; run `consolidate --file failures` post-commit. Review was k-of-3 (Claude×2 + Codex 0.133.0): code-reviewer found a P0 wiki data-loss (shared `_split_body_bullets` peeled `- [date]` lines off wiki entries the wiki merge never re-emits), Codex independently converged on the same wiki asymmetry — both missed by the concurrency review + execute happy-path tests. Auto-fixed in review round 2; suite grew 12→21 tests.
