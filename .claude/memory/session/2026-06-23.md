# Session Log — 2026-06-23

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:judgment-ac-binding] judgment binds via an INDEPENDENT reviewer, not the builder (ADR-006) | 00:00 UTC | stage:wrapup
The plan-validator's lead critical was verification theater: if the wrapup builder grades its own just-built work, `judgment_verdict==pass` is a self-assertion and the Production gate the user chose (Round 2) is a checkbox the author ticks. The user (Interview #5) confirmed Option A — the judge is a read-only `judgment-reviewer` agent Task-dispatched separately from the builder (the proven /hm:review pattern), returning a per-rubric-criterion verdict with cited locators. This is the ONLY independence model coherent with the user's own prior choices (strict PASS=bound + Production STOP); options B (grounded self-grade) and C (advisory-only) either keep a dishonest gate or reverse the teeth. Residual (documented, accepted): the orchestrator transcribes the reviewer's verdict, and the no-network contract keeps the judgment LLM-in-template (Python `mark_judged` is pure storage). Two adversarial-review near-misses landed before commit: a symlink-DIR path-traversal in the subject hasher and an ALL-exists gate-scope that skipped partially-present subjects (both = absent-case / FS-containment failure classes).

## [decision:judgment-stale-health-display] stale-judgment health signal is weight-0 ∧ hard_gate-False display-only | 00:00 UTC | stage:wrapup
The deferred /hm:health stale-verdict display from PLAN-judgment-ac-binding was closed by adding a `judgment_verdict_freshness` advisory signal to `readiness._dim_guardrails`, NOT a new gate. The non-obvious constraint: weight=0 alone is NOT sufficient to guarantee display-only — `_score_signals` floors the whole dimension to 0 if any FAILED signal has `hard_gate=True`, so a `hard_gate=True` weight-0 signal would be a stealth gate. Both are pinned explicitly (ADR-001) and asserted by a fresh-vs-stale same-fixture test (+ a signal-absent baseline assert added in review to defeat the confoundable stale-vs-absent comparison). ADR-002 deliberately DIVERGES from the cited `wrapup_oracle_waiver_dev_mode_match` precedent: a malformed machine SPEC is fail-LOUD (failed signal), not N-A, because a present-but-unreadable SPEC means freshness is UNKNOWN (the fail-closed binding loop's advisory must be fail-loud). k-of-3 review (2 Claude + Codex) caught a consensus-P1 overcount before commit — the "N fresh" count ignored the detector's `_judgment_in_scope` scope filter and counted absent-subject passes as fresh; fixed by reusing the predicate. This completes all 4 SDD AC-type binding surfaces (mechanical/parametric/property/judgment) AND their health visibility.

## [decision:spec-requirement-gate] spec-driven SPEC-need gate via durable resume-marker; k-of-3 caught a gate-defeating fail-open | 00:00 UTC | stage:wrapup
The re-entry contract was the hardest decision: `/hm:spec` is a terminal STOP, so "plan invokes spec then resumes plan" cannot be a function-return. Two validator+Codex passes (MAJOR_REVISION → resolved) settled it as a durable one-shot resume-marker + user re-runs `/hm:plan` (auto-chain deferred as fragile + Claude-only) — the marker skips re-detection and a surface-never-re-invoke guard kills the infinite plan→spec→plan loop. Detection is fail-closed: empty/uncertain prefilter ⇒ `not-evaluated` (distinct from `none`), which Check 6 FAILs on; the prefilter is a hint, the LLM verdict runs regardless. The most valuable catch came in REVIEW: Codex (k-of-3) found a fail-OPEN that defeats the whole gate — Step 5's required PLAN frontmatter omitted the `spec_need_verdict` field that Check 6 keys on, so a literally-written frontmatter → Check 6 N-A clean-pass → gate bypassed. Neither Claude reviewer caught it because they were scoped to disjoint halves (Python module vs prose); only the cross-stage producer↔consumer view exposed it. Six review findings (incl. path-traversal slug guard, readiness waiver double-count, dead waiver-rate, cwd-fragile `--root .`→`--root <WT>`) were all auto-fixed before commit. Built via 4 delegated executor phases (49+16+25+11 tests) + a 6-fix review round; all render-gated to spec-driven (task-driven byte-unchanged).
