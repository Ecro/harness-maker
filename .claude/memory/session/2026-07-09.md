# Session Log — 2026-07-09

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:second-opinion-multi-model] Generalized codex_second_opinion → second_opinion.models with K=2-fixed consensus | 13:15 UTC | stage:wrapup
Shipped multi-vendor cross-model second opinion (`second_opinion.models: [codex, antigravity]`). Non-obvious decisions: (1) The consensus threshold K stays **fixed at 2** as the voter pool N grows — this was a deliberate recall-favoring choice (more models → consensus is *easier*, agreement % drops 67→50→40), NOT a bug, and it required ZERO change to `conditional_router.scope_aware_consensus` because its `len(reviewers)>=2` rule was already source-agnostic (ADR-006, pinned after the 2nd plan-validator pass flagged it was asserted-by-example not specified). (2) The Phase-1 empirical `agy --sandbox` write-probe (ADR-012, gate before template work) found the safety comes NOT from `--sandbox` blocking writes but from a **project-less** `agy --print` exposing no file tools at all — so the recipe must never pass `--project`/`--new-project`/`--add-dir`. It also found `agy` can HANG on a file-request, forcing a timeout. **Recurrence worth flagging** (the failures-CLI rejected the append — the existing slug `codex-exec-recipe-compound-command-defeats-allow-rule` is >40 chars, grandfathered, so `upsert-failure` won't accept it): I re-tripped that exact pattern by rendering `timeout 120 agy …` — the command begins with `timeout`, so `Bash(agy:*)` can't prefix-match → headless permission-deny → the graceful-skip path never runs. Caught by the Codex third-voter + security-reviewer in /hm:review (green unit suite couldn't see it). Fixed by using agy's native `--print-timeout` so the command begins with `agy --print --sandbox`, + tightening the allow rule to the scoped `Bash(agy --print --sandbox:*)`. The heterogeneous voter earned its keep: Codex independently caught a truncated-inner-JSON acceptance in `extract_antigravity_payload` that the Claude code-reviewer had cleared.
