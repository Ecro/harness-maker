# Session Log — 2026-06-25

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:autopilot-config-surface] surface autopilot in interview + nullable caps + cross-session persistence | 02:01 UTC | stage:wrapup
Shipped autopilot config surfacing (`_ask_autonomy` interview round), nullable caps (`null`=unlimited), and SessionStart auto-arm persistence (`autopilot_persistent`). Two non-obvious decisions worth remembering: (1) the cap default flip applies to NEW interviews only — the Pydantic field default stays bounded 20/300 so existing/absent harnesses are never silently unbounded on `--update` (ADR-005), which also kept the existing `:167/:182/:171` fallback tests unchanged; (2) the `/hm:health` autoarm smoke is weight=0 display-only — a non-zero weight broke the "guardrail signals sum to 100" budget and inflated the empty-project score to exactly 30. The k-of-3 heterogeneous Codex voter caught two real bugs both Claude reviewers missed: a duplicate-stage-pipeline unbounded loop (boundedness invariant assumed uniqueness) and a strict-hook/lax-reverse-mapper bool-coercion mismatch — both fixed in-review. Version bump deferred to release per the repo's CHANGELOG `[Unreleased]` convention. User requested push+merge.
