# Session — 2026-06-17

## [decision:locale-and-command-observability] locale-everywhere + per-command banners shipped via wrapper-layer partials | wrapup UTC | stage:wrapup

Shipped PLAN-locale-and-command-observability through research→plan→execute→review→wrapup. Non-obvious decisions/surprises worth remembering:

- **Two prompt features, one mechanism**: a single `{{ config.locale }}`-driven `output_language.md.j2` partial + reframed `step_manifest.md.j2` start banner + new per-stage `stage_end_summary.md.j2`, all at the wrapper/stage-template layer — zero new locale-variant files (the LLM translates at runtime). Enforcement is deliberately asymmetric and stated honestly (ADR-002): end-summary vars are StrictUndefined-required (hard), locale is `/hm:health` presence-audit only (config.locale always defaults → no UndefinedError leg).
- **Scope expanded twice from review**: validator W2 added AGENTS.md to the locale surface (Codex's CLAUDE.md-equivalent); the P3b security fix added a `models.py` locale sanitizer (single-line/≤35/else en) — outside the original PLAN scope but an intentional, user-requested, review-driven addition. Documented rather than re-running review (drift gate reads the REVIEW verdict, doesn't re-analyze).
- **The auto-fix loop earned its keep**: my P2 fix (truthiness-only locale guard) regressed the absent-key case (StrictUndefined raises on `bool()` of an absent `config.locale`); the full-suite "verify build" step caught it → converged on the combined `is defined and ... and <truthy>` guard. See failures [[jinja-is-defined-returns-true-for-none]] (count++).
- **Verify reconcile-family claims against source**: an Explore pass asserted CLAUDE.md uses the inverted `@hm:harness:*` family (would mean a new section does NOT reach existing installs); reading `reconcile.py` directly disproved it (it's `@hm:user:*` → outside-marker content propagates). The wiki [[reconcile-keeps-frontmatterless-renders-forever]] discipline applied.
- **Deferred (not done)**: dogfood `.claude/` re-render via `/hm:make` — the source templates + tests are the deliverable; re-rendering this repo's own committed harness is a separate rollout step. Until then, the new banners/locale directive are NOT yet active in this repo's own `/hm:` commands.

## [decision:codex-second-opinion-sandbox] main-loop Codex exec + sandbox escape, hardened in review | 09:40 UTC | stage:wrapup
Landed PLAN-codex-second-opinion-sandbox: moved `codex exec` out of the tool-restricted reviewer subagents into the stage main loop (new shared `codex_exec_mainloop.md.j2` partial), added a `Bash(codex exec:*)` settings allow rule + `dangerouslyDisableSandbox` directive (gated on `codex_second_opinion.enabled`), deleted the 3 dead agent-body partials, and re-homed the plan-validator reconciliation contract (PIDA) into its body. **Non-obvious:** the k-of-3 heterogeneous Codex reviewer caught the highest-value bug its Claude peers missed — `Bash(codex exec:*)` is a command-PREFIX match, so the original compound recipe (`content=…; … codex exec …`) would be denied headless, recreating the exact skip the PLAN set out to kill. The combined Round-2 fix (bare `codex exec` command + Write-tool prompt build + is_codex gate) closes that plus an untrusted-`$(…)` expansion vector in one restructure. **Merge surprise:** a concurrent session committed the locale feature (c728967) onto the same plan/review stage files mid-work; the worktree 3-way merge combined both cleanly. Deferred (ADR-001): re-render this repo's own `.claude/` (+ log_agent) from the fixed templates.
