# Session 2026-05-15

## [decision:fix-work-docs-naming-footgun] adopt two-layer non-symmetric guardrail (template warning + verify-stage advisory probe) | 02:15 UTC | stage:wrapup
Root-caused user-observed `work_docs/` vs `work-docs/` coexistence in `~/edge_testfarm_os` as a YAML-key-vs-value LLM footgun (zero literal `work_docs/` references in our src/ — the offending PLAN had `validator_outcome: SELF_REVIEW`, not a /hm:plan-standard value). Validator surfaced a critical: appending a 7th check to `verify-before-completion/SKILL.md.j2` would falsify the "6 checks" doc-truth invariant across 7+ doc sites — pivoted Layer 2 from the SKILL into `verify.md.j2` stage body under a new `## Advisory probes (non-blocking)` section. Validator also surfaced that Layer 1 only protects stage-template-reading LLMs; ad-hoc invocations bypass it — explicitly accepted this limit in ADR-003 Consequences and chose NOT to add a Layer 3 (CLAUDE.md instruction line) until WARN telemetry shows recurrence. User declined backward-compat / opt-out marker / migration tooling because the project is pre-release ("아직 릴리즈 되지 않았기 때문"). Verify check is WARN-only, not BLOCKING (ADR-005) — consistent with ADR-002 (ad-hoc artifacts allowed inside `work-docs/`), so we cannot be certain `work_docs/` is always wrong. Surprise during Phase 3: hit `[fail:test] snapshot-regen-inside-worktree` for the 4th time — ran `tests/snapshot/regenerate.py` from main repo BEFORE worktree finalize, so new templates weren't yet in main; correct sequence (per pending-proposals.md `snapshot-regen-order-guard`) is finalize → regen → full pytest. Recovered by re-running regen post-finalize; full suite green.
