# Session Log — 2026-06-29

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:autopilot-invocation-and-marker-fix] dogfooded the invocation-convention + marker-root fix end-to-end | 23:50 UTC | stage:wrapup
Non-obvious seam surfaced during wrapup itself: `memory_md._base_root` deliberately strips the `.worktrees/<slug>` suffix (ADR-001/002 — memory is shared, always base), so the wrapup memory writes land in the BASE working tree, NOT the `hm/<slug>` worktree. In the per-task feature-branch model the wrapup commit happens IN the worktree and Step 7.7 `task-land` self-aborts on a dirty base — so base memory dirt must be folded into the squash commit AFTER land (stash base memory → task-land → pop → `git commit --amend`), or it blocks the land. This is a latent tension between memory-at-base and the per-task land model worth a template follow-up. Substance of the work: REVIEW round-2's most important finding was code-reviewer-only (neither security nor Codex looked at `autopilot_caps`) — a marker/ledger root asymmetry I introduced in round-1 where the marker resolved to base but the paired ledger ops kept the unresolved worktree root, splitting the step count. Cross-model value held again: Codex caught the original audit's `grep -v "uv run"` undercount (31 hidden `--with`-less sites).
