# Session Log — 2026-05-10

> Non-obvious decisions and constraints from each work unit this session.
> Trivial work omitted. Format: `## [decision:<slug>] <what> | <HH:MM> UTC | stage:<stage>`

## [decision:loop-interview-intensity] runtime: block in loop-context YAML is mandatory for counter survival across /compact | 14:00 UTC | stage:wrapup
`/hm:loop` runs `/compact` every ~10 iterations (or at 60% context fill). In-memory counters (`convergence_streak`, `criterion_ambiguity_counts`, `checklist_fail_counts`, `last_test_result`) reset on compaction. The only safe persistence path is the loop-context YAML itself — adding a `runtime:` top-level block that the loop driver reads back after each compaction. The alternative (rely on Claude's summarized context to recover counter values) is unreliable because summaries don't guarantee numeric precision for convergence_streak, and the Gate 3 baseline (exact set of failing test names) cannot survive summarization. Key constraint: the `runtime:` block is cleared at loop START (not end), so a crash-and-resume gets stale counters from the previous run — this is intentional (fresh counters on resume = conservative, no phantom streak carried in).

## [checkpoint:compaction] autocompact-20260510-035112Z | 20260510 035112Z UTC | trigger:auto
Context compaction fired. Progress snapshotted to `.claude-progress-checkpoint-20260510T035112Z.json`.
Resume: load `.claude-progress.json` and continue from `current_phase`.
