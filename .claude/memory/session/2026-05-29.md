# Session log — 2026-05-29

## [decision:worktree-base-artifact-pollution] keep-base-clean over a 6th defense layer | 14:40 UTC | stage:wrapup

Shipped PLAN-worktree-base-artifact-pollution: parallel `/hm:execute` across sessions was blocked because the harness dirtied its OWN base (telemetry every tool call → `.claude/observability/`; loop-context/iter-receipts/render-manifest), so finalize stashed every run and the queue-guard blocked the next `create`. Chose to make the existing 5-layer defense go QUIET (gitignore churn + align both dirt-filters + drain orphan stash-refs + commit RESEARCH/SPEC) rather than add a 6th guard. Non-obvious constraints discovered in execute/review: (1) the create-time `.gitignore` append self-dirties `.gitignore`, which trips the dirty-base guard AND gets stashed — had to recognize `.gitignore` as co-managed/non-dirtying (safe: finalize squash never touches base `.gitignore`). (2) `prune_stale` over a ref whose recorded base dir is gone crashed with uncaught `FileNotFoundError` (`_run` only catches CalledProcessError/TimeoutExpired) — fixed by draining gone-base refs before any git call. (3) finalize's `git add -A` WIP capture swept a cwd-writing test's stray `new.txt` into base when the suite was run inside the worktree — the count:7 "don't run snapshot/e2e in WT" lesson is insufficient; plain unit tests leak too. Accepted limitation: non-default `work_docs.dir` is not churn-isolated (the dominant `.claude/` churn is unaffected). Standalone `/hm:review` (run separately after exec-rev) caught 3 issues the bundled review missed — independent re-review earns its cost. Open follow-ups: `_ensure_gitignore_entry` RMW race → atomic-append; orphan-marker sweep vs stage-only kept-alive marker; `work_docs.dir` churn-isolation.
