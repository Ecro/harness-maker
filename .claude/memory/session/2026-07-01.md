# Session Log — 2026-07-01

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:command-surface-registry] Fail-open misroute guard over a hand-maintained registry | 02:30 UTC | stage:wrapup
The root-cause fix for LLM misrouting `python -m harness_maker.*` calls is a self-describing command surface, not per-module band-aids. Key non-obvious trade-off: the guard is FAIL-OPEN (redirect only when the token is another module's verb) precisely so wiring it into ~19 modules can never false-break a valid subcommand the hand-maintained registry under-lists — a missed subcommand degrades to pre-guard behavior. The residual risk (an under-listed verb that collides with another module) is contained by the T-C2 bidirectional parity gate, which is why that gate is load-bearing, not cosmetic. Direction of unification was reversed mid-plan by evidence: dot-form is 230:1 dominant, so `autopilot` (the lone Typer form) was pulled DOWN to dot-form rather than unifying everything up to Typer.

## [decision:review-grade-criteria] surface unverified-severe findings, don't re-grade; path-differentiated halt | 00:00 UTC | stage:wrapup
Diagnostic question ("is /hm:review's grade trustworthy?") turned into a hardening change. Key trade-off locked across 3 plan-interview rounds + a MAJOR_REVISION→APPROVED validator loop: keep the grade LETTER deterministic and unchanged (non-breaking), and add surfacing on top — `human_review_needed=true` when a manual-only/weak-consensus P0/P1 exists at grade≥threshold. Round 3 chose path-differentiated halt (interactive STOPs, loop proceeds) over full-halt or cosmetic-only. Cross-model review earned its keep twice: Codex caught that "non-breaking/surfacing-only" understated a real control-flow change (`human_review_needed` is already an autopilot STOP signal); a second code-reviewer caught that the "surface at loop close" promise had NO runtime reader on the loop path (inert mitigation) — corrected the prose to stop over-promising rather than expand scope into loop.md.j2. Enforcement is prose + a contract render test (producer-gate), NOT a Python backstop — deliberately, per the llm-prose-invokes-python theater risk. Two named follow-ups shipped as REVIEW notes, not code: a real loop-close reader, and reconciling consensus-passed-by-scope into review.md.j2's inline Grade Computation.
