# Session log — 2026-05-20

## [decision:worktree-finalize-stash-isolation] L2+L3 scope, transparent stash isolation chosen over abort-style guard | 21:45 UTC | stage:wrapup
PLAN-worktree-finalize-stash-isolation closed Phases 1+2+3. Round 4 reframe shifted L2 mechanism from runtime-diff detection + abort to `git stash push -u` envelope around `git merge --squash`. Stage-only mode required a finalize→wrapup handshake (`.hm-finalize-stash-{wt_name}` ref file + new `post-commit-pop` CLI) because popping before wrapup commits would mix user dirty into the staged squash result (validator 2nd-pass critical). ADR-001 §4 pins the `handed_off` flag flip BEFORE cleanup; ADR-005 carves out submodules as the single non-transparent exception. Two consensus-passed REVIEW findings (atomic `.gitignore` write + exact stash-list match) were auto-fixed in Round 2; manual-only P0/P1 (positional `stash@{N}` ref staleness, path-traversal on `session:` field) tracked as Phase 4 follow-up. The `/hm:exec-rev` invocation that ran this work used `uv run --with .../0.17.1` (installed plugin version) → the actual finalize used OLD code; unit tests validated the new code directly. Real-world end-to-end requires plugin update.

## [decision:wrapup-commit-strategy] include all 120 pre-existing dirty files in this commit per explicit user override | 21:45 UTC | stage:wrapup
Pre-flight surfaced 120 unstaged files (`.claude/memory/*`, `tests/e2e/sandbox-plugin-test/*`) from prior sessions — exactly the contamination scenario PLAN-worktree-finalize-stash-isolation was designed to prevent. Initial AskUserQuestion offered three policies; user initially chose "separate commits" then changed mind to "그냥 같이 commit and push 하자". Stash was popped + everything staged + single commit. Decision is logged so future readers know the commit body includes work-units it doesn't enumerate (memory + sandbox-plugin-test fixture rerender from a prior unfinished work unit). The PLAN's own protection mechanism remained inert on its own enabling commit — a notable irony documented in REVIEW-worktree-finalize-stash-isolation-2026-05-20.md.

## [decision:total-spec-coverage-0.18.0] ship full SPEC framework + 174 SPECs + cache mutation 98.08% | wrapup | stage:wrapup
PLAN-total-spec-coverage executed end-to-end across 8+ commits in worktree `execute-20260519T1544Z`, squash-merged to main as `bf65a1d` then `419b6c5` (snapshot regen). Headline: spec_machine + spec_mutation + spec_inventory subsystem (~2000 LOC), batch_generator emitting 174 dual-file SPECs, batch_refiner with auto-derived AC titles from module docstrings (97.6% test_ids resolved post `-q` bug fix), spec_quality 8-dim rubric with structural-signal bonus for testability (T1 ≥85 1/3 → 3/3, T2 ≥85 1/29 → 29/29), cache.py mutation kill rate 3.8% → 98.08% via 16 new mutant-killing tests, LLM judge wiring (INTEGRATION=1 gates AnthropicJudgeClient), templates baked (spec.md.j2 dual-write + loop.md.j2 P5 batch procedure). 5-file version sync 0.17.1 → 0.18.0. **Key trade-off**: P5 bulk authoring switched from `autoloop_driver.run()` to prompt-driven `/hm:loop p5-batch-N` after plan-validator R2 caught the Python-callable-cannot-invoke-slash-command infeasibility; ADR-013 R2 codifies — BatchSpecState is CRUD helper, NOT ExecutorCallable. **Key surprise**: 3 mutmut survivors after the test boost initially mis-classified — mutant 42 (`path=None` in invalidate) actually killed by new test, but mutmut's per-mutant cache returned stale "survived" verdict. Force re-run via `mutmut run 42 --paths-to-mutate=...` confirmed kill, raising real rate to 98.08%. Pattern: `mutmut-stale-cache-on-test-changes` wiki entry. **CWD inheritance footgun**: after squash merge, `/hm:wrapup` ran with cwd=worktree (not main); Bash commands silently linted/tested worktree files while Edit modified main. Symptom: `grep` and Read disagreed on same relative path. Detected via `readlink -f`. Fix: explicit `cd /home/noel/<project> &&` prefix or exit worktree before wrapup. Captured in failures (`cwd-inherited-from-worktree-into-main-commands`). Push to remote completed on `419b6c5..main`.

## [decision:fresh-install-p0-calibration-0.19.2] wire INTENDED_P0_SIGNALS into user-facing emitter (two-branch policy + footer) | 16:00 UTC | stage:wrapup
PLAN-fresh-install-p0-calibration closed Phases 1+2+3+4 in worktree `execute-20260520T0641Z`. Diagnosis correction in /hm:plan Round 1 was the load-bearing moment — prior conversation framed the fix as "2 things" (P0 noise + harness-maker's own agents > 200 lines), but investigation showed all 5 over-line agents in the test environment were USER-owned custom files (no `generated_by` provenance); harness-maker's own ships are all ≤188 lines. Scope collapsed to single fix: priority demotion only. Two-branch policy chosen — TELEMETRY_AUTO_RESOLVE_SIGNALS suppressed when `metrics_has_samples.passed == False` (samples<5), USER_AUTHOR_SIGNALS forced to "P2" regardless of weight. Footer line emitted when counters > 0. `INTENDED_P0_SIGNALS` retained as union for backward-compat (Phase 4 integration test allowlist still imports it). **Key trade-off**: positional tuple return on `_extract_layer1_actions` (3-tuple) accepted instead of NamedTuple — one private caller today, refactor deferred to M2 follow-up. **Key infrastructure block**: all 7 specialist agent dispatches (plan-validator, test-reviewer, code/security/ux/perf/concurrency-reviewer) failed with `claude-4-7-opus[1m]` and `claude-4-6-sonnet` model-not-found errors — the rendered agent definitions pin to model IDs that the current Claude Code runtime can't dispatch. Each stage's quality gate (plan validation, A.5 test-reviewer gate, consensus filter) fell back to in-process self-review; documented inline in PLAN's `## 🔍 Plan Validation` + REVIEW's `## 📝 Manual-Only Findings`. Separate `[fail:tooling] plugin-agent-model-pin-dispatch-fail` failure entry filed. **Side issue surfaced**: integration tests invoking the full renderer pipeline leak the worktree path into 127 `tests/e2e/sandbox*/**` snapshot files because `_pin_harness_maker_pkg_root` fixture is only in `tests/unit/conftest.py`, not in integration conftest — manually reverted before commit; `[fail:render] e2e-sandbox-worktree-path-leak-on-integration-regen` filed. 5-file version sync 0.19.1 → 0.19.2; CHANGELOG [Unreleased] section cut to [0.19.2] (bundles the prior pending worktree-stash-isolation work). Final: 13 files staged, full pytest 2518 passed / 44 skipped, grade A approved.

## [decision:fresh-install-p0-calibration-re-tag-0.19.3] re-tag after v0.19.2 ruff-format quality-gate failure | 16:45 UTC | stage:wrapup
v0.19.2 tag push 트리거된 release.yml run (id 26148626375) 이 quality-gate 의 `Ruff format check` 단계에서 즉시 fail (5 files would be reformatted). 원인: 로컬 wrapup verify pass 가 `ruff check` 만 돌리고 `ruff format --check` 는 누락. PyPI publish + GitHub Release 모두 진행 안 됨 (quality-gate 가 첫 job). CLAUDE.md 릴리스 절차 따라 v0.19.2 tag 는 dead 로 남기고 v0.19.3 으로 re-tag — `ruff format` 적용 + 5 version files 0.19.2 → 0.19.3 bump + CHANGELOG [0.19.2] → [0.19.3] 재명 + 0.19.3 re-tag explanation 추가. `[fail:lint] ruff-format-not-in-local-verify-pass` 등록. **Lesson**: harness-maker `/hm:wrapup` template Step 2 toolchain 명령 set 이 `ruff check` 만 명시 — `ruff format --check` 도 명시 필요 (별도 follow-up PR scope).
