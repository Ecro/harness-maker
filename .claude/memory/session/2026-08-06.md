# Session Log — 2026-08-06

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

**worktree-side-defaults** — collapsed the `worktree:` config axis from four keys to one `worktree.enabled` boolean (7 PLAN phases, 7 ADRs, ~57 files). Three of the four keys had zero runtime effect and two (`scope`, `branch_prefix`) were hardcoded template literals, so hand-edits were silently reverted on every re-render and a Side harness answering 'isolation off' rendered `scope: [execute]` and turned execute isolation ON. Now ON = every /hm: stage runs in .worktrees/<slug>/ on hm/<slug>; OFF = none. Enforced by one reader (`worktree_enabled`, 3-generation fail-closed fallback) and one writer (`cli._apply_worktree_enabled`), each pinned by a structural test forbidding a second. Review: D -> A after one auto-fix round, 5 voters, 2 P0 + 6 P1 consensus. Both P0s were self-inflicted by this change: `WTR = '.'` made `wrapup_land` abort on every OFF wrapup (it requires an absolute path), and a False conjunction in the finalize dispatch routed flag-OFF + a live hm/* worktree into the destructive legacy squash — the count:3 contamination class. Drift: 3 scope violations, each already justified in the PLAN's execution table. Memory: 1 wiki (single-reader/single-writer config axis), 3 new failures, 1 reuse (`remediation-instructs-refused-action` -> count:3, the guard's own refusal message advising `task-land` on a possibly-live foreign worktree). Docs: Phase 7 updated CHANGELOG/README/HOW-IT-WORKS prose but missed six YAML config examples still advertising the retired `scope`/`cleanup` — fixed here in README, both HOW-IT-WORKS locales, TECH_SPEC and ARCHITECTURE.

**onboarding-interview-ux** — made the first interview disclose what it decides silently and offer what it detects. Shipped: `harness-maker detect-tools --json` (uncached by design — `ProjectProfile`'s 24h cache is invalidated by manifest mtime, and installing a CLI touches no manifest, so a cached answer would call a tool installed minutes ago absent); a "Set for you — not asked on this path" table on the make summary; exactly one conditional second-opinion question, asked only when a CLI is detected; `/hm:configure` entries for `second_opinion` / `autonomy` / `locale` (previously an axis silently disabled at install could only be changed by hand-editing `harness.yaml`); a `/hm:health` advisory for the installed-but-disabled state, silent for that feature's whole lifetime because its smoke was gated on the axis already being ON; removal of the `consensus` / `caching` questions (no code branches on either — fields and preset defaults kept, no migration); and the quick-start's dead `/hm:ai-readiness` replaced by `/hm:health`, with the documented-commands gate widened to `/hm:<name>` + `@hm-<name>` across `commands/**` and both READMEs. Review: D -> A over 3 iterations, 4 voters (code-reviewer, security-reviewer, codex, antigravity), K=2. **The single P0 was the disclosure table being false about its highest-consequence row** — it stated `autonomy.level: gated` (off) while a fresh install renders `auto_safe` + `autopilot_persistent: true`, re-armed by a SessionStart hook every session; the structural gate written in the same phase missed it because it asserted `re.search("autonomy|autopilot")`, presence rather than truth. Two silent-success failures in one session, neither caught by the thing claiming success: a background pytest notification reported exit 0 on `rc=1`, and a `str.replace` patch script printed "patched" after matching nothing, so the egress-disclosure security fix silently did not land and surfaced only when the round-2 reviewer re-read the file. Also learned: **editing one rendered template trips six gates** (aggregate surface ratchet, baseline-delta attribution doc, command-surface registry, synthesize snapshots, round-trip budget, render fixtures) — the PLAN anticipated one, four were found by tripping them. Deferred deliberately: `docs/**` still carries ~40 retired-command references, three with whole sections in `HOW-IT-WORKS.md` (a rewrite, not a rename). Wrapup note: the verification marker read "not fresh" until the probe was re-run in the same `uv run --with` shape `/hm:verify` had used — three launcher shapes gave three different keys on an unchanged tree (instance 3 of `verification-cache-key-nondeterministic`, now escalated). Memory: 2 wiki, 1 new failure, 3 reuses, 2 escalations.
