# Session Log — 2026-08-06

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

**worktree-side-defaults** — collapsed the `worktree:` config axis from four keys to one `worktree.enabled` boolean (7 PLAN phases, 7 ADRs, ~57 files). Three of the four keys had zero runtime effect and two (`scope`, `branch_prefix`) were hardcoded template literals, so hand-edits were silently reverted on every re-render and a Side harness answering 'isolation off' rendered `scope: [execute]` and turned execute isolation ON. Now ON = every /hm: stage runs in .worktrees/<slug>/ on hm/<slug>; OFF = none. Enforced by one reader (`worktree_enabled`, 3-generation fail-closed fallback) and one writer (`cli._apply_worktree_enabled`), each pinned by a structural test forbidding a second. Review: D -> A after one auto-fix round, 5 voters, 2 P0 + 6 P1 consensus. Both P0s were self-inflicted by this change: `WTR = '.'` made `wrapup_land` abort on every OFF wrapup (it requires an absolute path), and a False conjunction in the finalize dispatch routed flag-OFF + a live hm/* worktree into the destructive legacy squash — the count:3 contamination class. Drift: 3 scope violations, each already justified in the PLAN's execution table. Memory: 1 wiki (single-reader/single-writer config axis), 3 new failures, 1 reuse (`remediation-instructs-refused-action` -> count:3, the guard's own refusal message advising `task-land` on a possibly-live foreign worktree). Docs: Phase 7 updated CHANGELOG/README/HOW-IT-WORKS prose but missed six YAML config examples still advertising the retired `scope`/`cleanup` — fixed here in README, both HOW-IT-WORKS locales, TECH_SPEC and ARCHITECTURE.
