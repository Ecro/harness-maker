# Session log — 2026-06-20

> Non-obvious decisions, constraints, and surprises. One entry per `[decision:<slug>]`.

## [decision:multisession-worktree-phase0-partial-wrapup] Phase 0 of an 8-phase PLAN landed as a deliberately-partial wrapup | 03:50 UTC | stage:wrapup
The multisession-worktree-concurrency PLAN is an 8-phase per-task feature-branch overhaul; only Phase 0 (ADR-009 drain-trigger relocation) was implemented this unit. Wrapup's quality bar wants `status: complete` + zero `- [ ]`, but that would falsely claim the whole feature is done. Deliberate deviation: PLAN frontmatter set to `status: in-progress`, the feature-level Success-Criteria checkboxes left UNCHECKED (genuinely unmet until Phases 1–7), Phase 0 carries its own `✅ DONE` status line. A single-phase commit is the right call here — the PLAN itself recommended landing Phase 0 (the 76-branch leak fix) independently first.

## [decision:snapshot-regen-verify-from-canonical-root] Verified snapshot regen had no worktree-path contamination by re-running from main root | 03:50 UTC | stage:review
Hit the known `snapshot-regen-inside-worktree` (count:7) trap — regenerated snapshots while cwd was the execute worktree. Rather than assume contamination or assume cleanliness, ran the deterministic guard: after `finalize stage-only` merged to base, re-ran `regenerate.py` from the **main repo root** and diffed `tests/snapshot/` — empty diff proved byte-identical output (no pkg-path leak into the command-body hashes). Also confirmed blast radius by checking that only the 2 edited templates' command hashes moved (`harness.yaml`/`help`/`exec-rev` unchanged). The trap action does not always contaminate; the diff-from-canonical-root check is how you KNOW.

## [decision:autonomy-p1-only-honest-incomplete-wrapup] Shipped only Phase 1 of the 9-phase autonomy PLAN; wrapup committed an increment with status:in-progress, not a false complete | 04:30 UTC | stage:wrapup
The pipeline-auto-advance feature is 9 phases (validator flagged it high-risk; P0 is a go/no-go feasibility spike for the runtime-chaining premise, which gates P3/P6). This execute pass deliberately scoped to **P1 (schema foundation only)** — a complete, fully-verified vertical slice (AutonomyConfig round-trip, 12 tests, ruff+mypy+full-suite green) — rather than cramming 9 phases into one turn and faking green. wrapup therefore set PLAN `status: in-progress` (NOT `complete`) and left the unmet feature-level Success Criteria checkboxes unchecked, deviating from wrapup Step 4's one-wrapup-per-PLAN assumption. Key design lock-ins from the plan interview that the schema already encodes: never-auto deny baseline is code-fixed not a config field (ADR-003, so a yaml edit can't subtract a guard); reverse-mapper uses `model_validate(strict=False)` so the enum-list pipeline coerces from yaml while the level Literal still rejects bad values. Two unrelated leftovers observed in base: a foreign-session worktree `execute-00db828ee6a9-*` (different uuid → left untouched per uuid-ownership discipline) and a 2026-05-25 finalize-stash + 75-branch backlog (pre-existing, surfaced by `worktree create` warnings — not this task's).

## [decision:autonomy-p2-failsafe-marker] Phase 2 `.hm-autopilot` marker landed; fail-safe-to-gated is the whole design, and strict-model/lenient-load is now confirmed cross-cutting | 05:40 UTC | stage:wrapup
Phase 2 (session marker + CLI) shipped; PLAN stays `in-progress` (2/9). The marker design is fail-safe-first: load() → None on absent/corrupt/empty/schema-invalid, active_marker() adds a session-uuid ownership gate, effective_level() clamps an unknown harness.yaml level to gated — every degenerate path resolves to the SAFE default, never to an enabled level. The k-of-3 review (2 Claude + Codex) was unanimous Grade A but surfaced a genuine cross-voter divergence on effective_level (Codex "force gated" vs reviewer "validate the value"); resolved by clamping only *unknown* yaml levels to gated while still honoring a valid committed level (correct ADR-006 precedence). Confirmed cross-cutting idiom: a pydantic `strict=True` model needs `model_validate(data, strict=False)` at the JSON/YAML load boundary or str→enum coercion is rejected — bit in BOTH Phase 1 (`_parse_autonomy`) and Phase 2 (`autopilot.load`); do not "fix" it away. New failure logged: typer.Option(default_factory=, "--flag") is a SyntaxError. P0 feasibility spike still pending (gates the runtime-chaining phases P3/P6).

## [decision:autonomy-p4-marker-gated-guard] Phase 4 never-auto guard landed marker-gated (ADR-003 refined mid-impl); F→A over 2 review rounds | 07:50 UTC | stage:wrapup
Phase 4 (autopilot_guard) shipped; PLAN stays in-progress (4/9). Mid-implementation I HALTED and surfaced an ADR-003 conflict: the literal "always-on static settings.json deny" would block the maintainer's own manual `git push` (every wrapup pushes manually). User approved refining ADR-003 to a marker-gated PreToolUse hook — never-auto fires only while `.hm-autopilot` is active, so autopilot-OFF leaves manual workflows untouched. The k-of-n review (2 code + security + Codex) graded the first impl F: marker not worktree-aware (silent no-op in the exact mode it guards), adjacency-regex git bypass (`git -c k=v push`), Bash-redirect + hooks.json self-disable slipping past the Write-only block. All fixed in auto-fix round 1; a round-2 security re-review verified the fixes hold + found 2 minor (future-dated-TTL fixed; a shlex-fallback false-positive DECLINED because the reviewer's fix would introduce a false-negative bypass — block-biased is the safe direction for a security guard). Final A. Base moved to 22084b4 (other session's worktree-concurrency Phases 1-4) during the work; finalize stage-merged P4 on top, history intact.

## [decision:autonomy-p3-stop-hook-backstop] Phase 3 Stop-hook backstop landed (Grade A); pre-existing memory red root-caused to lost wiki close-marker, fixed separately | 20:10 UTC | stage:wrapup
Phase 3 (`autopilot_guard --mode stop-hook`) shipped + reviewed A; PLAN stays in-progress (P0,P1,P2,P3,P4 done / P5-P8 remain). The non-obvious turn this unit: the wrapup suite-gate red (`test_cli_real_repo_memory_surfaces_recent_entry`) was initially scoped as a "brittle top-K assertion" to harden via /hm:quick — but investigation DISPROVED that. The test was correctly catching real data corruption: `.claude/memory/wiki.md` had lost its `<!-- @hm:/user:entries -->` close marker (overwritten by MY P4 wrapup's wiki append at 7ac9c30), so `parse_entries` returned [] and all 132 wiki entries were invisible to retrieval. New-evidence-beats-prior-plan: fixed the root cause (restore marker, no test weakened) in a separate commit 5d5ea1e per user direction, NOT folded into P3. This is recurrence #3 of [fail:render] wrapup-eof-append-outside-marker → crossed the proposal threshold → filed wrapup-close-marker-integrity-guard (mechanical guard, since the prose reminder has now failed 3×). Also surfaced for user: a stale `.hm-loop-active` marker (no .current-iter → not a real autoloop) and a lingering owned worktree `execute-b8f1c7f74b4f` holding UNRELATED uncommitted work (stage templates + worktree.py) — left untouched per multi-session data-loss discipline. P3 review fixes: isatty guard (TTY hang), descriptive-only reason (false-imperative pre-P6), declined --mode required=True (PreToolUse entry relies on default).

## [decision:autonomy-p5-caps-kill-switch] Phase 5 runaway caps + kill switch landed (Grade A); Codex k-of-3 caught a P1 both Claude reviewers missed | 22:05 UTC | stage:wrapup
Phase 5 (`autopilot_caps` + minimal `autopilot_ledger`) shipped + reviewed A. PLAN stays in-progress (P0,P1,P2,P3,P4,P5 done / P6-P8 remain). P6 (the phase that actually turns auto-advance ON) now has both its dependencies (P4 enforcement + P5 caps) satisfied. Non-obvious turn this unit: the **heterogeneous k-of-3 voter earned its seat** — Codex flagged that `autopilot_ledger.append_event` validated `event in EVENTS` but then did `record.update(fields)`, so a caller `fields={"event":"pass"}` overwrites the validated event AFTER the guard and writes a forbidden iter_receipts.Verdict literal to disk (ADR-009 bypass). Both Claude reviewers missed it. Single-source → grade stayed A, but fixed voluntarily (set authoritative keys LAST) because it defeats the ledger's whole point. Also hardened the disjointness invariant to be structural (derive EVENTS from the Literal + import-time assert) instead of test-only. Multi-session note: another session's loop was active throughout (`.hm-loop-active` + a live worktree `execute-4a718bcbad94`); left untouched, my wrapup commits only my staged P5 changeset + memory. The known loop-marker-not-session-scoped gap (single global `touch` file, session-blind loop_gate) remains a candidate follow-up.

## [decision:autonomy-p6-live-auto-advance] Phase 6 landed — auto-advance is now LIVE (Grade A round2); 2× cross-session base moves resolved | 23:55 UTC | stage:wrapup
Phase 6 (boundary CLI + stage-template auto-branch + session picker + cross-IDE gating) shipped + reviewed A. PLAN stays in-progress (P0-P6 done / P7-P8 remain). This is the phase where the feature functionally turns ON. Three non-obvious things this unit: (1) **gate-ordering**: the Codex k-of-3 voter (+ one Claude) caught that the boundary CLI recorded the `advanced` event BEFORE the prose evaluated the mandatory gate → a gate-stop recorded a phantom advance + double-counted on retry. The other Claude reviewer wrongly called it benign (only modeled crash-retry, not the common gate-stop path). Fix = reorder prose gate-FIRST, CLI side-effect only after gate clears → wiki [[deterministic-side-effect-after-llm-gate]]. (2) **str-enum gotcha**: `str(AtomicStage.X)` is the enum repr not the value, so a stringify made every valid `--current` look unknown — compare against members directly. (3) **multi-session**: the other session's loop landed to main TWICE mid-P6 (d12d91f, then 29f3fff) on the SHARED base repo. finalize stage-merge hit snapshot conflicts both times; resolved by confirming my P6 files are DISJOINT from the other session's (cli/worktree/harness-yaml) + regenerating the conflicting snapshots from the combined state + verifying the combined full suite green. The other session's work was preserved untouched throughout. This validated that the worktree-concurrency model + snapshot-regen-as-conflict-resolution actually works under real parallel pressure — but also reconfirmed the `.hm-loop-active` not-session-scoped gap (a single global marker) as a real follow-up.

## [decision:autonomy-p7-ledger-health] Phase 7 landed (Grade A) — ledger consumers + /hm:health smoke; only P8 (docs+version) left | 00:30 UTC | stage:wrapup
P7 (gate_blocked call-site + /hm:health auto-advance smoke + ADR-009 collision regression) shipped + reviewed A. PLAN in-progress (P0-P7 done / P8 remains). Non-obvious: the only finding (reviewer-A, single-source, Codex+reviewer-B clean) was that smoke_check's `level != "gated"` enablement predicate diverged from the codebase's clamp-unknown-to-gated fail-safe — a typo'd level would raise a false "never fired" alarm. Fixed with a positive allow-list. This is the "absent-case = feature black hole" meta-pattern (CLAUDE.md count:8) in predicate form → wiki [[enabled-predicate-must-match-the-clamp-fail-safe]]. base was stable this phase (other session didn't land), so finalize was clean — no cross-session snapshot conflict for the first autopilot phase since P5.

## [decision:multisession-worktree-phase7-final-landed] Phase 7 (FINAL) landed Grade A — feature complete; 2 data-loss-core tests deferred to a dedicated plan | 13:50 UTC | stage:wrapup
PLAN-multisession-worktree-concurrency Phase 7 (the final phase) shipped: sibling git-dirt probe parity in `enablement_preflight` (AC4), CLAUDE.md feature-branch model rewrite (≤500), MANUAL_CHECKLIST C10. k-of-3 grade A (code 2×P2 cosmetic, security clean, Codex clean). Non-obvious: two `HM_RUN_PARALLEL_SESSION=1` integration tests are pre-existing RED (verified on pristine HEAD by stashing) — `test_post_commit_pop` needs the explicitly-deferred dirname-embedded-UUID refactor, and `test_create_time_prune` exposed a REAL data-loss bug (stage-only finalize writes a landed marker → prune deletes the recovery branch). The clean one-line fix (`if auto_commit:`) was applied then REVERTED because it breaks a deliberately-pinned unit test that encodes the buggy behavior — resolving that is a data-loss-core design decision across two other plans, so it was deferred to a dedicated `/hm:plan` rather than forced into a loop-mode iter (count:3 contamination history; safety-first). Cross-session: the concurrent autopilot session committed P6+P7 mid-iter (main 29f3fff→9849ccf), so I landed via a path-scoped checkout of my 4 disjoint code files + appended memory directly to main's latest wiki/failures (avoiding the wiki.md squash conflict both sessions' memory edits would cause). Wiki: [[stage-only-finalize-must-not-write-a-landed-marker]], [[cross-base-safety-probe-must-cover-every-base-not-just-primary]].

## [decision:autonomy-p8-feature-complete] Phase 8 (FINAL) landed — autonomy feature COMPLETE (P0-P8, all Grade A); version bump deferred | 2026-06-21 00:45 UTC | stage:wrapup
P8 (e2e + docs + the timestamp fix it caught) shipped + reviewed A. PLAN status → complete. The 9-phase autonomy/auto-advance feature is now code/docs/e2e done end-to-end: schema (P1) → marker (P2) → stop-hook (P3) → never-auto guard (P4) → caps (P5) → live auto-advance (P6) → ledger+health (P7) → e2e+docs (P8). Non-obvious P8 turn: the mechanical e2e caught a step-cap bug ALL prior unit tests masked — they fed the same explicit ts to both the marker and the ledger, hiding that the real defaults differ in resolution (marker microsecond isoformat vs ledger second-truncated). Lesson recorded: cross-component producer/consumer contracts need an e2e where both sides use production defaults → wiki [[e2e-acceptance-catches-what-same-input-units-mask]] + [[timestamp-resolution-must-match-across-producer-and-consumer]]. Deferred: the 5-file version bump + CHANGELOG release-sectioning → coordinated 0.31.0 release (user decision; worktree-concurrency also landed FINAL this session, so both features ship together). Multi-session note: the other session's worktree-concurrency feature landed FINAL concurrently; throughout P5-P8 the base moved under my worktrees 4×, each resolved by disjoint-file confirmation + snapshot-regen-as-conflict-resolution + combined-suite verification — the concurrency model held under sustained real parallel pressure.
