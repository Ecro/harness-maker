# Session Log — 2026-06-22

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:spec-tetrad-completeness] Evidence-gated oracle axis added to the spec plugin (schema v2 + property AC + equiv-mutant audit) | 15:30 UTC | stage:wrapup
Full research→plan→execute→review→wrapup pass landed the ORACLE axis for consuming projects. Key non-obvious decisions: (1) the independence claim is deliberately NARROWED to "evidence-gated at authoring", not "proven independent" — an LLM judge cannot prove a golden/property/differential oracle wasn't derived from the impl, so `spec_quality` scores EVIDENCE quality, not the declared `oracle_source` enum (which is gameable). (2) The schema_version migration footgun was the riskiest bit: the pydantic field default MUST be the literal `1`, not the bumped `SCHEMA_VERSION=2` constant, else a constant bump silently re-tags every version-omitting legacy spec as v2 and force-fails it on the new mandatory oracle_source (ADR-006). (3) equiv-mutant exclusion is the loophole risk — unknown survivors stay `pending-review` in the denominator; only a documented rule-id excludes, with a growth guard, so kill-rate can't be inflated by relabeling. (4) Two real defects shipped past my own implementation and were caught in /hm:review: an unguarded `int()` on a graceful-degrade path (code-reviewer P1) and a waiver bypassing the spec-driven gate (Codex-only catch — the heterogeneous k-of-3 voter earned its seat again). harness-maker's own dogfooded harness uses task-driven mode; the work targets the rendered plugin, not the self-tests. Deferred (suggestion-tier): wrapup waiver-enforcement wiring, x-contract mapping drift-guard test, v2-under-stale-v1-template compat fixture.

## [decision:wrapup-waiver-enforcement] Task-driven oracle-waiver advisory wired into /hm:wrapup as tri-state warn (not block) | 16:40 UTC | stage:wrapup
First fully-autopilot-driven follow-up (plan→execute→review→verify auto-advanced; the 60-min time-cap halted before wrapup, so the land+push was a deliberate manual /hm:wrapup). Key decisions: (1) consciously SUPERSEDED spec-tetrad ADR-003's "block before close" with loud-warn + receipt + tri-state status — the user chose visibility over friction for task-driven (the less-strict mode by design). (2) The per-AC evidence ladder moved to a single pure spec_machine.score_ac_oracle_evidence so the new waiver-check CLI and spec_quality's aggregate dim can't drift; a static dup-guard test enforces it. (3) The hard lesson (recorded under partial-guard-on-degrade-path): a "never-raises/exit-0" CLI over untrusted yaml needs a top-level except-Exception floor PLUS honest tri-state — the review found 6 untrusted-input holes (non-UTF-8, non-str fields, non-dict-false-ok, receipt failure, 4096 limit), all fixed with regression tests. (4) The dev_mode render-time branch is a silent-drift risk, so the readiness dev_mode-match signal is MANDATORY (flip-without-re-render guard). Autopilot's time-cap (auto_safe) is doing exactly its job: it bounded the autonomous run to 60 min and cleared its own marker, leaving the human-gated land/push to an explicit command.

## [decision:nonmechanical-ac-binding] enforcement moved /hm:verify → /hm:wrapup (ADR-005 supersedes ADR-004) | 00:00 UTC | stage:wrapup
The PLAN's Round-2 interview chose a /hm:verify hard-block for Production enforcement, but plan-validator + Codex showed that surface was structurally dead: the only available predicate (`spec_drift.scan`) is dev_mode-gated to spec-driven, so a task-driven Production harness — which is harness-maker's OWN preset×dev_mode config — gets an empty report and the gate never fires; and it keyed on recorded `test_ids`, blind to the primary miss (`pending_test=true` + `test_ids=[]`). Redesigned (Round-3 interview) to enforce at /hm:wrapup `find-unbound` (dev_mode-agnostic, convention∪test_ids collect-scan). Then k-of-3 review caught that the *implementation* was fail-OPEN (the can't-run-pytest branch collapsed to a clean pass), the precise inverse of the feature's purpose. Two-layer near-miss — wrong surface, then fail-open on the right surface — both caught by adversarial review before commit. NB: this task has no machine.yaml, so the very enforcement it ships does not self-apply here.
