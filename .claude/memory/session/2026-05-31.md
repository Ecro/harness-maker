# Session log — 2026-05-31

## [decision:agent-model-alias-namespace-boundary] alias for Claude-launch, concrete for Anthropic-API | 01:44 UTC | stage:wrapup
Fixing the pinned-agent-model bug forced a non-obvious split: model ids live in TWO namespaces. Claude Code agent-launch surfaces take version-agnostic ALIASES (`opus`) it resolves at launch; Anthropic-API consumers (aider/Continue foreign configs, SDK `messages.create`) need CONCRETE ids and cannot use aliases. The trap was assuming one map serves both — `CURSOR_MODEL_IDS` holds Cursor's reversed-format ids (`claude-4-7-opus`) that are NOT valid Anthropic API ids, so foreign configs needed a SEPARATE `_FOREIGN_MODEL_IDS`. ADR-006's literal "reuse CURSOR_MODEL_IDS" was wrong; the refinement landed at implementation. Also: the root cause only became a bug when Claude Code fixed #43869 and started respecting a field long treated as decorative — a dormant assumption flipping load-bearing under you.

## [decision:split-template-vs-worktree-execution] template chain runs in base, worktree chain isolated | 13:00 UTC | stage:wrapup
PLAN-latency-worktree-step-preview execute: the template-refactor phases (P1–P5) CANNOT run under worktree isolation. The snapshot infra pins `_HARNESS_MAKER_PKG_ROOT` to the MAIN checkout (`tests/unit/conftest.py:36-40`, `tests/snapshot/regenerate.py:113-117`) — inside a worktree the snapshot tests render the unedited main-checkout templates and pass FALSELY (worktree edits invisible), or forcing `HM_MAIN_CHECKOUT_PATH=<WT>` bakes worktree paths into snapshots ([fail:snapshot-regen-inside-worktree], count 11). The worktree chain (P6–P7, worktree.py) has no such pin and its tests run against the editable install. Decision: split execution by chain — template chain in BASE (no isolation), worktree chain ISOLATED, two sequential passes. This also mooted the parallel-safety churn-dir move (P5↔P7) added during plan re-validation; reverted to keep the `.hm-memory-cache/` reg in P5. Phase 1 (gate0-receipt + inequality-formula partials) landed this way: golden-master byte-identical, REVIEW grade A.

## [decision:two-reviewer-split-caught-suppression-gap] step-manifest loop-suppression needed worktree-aware marker resolution | 13:40 UTC | stage:wrapup
Phase 2 (step-manifest, ADR-003) shipped a `.hm-loop-active` suppression check that said only "skip if the marker exists at the project root." REVIEW ran two reviewers on disjoint lenses: the render/wiring reviewer found it clean (codex frontmatter intact, no StrictUndefined, one-manifest-per-workflow); the BEHAVIORAL reviewer caught a P1 — the loop driver runs with cwd inside `<WT>`, so the naive check misses the root-level marker and would flood every iteration. Fixed in-phase by mirroring `plan.md.j2`'s explicit project-root resolution; the belt-and-suspenders "driver, never print the manifest" instruction (loop reads the workflow file inline, no context boundary) was routed to Phase 3 (which owns loop.md.j2) to avoid scope drift. Non-obvious takeaway: prompt-template changes need a behavioral reviewer, not just a render/syntax pass — and single-source P1s that hit the PLAN's own flagged risk should be acted on, not waved through on the manual-only/consensus technicality.

## [decision:p5-batch-full-parity-extraction] loop slim folds in both prior routed findings | 14:30 UTC | stage:wrapup
Phase 3 (ADR-006) extracted the niche P5-batch mode out of the loop body into `/hm:loop-p5-batch` + a codex `hm-loop-p5-batch` skill (user chose full parity over claude-only), reducing loop.md by 127 lines. It ALSO folded in the two routed review findings from earlier phases: the Phase-1 inequality-formula 4th-copy (now `{% include %}`d → truly single-sourced across research/spec/plan/loop) and the Phase-2 driver-ignore-the-manifest backstop. REVIEW caught a real P1 — the codex skill's `!uv run` bash blocks are inert on Codex (pre-existing: the original section never branched them) — mitigated with a codex-exec note; the deeper multi-line-`python -c` → `Bash()` rework is a broader pre-existing limitation flagged for later. Cross-ref: [[codex-bash-syntax-parity-on-shared-render]]. Decision context for the queued permissions work: user wants the main-session settings.json deny-list to be a whole-deny opt-out (default off, full-trust solo), readiness.py made setting-aware, reviewer agents' rm-deny untouched — sequenced AFTER this P3 wrapup.

## [decision:p6-p7-P1-orphan-branch-content-gate] orphan-branch leak closed with a preserve-biased content-gate; one defense-fix deferred per ADR-001 | 14:15 UTC | stage:wrapup
PLAN-p6-p7-worktree-finalize P1 (the de-risked successor to the parent P6/P7) closed the orphan-branch leak: `prune_stale` at create-time now sweeps leaked worktree-prefix branches whose dir is gone, gated by `_branch_content_in_head` (mirrors `_stash_content_in_head`, path-keyed blob equality, NOT `git branch --merged` — a squash-merged tip isn't a HEAD ancestor). Three non-obvious calls this unit: (1) the fix had to live in prune_stale-at-create, NOT finalize-cleanup, because `cleanup(on_success=True)` runs on the stage-only path too and deleting the branch there would destroy the `wip(execute)` stash-conflict recovery net (P6 finding). (2) REVIEW (grade A, 2 reviewers) deferred N1 — fence-guarding the unfenced sweep against a concurrent finalize re-run — because its fix touches the Layer-4 merge-fence, which ADR-001 explicitly scopes OUT ("leave the shipped defense intact"); recorded as a future Layer-4 hardening item, not papered over. (3) execute had silently skipped P1's named INTEGRATION exit criterion (only the 3 unit tests were authored); caught at wrapup Step 4 and authored then → [fail:workflow] execute-skips-named-integration-exit-criterion. Also fixed pre-existing P3 base-RED (count tests not bumped for loop-p5-batch) in a separate commit 6ea501c → [fail:test] enumeration-tests-not-updated-with-new-rendered-artifact. P2 (merge-fence widening) + P3 (safe polish) remain.

## [decision:p6-p7-P2-merge-fence-widening] fence wraps full critical section; 2 reviewer findings ADR-overruled/deferred | 15:10 UTC | stage:wrapup
PLAN-p6-p7-worktree-finalize P2 (ADR-003) widened the Layer-4 merge fence to wrap exactly `{stash, staged_before snapshot, merge}` (was: merge only) — closing the parallel-stash race where two finalizes `git stash push` the same base concurrently. Key implementation calls: `_capture_pending` moved BEFORE the fence (worktree-side, not base — fencing it only inflates hold-time); `staged_before` extracted to `_snapshot_staged_paths` and captured strictly AFTER the stash (load-bearing for the scope-guard `--allow-dirty-base` exemption); `handed_off` computed after the fence inside `if wt_rc==0`, init False so the rollback finally always has a value; ref-write/cleanup/both pops pinned outside. Phase A.5 FAILed first round (only upper boundary pinned + threaded test used a bare sleep) → added a full boundary-ordering test + a positive-barrier gating test → [fail:test] fence-position-test-pins-only-upper-boundary. REVIEW grade A; two reviewer findings NOT fixed: CN1 (60s fence acquire-timeout < 300s stash hold → 2nd parallel finalize spuriously times out) is an ADR-003 ACCEPTED risk ("keep 60s, raise only if a real run trips") — reviewer lacked ADR context; CR1 (success-mode squash-conflict → stash-apply over un-reset dirty index because the rollback reset is gated `if not auto_commit`) is `out_of_diff` (pre-existing) but THIS diff raised its reachability (conflict now caught instead of crashing) → flagged as recommended near-term follow-up (gate reset on `wt_rc != 0`), kept out of P2 per scope+ADR-001. Also this session: enabled codex_second_opinion (commit aec8fea) — first review with it on; code-reviewer didn't emit codex_status (codex login likely absent → warn-and-proceed silent). P3 (safe polish) remains.

## [decision:p6-p7-PLAN-complete] P3 polish lands; whole worktree-finalize PLAN closed | 15:40 UTC | stage:wrapup
PLAN-p6-p7-worktree-finalize P3 (safe non-defense polish) done → PLAN status flipped to `complete` (P1 orphan-branch + P2 fence-widening + P3 polish all landed). P3: extracted `_porcelain_path` (3 divergent parses → 1) + batched `git check-ignore --stdin` (N→1 subprocess). REVIEW was the cleanest of the three — BOTH reviewers returned ZERO findings. The non-obvious bit a reviewer surfaced: when unifying divergent parsers that feed a SAFETY gate (the dirty-base guard), the clearing argument isn't "they're equivalent" but "the direction they CAN diverge is fail-safe" — here `[1:-1]` strips ≤ old `.strip('"')` → biases toward user-dirt detection → guard fires more not less. Dropped items (flock probe, hot-path move) stayed dropped per ADR-001 across all 3 phases — the de-risk thesis held. Outstanding (NOT in this PLAN): CR1 success-mode rollback reset already fixed separately (6c5a2a6); CR2 (orphan-stash-on-SHA-failure) + CN2 (unfenced-pop race) are pre-existing optional follow-ups; CN1 (60s fence vs 300s stash) is an ADR-003 watch item. Commit stack this session: 6ea501c→2eb6abe→aec8fea→4a1eff4→6c5a2a6→(P3).

## [decision:cr2-cn1-cn2-followups] all 3 remaining review follow-ups fixed; CN1+CN2 supersede ADR-003 | 15:55 UTC | stage:followup
User directed "fix all CR and CNs" after the PLAN closed. Fixed: CR2 — `_stash_base_dirty` now matches its stash by `message in subject` substring (extracted `_match_stash_sha`), so a git `%gs` format quirk no longer raises + orphans the pushed stash (the 32-hex uuid4 suffix keeps substring-match collision-safe). CN1 — merge-fence acquire-timeout raised 60→`_FENCE_TIMEOUT`=360s (= stash 300 + merge 60) so a slow first finalize doesn't spuriously time out a parallel second; **supersedes ADR-003's "keep 60s, raise only if a real run trips."** CN2 — both base-stash pops routed through `_fenced_restore_base_dirty` (fence-wrapped restore + unfenced fallback on fence-acquire failure) to serialize the shared-stash race; **supersedes ADR-003's "pops stay outside the fence."** TDD: 3 RED tests (`_match_stash_sha` substring unit test, fence-timeout≥300 recorder, success-pop-bracketed-by-fence recorder) → GREEN; 108 worktree-suite tests pass. Two-reviewer pass: code-reviewer clean; concurrency-reviewer confirmed NO self-deadlock (merge fence always released before either pop) + NO double-pop (fence raises pre-yield) + cross-session-collision-safe substring. Accepted narrow downside (concurrency P2): the 360s budget lengthens the O_EXCL *secondary*-path stale-lock stall on a SIGKILL'd holder (flock primary auto-releases; self-heals via fallback) — a proper mtime/PID staleness guard on `_excl_lock` is a separate TOCTOU-careful fence-primitive hardening, deliberately deferred not rushed. Updated the `merge-fence-wraps-full-critical-section` wiki entry (the old "ADR-003 keeps 60s" claim was now stale). No CRs/CNs remain.
