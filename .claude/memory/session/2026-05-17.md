# Session — 2026-05-17

## [decision:second-brain-write-failure] fix Second Brain crash + harden interview/CLI surfaces | 03:35 UTC | stage:wrapup
User report: "Obsidian에 저장이 안 되던데". RESEARCH surfaced **three stacked defects** stacked on top of each other, every one blocking writes: (1) `second_brain._load_config` used single-doc `yaml.safe_load` against frontmatter-wrapped `harness.yaml` → every CLI invocation crashed; (2) `folders: []` silently rejected writes + emitted obscure "not under a configured write folder" error; (3) configured `vault_path` pointed at a subfolder of an Obsidian vault that did not yet exist on disk. PLAN locked 8 ADRs across 3 interview rounds (2 user-policy rounds + 1 validator-driven follow-up round); 5-phase implementation landed the helper extraction (ADR-001, staged migration via `docs/followups/io-utils-migration.md`), smart vault detection (`.obsidian/` parent check — accept missing subdir on a real Obsidian vault, reject otherwise — ADR-002), graceful degrade for `folders=[]` (ADR-008: load returns cfg + warns, search returns [] + warns, write/append/patch raise with `/hm:configure` remediation pointer), interview folder enforcement (default `99_HM/{project_id}/`, ADR-003/004), `configure-second-brain` typer subcommand as the slash-command dispatch surface, and a render-based e2e test (`tests/integration/test_second_brain_e2e.py`) that calls `harness_maker.render.render` LIVE so any future renderer-vs-loader drift fails immediately. Review surfaced 3 P1 + 5 P2 findings — all `manual-only` under the 2-reviewer no-overlap consensus rule, but 3 P1 + 2 P2 were genuine correctness defects in the implementation (stale `content_hash` after `--add-folder` blocking reconciler re-render; `load_harness_yaml` returning provenance dict on truncated writes; `..` path traversal in `SecondBrainFolder.path` validator; `--add-folder` not deduplicating; `_validate_vault_existence` resolving relative paths against cwd instead of `harness_root`) so the executor applied them with regression tests. Final state: 1885 unit/integration tests pass, ruff clean, mypy --strict clean, original bug reproducer now returns the expected graceful-degrade warning + empty result instead of `YAMLError`.

## [decision:slash-command-vs-cli-separation] configure.md.j2 update deferred to follow-up | 03:35 UTC | stage:wrapup
PLAN Phase 3 originally listed `templates/commands/hm/configure.md.j2` in scope to wire the slash command through the new `configure-second-brain --check` CLI subcommand (CLAUDE.md item 4 — slash commands cannot conditionally branch on runtime state, must dispatch to CLI). Descoped at execute time because the template change requires snapshot regeneration, and `[fail:test] snapshot-regen-inside-worktree` (count:4) prevents regen from inside the worktree. Workaround for now: the `/hm:configure` slash command's existing Second Brain section continues to ask the user manually — functional but does not use the new JSON guidance protocol. The CLI subcommand IS landed and tested; the template wiring is a separate small PR once the snapshot regen flow is cleaned up. Recorded in Plan Validation table of `work-docs/PLAN-second-brain-write-failure.md` and in `REVIEW-second-brain-write-failure-2026-05-17.md` "Notes for /hm:wrapup".

## [decision:test-fixture-parity] renderer drift killed by paired unit-fixture + live-render e2e | 03:35 UTC | stage:wrapup
The unit test suite passed for months while production crashed on every Second Brain CLI call because `_write_harness_yaml` fixture wrote `harness.yaml` via `yaml.safe_dump` — single-document, no provenance frontmatter. Real renderer always wraps the body in a frontmatter block. Lesson promoted to wiki + failures. Defense-in-depth response: update the unit fixture to inject the frontmatter AND add a render-based e2e test that invokes `harness_maker.render` live (no snapshot-pinning of the rendered bytes — that would freeze the bug into the regression net). Validator critique W8 explicitly called out this trap; the e2e tests now assert on `_load_config` output rather than on rendered bytes.
