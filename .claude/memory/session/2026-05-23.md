# Session log — 2026-05-23

## [decision:auto-feedback-2026-05] opt-in feedback module shipped 0.24.0 | 08:50 UTC | stage:wrapup

PLAN-auto-feedback-2026-05 — maintainer-dogfooding opt-in feedback drafts. **Key non-obvious decision:** in-band LLM judgment in the 2 dispatcher wrapper templates (`atomic_command.md.j2` + `workflow_command.md.j2`), not in stage bodies and not via out-of-band `anthropic.Anthropic()` call. Why: (1) stage bodies are pre-rendered by Python in `synthesize._atomic_command_files` with a fixed context dict that does NOT include `feedback_enabled` — inserting the conditional there would silently no-op (validator C1 catch); (2) out-of-band LLM call would open a socket → break `tests/unit/test_no_network.py` ADR-005 positive obligation. Wrapper-layer placement keeps `feedback_enabled` injection to a single line in `synthesize.synthesize()` (lines 709-729 FileEntry context merge) and reaches all 7 atomic stage outputs + every fused workflow command via the existing wrapper render pipeline. Surprise from REVIEW round 1: code-reviewer (single-source, manual-only consensus tag) flagged a P1 path-traversal in `draft_writer.write()` — `task_slug` was the only string field embedded in the on-disk filename without a validator; `file_paths` had `.claude/` enforcement, `error_message` had `_SECRET_PATTERNS` redaction, but `task_slug` was overlooked. Pattern: when one model's fields collectively contribute to a filename, the invariant is "every path-contributing field has a validator", not "some fields do" — relying on the existence of other validators is the footgun. Fix shipped same round + 3 regression tests + memory entry `[fail:design] path-traversal-via-unsanitized-slug-in-fstring count:1`.

## [decision:loop-mid-stop-and-review-skip] Phases 1-4 shipped via 4-phase dogfooding + cherry-pick recovery | 21:30 UTC | stage:wrapup

PLAN-loop-mid-stop-and-review-skip Phases 1-4 (of 6) — Gate 0 receipt mechanism closes the 2026-05-22 silent-review-skip failure mode. **Key non-obvious decision sequence:** (1) chose to dogfood the PLAN across 4 separate `/hm:exec-rev` turns (one per phase) rather than one monster turn — because the PLAN itself exists to prevent silent-stage-skipping that monster turns are prone to. (2) Phase 3 review caught a self-referential PLAN drift: my EXPECTED_STAGES table named `plan-exec-rev` (3-stage) which didn't exist in the registry yet; the registry has `plan-exec-rev-wrap` (4-stage). PLAN ADR-002 was correct in intent (add 3-stage variant) but Phase 3 had to provisionally remove the reference until Phase 4 landed the registry entry — then re-add. (3) Catastrophic mid-recovery: after Phase 4 partial work, base repo entered broken state (4 unpopped finalize stashes from never running `/hm:wrapup` between phases; subsequent `stage-only` finalize failed on stash because of accumulated `UU` conflicts; `fail` finalize fallback deleted the worktree). All 4 phases' source code appeared LOST. **Recovery path that worked:** `git fsck --unreachable --no-reflogs` + `git reflog --all` surfaced 5 WIP commits on branch refs (each `worktree finalize` creates a `wip(execute): capture uncommitted work` commit on its branch before attempting cleanup, separate from the stash). Cherry-pick T0555Z → T0608Z → T0812Z → T1141Z in order onto current `d92c6b3` HEAD, resolve snapshot conflicts with `--ours` (final regen reconciles), apply Phase 4 missing piece (Gate 0 table re-add), remove xfail markers, regen snapshots, soft-reset all 4 WIP commits into a single staged blob, single commit. **Permanent lesson:** the harness's stage-only finalize + wrapup post-commit-pop handshake silently decays when wrapup is skipped between exec-rev turns. Wiki entry `[wiki:pattern] loop-mechanical-receipt-gate` + failure entry `[fail:design] finalize-stash-queue-no-wrapup-handshake count:1` + follow-up PLAN candidate `PLAN-finalize-stash-queue-guard` (block new worktree create when N ≥ 2 unpopped finalize stashes exist). Phases 5-6 (state-pin file + docs/skill updates) explicitly deferred.
