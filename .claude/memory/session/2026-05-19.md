# Session Log — 2026-05-19

## [decision:readme-one-prompt-autoinstall] paste-prompt model preserved; /reload-plugins accepted as irreducible step | 08:30 UTC | stage:wrapup

User flagged that README's "one-prompt" bootstrap pasted into Claude redirected the typing back to the user (asking them to type `/plugin install ...` slash commands). Three plan rounds locked in: (1) paste model preserved over a shell one-liner alternative — user wants paste UX; (2) verdict on whether `/reload-plugins` post-install auto-triggers the next assistant turn was DEFERRED, conservative `manual-enter-required` wording shipped (harmless if reload auto-triggers, necessary if it doesn't); (3) all 3 IDEs auto-install via Bash in same paste, but per-IDE step budget table shipped honestly (Cursor still needs Reload-Window, Codex may need restart). Non-obvious: built-in slash commands (`/plugin`, `/reload-plugins`) cannot be assistant-fired via Skill (explicitly excluded by the tool description) nor via Bash (subshell can't reach parent session). This forced the `Bash:`-prefix-in-runbook design — see [[wiki:gotcha:readme-one-prompt-bash-not-slash]].

## [decision:phase-0-deferred-conservative-verdict] empirical verification skipped, conservative wording shipped | 08:35 UTC | stage:execute

Phase 0 of PLAN-readme-one-prompt-autoinstall called for n=3 nested `claude --output-format=stream-json` trials with HOME=$(mktemp -d) isolation to verify whether `/reload-plugins` auto-triggers next assistant turn. Skipped per user lock-in because (a) Sub-step 0a (isolation gate) involves running real `claude plugin install` against potentially-real `~/.claude/plugins/` if HOME-override doesn't govern config root; (b) 0b consumes Claude subscription quota and the nested-stream-json injection harness is fragile. Chose `manual-enter-required` verdict because it is a strict superset of correctness: "Type `/reload-plugins`, then press enter once" is harmless when reload auto-triggers and necessary when it doesn't. Future maintainer can run Phase 0 protocol on a throwaway machine — protocol preserved in work-docs/PHASE0-readme-one-prompt-verification.md.

## [decision:worktree-finalize-118-conflicts-manual-resolution] | 09:10 UTC | stage:execute

During execute Step 5 `worktree finalize stage-only`, the squash-merge into main produced 118 unmerged files (117 `tests/e2e/sandbox-plugin-test/*` fixtures regenerated by a parallel main commit + 1 CHANGELOG.md against a new 0.17.0 release entry). Worktree did not edit those fixtures. Resolution: `git checkout --ours -- <fixture-dirs>` (118 fixtures take main's version, no real conflict in intent), then manual CHANGELOG merge keeping both my `[Unreleased]` line + main's 0.17.0 entry. After resolution: manual cleanup `git worktree remove --force <WT>; git branch -D <wt-branch>` because the finalize CLI does not retry-after-resolution. See [[fail:design:worktree-finalize-stage-only-merges-against-shifted-main]].

## [decision:oss-readiness-audit-launch-floor] 3-layer launch readiness landed; SHA pin + 4 phases deferred to user | 18:50 UTC | stage:wrapup

PLAN-oss-readiness-audit's 11 phases split across three layers (trust floor / positioning surface / discovery + announcement). This wrapup ships layer 1 + layer 2: PR CI restored after 565d7ce had removed it the day after the repo went public; 6 community files (CONTRIBUTING/CoC/SECURITY/3× ISSUE_TEMPLATE/PR template); Dependabot config; PRIVACY.md + AST-walk test (caught 2 P1 doc-vs-code mismatches during review: `OverrideRecord.source` had three fabricated values, `IntentMissEvent.trigger` had two fabricated values — the AST-walk test only covers field names, not Literal values, so the divergence would have shipped silently); README hero + Stability section + comparison rewrite (now category-axis only, no named competitors per ADR-007/012). Layer 3 (Phases 8–11) is user-action — gh api PVR toggle + Discussions ON + repo description + marketplace submissions + Show HN. Non-obvious: ran into the snapshot-regen-inside-worktree pattern AGAIN (count:6) because `uv run pytest -x` mutated `tests/e2e/sandbox*/.claude/**` fixtures inside the worktree; `git checkout HEAD -- tests/e2e/sandbox*/` reverted before commit. Separate concern: worktree finalize created an unrelated d6d522b commit ("fix(synthesize): propagate user locale into pre-rendered stage bodies") from pre-existing uncommitted work in the main tree; user confirmed "리셋하지 말고 잘 머지해. 필요한 반영이야." — d6d522b stays as a separate commit, this wrapup commit layers on top. Outstanding before first external PR: replace `actions/dependency-review-action@v4` mutable tag with SHA at `.github/workflows/ci.yml:59` — LLM cannot verify SHAs without network at write time, so the TODO marker is the safe failure mode. See [[fail:design:llm-cannot-verify-action-sha-at-write-time]] and [[wiki:pattern:oss-launch-readiness-three-layer]].

## [decision:test-fidelity-gap-layer-1-shipped] boundary-parse layer pins 5 file types; production templates clean on first run | 21:00 UTC | stage:wrapup

PLAN-test-fidelity-gap Layer 1 shipped: `tests/integration/test_boundary_*.py` × 5 (hooks_json + codex_toml + harness_yaml + cursor_mdc + settings_json) + meta-test + shared helpers + conftest marker registration. 49/49 pass in 3.66s (~15× faster than the original ~3-min estimate — recorded in PLAN frontmatter and ADR-002 amendment). Critical decisions: (a) ADR-002 chose `cli.make`-via-CliRunner over direct `synthesize() + render()` because CliRunner mirrors the actual user-boundary path already pinned byte-identical-idempotent by `test_render_idempotent_byte_identical`; (b) ADR-003 elected **fully advisory** CI (no PR merge block) — maintainer prioritized avoiding false-positive blocks; (c) ADR-004 v2 placed the safety net in `release.yml` `boundary-advisory` job (no secret needed because pure parser tests have zero LLM call) AND the CLAUDE.md release runbook — both visible failure paths. **Honest test outcome**: ZERO production-template issues found on first LIVE render — every recent fix referenced in RESEARCH (yaml-empty-list-renders-null, phantom-key, toml-section-header-injection, yaml-colon-in-unquoted-description) was already closed; Layer 1 confirms them empirically rather than discovers them. Real catches happened in two other places: (1) my own parser logic during dev (the `.mdc` `lstrip("-")` bug; the `harness.yaml` `targets is not None` not catching explicit null; the meta-test regex needed adjustment after seeing pytest's actual collect-only output format) — all caught in 1-2 iter; (2) review caught TWO P1 bugs in my OWN release.yml additions — `tee | $?` always returns 0 not pytest's exit (consensus-passed P1 between code-reviewer + security-reviewer; without `${PIPESTATUS[0]}` every release would silently misreport PASS) AND `gh release view` without an error guard would overwrite Release notes on transient API failure (manual-only P1). Both fixed in review round 2. Meta-irony: the Layer-1 advisory infrastructure I wrote was itself broken in two ways, but the review stage caught both before merge — living proof that the multi-layer defense matters. Non-obvious code-reviewer hallucination: it confidently claimed my meta-test regex was broken because pytest 9.x supposedly doesn't emit file-summary lines; I verified against the actual test output (the meta-test PASSED earlier in the same execute stage) and dropped the finding as false positive. See [[fail:review:reviewer-hallucinates-tool-output-format]]. Production code in `src/harness_maker/` was 0 lines changed — release-safe for the 0.17.0 user-base. See [[wiki:pattern:boundary-parse-test-layer]] + [[wiki:gotcha:pipestatus-vs-dollar-question-mark]].

## [decision:0.17.1-prep-+-personalization-positioning-+-codex-smoke-design] | 22:30 UTC | stage:wrapup

두 번째 사이클 (`/hm:research` → `/hm:plan` → `/hm:execute` → `/hm:wrapup`) — pre-launch-validation-strategy. RESEARCH 가 surface 한 핵심 gap: LLM code review 의 BugMatch ~60% + 144 test 중 단 1개만 real `claude` 호출 (그것마저 `--ci` interactive 우회) → Show HN 전 functional validation 부족. PLAN 12 ADRs, 10 phases, NEEDS_REVISION_RESOLVED. **Wrapup 이 실제로 ship 한 것**: (1) PyPI ↔ main 2버전 drift 해소를 위해 0.17.1 **사전** cut — 5-file version sync (3 manifests + pyproject.toml + __init__.py) + uv.lock 자기참조 일치 + CHANGELOG 0.17.1 entry (ADR-004); (2) 사용자가 lock-in 한 **per-project personalization** 차별점 positioning 을 README 영문/한글 hero + GitHub About sidebar (`gh repo edit`) 3 곳에 동시 박음 — "user 가 가장 먼저 보는 문구" 의 정의에 충실; (3) Codex smoke (Phase 6) MANUAL_CHECKLIST.md 5-step + BLOCK/DEFER threshold **사전 commit** (ADR-003) — 실행 사용자 deferred 지만 design 은 lock-in. Phase 1+2 citation 추가는 PLAN 본문에 inline 으로 처리. **Drift gate accept-as-risk**: REVIEW-pre-launch-validation-strategy.md 부재 — 본 work-unit 은 docs+config+test-checklist (no production code change, lint+mypy GREEN, execute Phase D 2,258 passed) 이므로 /hm:review 생략 + commit body 에 명시 기록. 같은 날 두 번째 `snapshot-regen-inside-worktree` recurrence (count:6 → 7) → pending-proposal "post-finalize-snapshot-regen-hook" automation 우선순위 격상. Phase 0 tag push 부터 Phase 8 Go/NoGo 까지 모두 사용자 manual action 으로 deferred — 본 commit 직후 `git tag -a v0.17.1 + git push origin main v0.17.1` 부터 시작. See [[wiki:positioning:per-project-personalization-hero-differentiator]].

## [decision:memory-md-operations-retrieval-replacement] | 22:10 UTC | stage:wrapup

세 번째 사이클 (research → plan → exec-rev → wrapup) — `.claude/memory/{wiki,failures}.md` 의 운영방식 비판적 검토 + retrieval-only 해결책. RESEARCH 가 surface 한 3-layer (format gate / lifecycle pass / retrieval rewrite) 중 사용자가 **Retrieval-only (C)** 만 lock-in (Round 1). 그 다음 mechanism question 에서 (Round 2) `harness_maker.llm_judge.AnthropicJudgeClient` docstring 의 "Not used inside Claude Code subscription — Layer 2 evaluation runs prompt-natively" 가 결정적 prior art → **hybrid (Python lexical pre-filter + 실행 중 Claude turn 의 인라인 rerank)** 채택. Plan-validator NEEDS_REVISION 6 warnings + 2 suggestions 모두 inline revision 으로 흡수 (rerank execution surface = ADR-005, no tier-quota = ADR-006). **Execute 단계 surprise 1**: PLAN 이 "research / plan / spec 세 stage 모두 first-60-lines + grep" 가정했지만 실제는 research 만. plan 은 memory loader 없음, spec 은 `rg "<key terms>"` 다른 모양. PLAN intent (세 stage 통일) 를 따라 replace / insert / replace-subset 로 분기 — 이 drift 자체를 wiki 에 design entry 로 박음 (`[[wiki:design:plan-vs-reality-template-loader-drift]]`). **Execute 단계 surprise 2**: test-reviewer 가 P0 으로 flag 한 `_REPO_ROOT = parents[2]` 가 worktree 에선 wrong 이라는 주장이 empirically 틀림 — `.claude/memory/wiki.md` 가 git-tracked 라 worktree 에도 존재 (line 258 의 boundary-parse-test-layer 확인). Test-reviewer override 는 evidence-backed 일 때만 정당화됨 (P0 override 는 매우 보수적으로). **Review 단계 surprise**: 3 reviewer × 0 consensus-passed → Grade A 자동, 하지만 security-reviewer single-source P1 × 2 (fence-attribute breakout + entry-body fence-close stored prompt-injection) 는 실제 위험 → orchestrator-judgment 로 인라인 fix + 3개 regression test 추가. grade-gate "STOP at threshold" 와 "manual-only P1 도 진짜 위험이면 고친다" 의 균형: 자동 loop 진입은 하지 않되, orchestrator 가 명시적으로 (REVIEW 문서에 documented) 적용 — 이 pattern 은 grade-gate 의 의도 (formal loop 의 cost 절약) 와 quality (실제 P1 무시 안 함) 둘 다 보존. **Push 직전 verify**: 2263 pytest passed, ruff/format/mypy 모두 green. CHANGELOG 만 update (TECH_SPEC/ARCHITECTURE/HOW-IT-WORKS 는 internal-infra change 라서 cross-ref 불필요 — wrapup template "use judgment" 적용). See [[wiki:pattern:memory-retrieve-hybrid-lexical-claude-rerank]] · [[wiki:gotcha:memory-md-prompt-injection-fence-close]] · [[wiki:design:plan-vs-reality-template-loader-drift]].

## [decision:model-routing-review-multi-agent-payoff] | 23:15 UTC | stage:wrapup

네 번째 사이클 (`/hm:plan` → 6 `/hm:execute` phases → `/hm:review` → `/hm:wrapup`). 0.15.0 model routing audit, 5 ADRs / 6 phases (Inventory → Resolver/Schema audit → Render correctness + IDE parser spec → Multi-agent /hm:review → Critical+High fixes + regression → Verify). 가장 중요한 결과: **security-reviewer 가 plan-author 가 Phase 2 에서 "verified safe" 로 표시한 path 에서 실제 injection vector 를 찾음** — Pydantic v2 `model_copy(update=...)` 가 field validators 를 우회한다는 documented Pydantic 동작이 `interview.answers_from_harness_yaml`:769/772 에서 `default_model` charset 검증을 silently bypass. Plan-author 의 Phase 2 Finding R-6 ("regex correctly anchored, validator complete") 는 regex 자체는 맞았지만 그 regex 가 load path 에서 절대 실행되지 않는다는 점을 놓침 — R-6 **RETRACTED**. ADR-005 의 multi-agent payoff 가 작동한 정확한 사례. **Validator C-2 risk materialization**: R3 에서 accept-as-risk 한 prose-discipline-only audit hole 이 "feared direction" (author bias toward known candidates) 의 정반대 — author MISS 로 발현, multi-agent 가 정정. Sealed pre-commitment artifact (validator C-2 의 권장 fix) 였다면 detection mechanism 이 audit trail 에 explicit. 다음 PLAN 부터 default 적용 검토. **Phase B false-RED escape**: test_mv3 의 정규식 `[a-zA-Z0-9_.:-]+` 이 "None" 을 알파벳적으로 match 하여 RED gate 가 false-PASS. Test-reviewer Round 1 의 widen-the-assertion 권고를 받아들였더니 wider form 이 bug-value 를 admit 하는 trade-off 가 발생. YAML-parse + explicit None check 로 inline 재작성. Lesson wiki 에 박음. **Wrapup scope decision**: README/TECH_SPEC/ARCHITECTURE 업데이트 안 함 — internal-infra fix (no user-visible surface change, docs/HOW-IT-WORKS.md §7a Agent Models 가 이미 post-fix 동작 기준으로 적혀있음 → 이번 fix 가 docs 의 약속을 실제로 충족시킴). CHANGELOG 에만 entry, wrapup template "use judgment" 적용. **Push 직전 verify**: integration test (INTEGRATION=1, 5/5 5.04s) + full unit (exit 0) + ruff check / format-check (이번엔 둘 다 명시 실행, `[fail:lint:wrapup-final-verify-skips-ruff-format-check]` count:2 의 가드) + mypy --strict (87 files clean). See [[wiki:pattern:multi-agent-review-catches-single-author-blind-spots]] · [[fail:design:pydantic-model-copy-bypasses-field-validators]] · [[fail:render:jinja-is-defined-returns-true-for-none]] · [[fail:test:regex-permissive-on-literal-alpha-tokens]].

## [decision:readme-domain-packs-accuracy-fix-plus-multi-session-worktree-leak] | 23:55 UTC | stage:wrapup

다섯 번째 사이클 (`/hm:plan` → `/hm:execute` → `/hm:review` → `/hm:wrapup`) — README 의 "Domain packs: --add-domain python (or node, rust) grafts standards, agents, and skills" 약속이 shipped 와 어긋남을 사용자가 audit 으로 잡음. 실제는 (a) `_SHIPPED_DOMAIN_SAMPLES = frozenset({"python"})` — python 만 sample, (b) standards-only inline into 5 reviewers, (c) agents/skills graft 메커니즘 자체 부재. 4 옵션 (README 정직화 / node·rust pack 추가 / full graft infra / 조사 더) 중 사용자 lock-in: 최소작업 README 정직화. PLAN skip-heuristic (single-file, no arch/contract change, reversible) 적용 → interview/validator/multi-reviewer 전부 skip 정당화. **wrapup pre-flight 가 잡은 silent index drift**: `/hm:review` 시점엔 `M README.md` 만 staged 였는데 wrapup 시작 시 index 가 5개 unrelated 파일 (CLAUDE.md / second_brain.py / worktree.py / 2 test) staged + README de-staged 로 silently 변경됨. 동시에 `7f00ace fix: model-routing review fixes (...)` 가 main HEAD 에 자동 생성된 commit 으로 발견 — `worktree finalize stage-only` 의 dirty-base 처리 부산물 (LLM-generated 진짜같은 conventional-commit message 라 contamination 감지가 어려움). 사용자 결정: option A (index reset + README 만 깔끔하게 commit). 7f00ace 는 unwind 안 함 (실제 작업물 내용 + 자체 wrapup 으로 다시 commit 하기엔 cost > value). **Pattern increment**: `[fail:design] worktree-finalize-pulls-orphan-wip-into-main` count 1 → 2, body 에 (a) dirty-base + finalize-stage-only 가 침묵 sweep 으로 LLM-generated commit 만든다, (b) review→wrapup 사이 index 가 무행위로 mutate 한다 두 sub-pattern 추가. **Required follow-up PLAN (별도)**: worktree create preflight ABORT + finalize scope-guard + CLAUDE.md multi-session 섹션 + per-session marker. M-1 P3 stylistic hedge ("today" / "Today") 도 wrapup 직전 제거 — `python` is the only sample that ships pre-filled 형태로. **No verification 실행**: docs-only diff (markdown 만), 7f00ace 의 코드 변경은 이미 그 자체로 정상 commit 됨. push 시 7f00ace + 본 commit 둘 다 origin 으로. See [[wiki:architecture:domain-packs-are-standards-only-not-agents-skills]] · [[fail:design:worktree-finalize-pulls-orphan-wip-into-main]].

## [decision:untested-trio-fix-cleanup-prefix-plus-sb-timestamps] | 14:55 UTC | stage:wrapup

여섯 번째 사이클 (`/hm:plan` → `/hm:execute` → `/hm:review` → `/hm:wrapup`). PLAN-untested-trio-review-2026-05-19 의 9-priority 중 P0-1 / P0-2 / P1-3 사용자 lock-in (R1 에서 P0-1 drop → 실제 fix 는 P0-2 + P1-3 두 개). 가장 중요한 결과: **plan-validator 가 ADR-006 의 "append/patch 도 updated bump" 가 실제론 NO-OP 라는 점을 catch** — pre-fix `append_note` (`second_brain.py:158-171`) 와 `patch_note` (`:174-189`) 가 fm dict 를 mutation 했지만 disk 에는 `existing + text` / `existing.replace(...)` 라는 raw concat 으로 write → mutation 이 disk 에 반영 안 됨. PLAN 의 "tests pass" 가 disk-state 가 아닌 in-memory mutation 만 검증해서 silent ship 했을 위험. ADR-009 로 frontmatter re-serialize (`_format_note(fm, new_body)`) 추가 + `patch_note` matching 을 body-only 로 narrow (frontmatter substring patching 은 undefined). Phase 2 가 9 tests / 209 LOC 으로 늘어남. **Validator C-2 mitigation**: PLAN ADR-005 가 "orphan worktrees untouched" 로 defer 했지만 prefix filter 추가 후 unmatched orphans 가 cleanup_all 의 시야에서 사라질 위험. Phase 0 exit 에 `git worktree list --porcelain` enumerate + assertion (모든 registered basename 이 candidate `_OWNED_PREFIXES` 와 매치) 으로 가드. Live 에선 trivially passed (registered = main only). **Execute surprise**: `worktree finalize stage-only` 가 main 의 staged README.md 변경과 충돌 (`git merge --squash` 이 working tree update 시도하다 conflict). git stash push → retry finalize → stash pop 으로 해결. 부산물: stash pop 이 README.md 를 unstaged 로 복원 (원래는 staged 였음). 또 다른 부산물: **Bash 도구의 cwd 가 subshell 별로 fresh 라는 docs 와 달리 worktree 경로로 silently 지속** — `git status` / `git stash` 가 worktree 컨텍스트에서 실행되어 main 의 staged 변경을 "no local changes" 로 잘못 report. 명시적 `(cd /home/noel/harness-maker && ...)` subshell 로 escape. **Review surprise**: code-reviewer 가 M1 (CLAUDE.md 라인 267 stale 2-prefix list) 를 single-source 로 flag. PLAN exit-criterion grep 이 라인 90 만 검사했어서 통과해버린 doc drift. M1 manual-only → 별도 1-line Edit follow-up 으로 적용 → 양쪽 라인 4-prefix 일치 확인. **wiki entry added**: `[[wiki:convention] plan-exit-grep-must-cover-all-doc-occurrences]` — 다음 PLAN 부터 doc-edit phase exit 은 "new text appears" + "old text 어디에도 없다" 둘 다 검증. **Skip 정당화**: 새 failure entry 안 만듦 — cwd 지속, sandbox drift, stash dance 모두 알려진 패턴이거나 single-occurrence (count 1) 라서 wiki convention 으로 forward-looking 만. See [[wiki:convention:plan-exit-grep-must-cover-all-doc-occurrences]].
