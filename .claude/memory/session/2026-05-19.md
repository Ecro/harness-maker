# Session Log — 2026-05-19

## [decision:readme-one-prompt-autoinstall] paste-prompt model preserved; /reload-plugins accepted as irreducible step | 08:30 UTC | stage:wrapup

User flagged that README's "one-prompt" bootstrap pasted into Claude redirected the typing back to the user (asking them to type `/plugin install ...` slash commands). Three plan rounds locked in: (1) paste model preserved over a shell one-liner alternative — user wants paste UX; (2) verdict on whether `/reload-plugins` post-install auto-triggers the next assistant turn was DEFERRED, conservative `manual-enter-required` wording shipped (harmless if reload auto-triggers, necessary if it doesn't); (3) all 3 IDEs auto-install via Bash in same paste, but per-IDE step budget table shipped honestly (Cursor still needs Reload-Window, Codex may need restart). Non-obvious: built-in slash commands (`/plugin`, `/reload-plugins`) cannot be assistant-fired via Skill (explicitly excluded by the tool description) nor via Bash (subshell can't reach parent session). This forced the `Bash:`-prefix-in-runbook design — see [[wiki:gotcha:readme-one-prompt-bash-not-slash]].

## [decision:phase-0-deferred-conservative-verdict] empirical verification skipped, conservative wording shipped | 08:35 UTC | stage:execute

Phase 0 of PLAN-readme-one-prompt-autoinstall called for n=3 nested `claude --output-format=stream-json` trials with HOME=$(mktemp -d) isolation to verify whether `/reload-plugins` auto-triggers next assistant turn. Skipped per user lock-in because (a) Sub-step 0a (isolation gate) involves running real `claude plugin install` against potentially-real `~/.claude/plugins/` if HOME-override doesn't govern config root; (b) 0b consumes Claude subscription quota and the nested-stream-json injection harness is fragile. Chose `manual-enter-required` verdict because it is a strict superset of correctness: "Type `/reload-plugins`, then press enter once" is harmless when reload auto-triggers and necessary when it doesn't. Future maintainer can run Phase 0 protocol on a throwaway machine — protocol preserved in work-docs/PHASE0-readme-one-prompt-verification.md.

## [decision:worktree-finalize-118-conflicts-manual-resolution] | 09:10 UTC | stage:execute

During execute Step 5 `worktree finalize stage-only`, the squash-merge into main produced 118 unmerged files (117 `tests/e2e/sandbox-plugin-test/*` fixtures regenerated by a parallel main commit + 1 CHANGELOG.md against a new 0.17.0 release entry). Worktree did not edit those fixtures. Resolution: `git checkout --ours -- <fixture-dirs>` (118 fixtures take main's version, no real conflict in intent), then manual CHANGELOG merge keeping both my `[Unreleased]` line + main's 0.17.0 entry. After resolution: manual cleanup `git worktree remove --force <WT>; git branch -D <wt-branch>` because the finalize CLI does not retry-after-resolution. See [[fail:design:worktree-finalize-stage-only-merges-against-shifted-main]].

## [decision:oss-readiness-audit-launch-floor] 3-layer launch readiness landed; SHA pin + 4 phases deferred to user | 18:50 UTC | stage:wrapup

PLAN-oss-readiness-audit's 11 phases split across three layers (trust floor / positioning surface / discovery + announcement). This wrapup ships layer 1 + layer 2: PR CI restored after 565d7ce had removed it the day after the repo went public; 6 community files (CONTRIBUTING/CoC/SECURITY/3× ISSUE_TEMPLATE/PR template); Dependabot config; PRIVACY.md + AST-walk test (caught 2 P1 doc-vs-code mismatches during review: `OverrideRecord.source` had three fabricated values, `IntentMissEvent.trigger` had two fabricated values — the AST-walk test only covers field names, not Literal values, so the divergence would have shipped silently); README hero + Stability section + comparison rewrite (now category-axis only, no named competitors per ADR-007/012). Layer 3 (Phases 8–11) is user-action — gh api PVR toggle + Discussions ON + repo description + marketplace submissions + Show HN. Non-obvious: ran into the snapshot-regen-inside-worktree pattern AGAIN (count:6) because `uv run pytest -x` mutated `tests/e2e/sandbox*/.claude/**` fixtures inside the worktree; `git checkout HEAD -- tests/e2e/sandbox*/` reverted before commit. Separate concern: worktree finalize created an unrelated d6d522b commit ("fix(synthesize): propagate user locale into pre-rendered stage bodies") from pre-existing uncommitted work in the main tree; user confirmed "리셋하지 말고 잘 머지해. 필요한 반영이야." — d6d522b stays as a separate commit, this wrapup commit layers on top. Outstanding before first external PR: replace `actions/dependency-review-action@v4` mutable tag with SHA at `.github/workflows/ci.yml:59` — LLM cannot verify SHAs without network at write time, so the TODO marker is the safe failure mode. See [[fail:design:llm-cannot-verify-action-sha-at-write-time]] and [[wiki:pattern:oss-launch-readiness-three-layer]].

## [decision:test-fidelity-gap-layer-1-shipped] boundary-parse layer pins 5 file types; production templates clean on first run | 21:00 UTC | stage:wrapup

PLAN-test-fidelity-gap Layer 1 shipped: `tests/integration/test_boundary_*.py` × 5 (hooks_json + codex_toml + harness_yaml + cursor_mdc + settings_json) + meta-test + shared helpers + conftest marker registration. 49/49 pass in 3.66s (~15× faster than the original ~3-min estimate — recorded in PLAN frontmatter and ADR-002 amendment). Critical decisions: (a) ADR-002 chose `cli.make`-via-CliRunner over direct `synthesize() + render()` because CliRunner mirrors the actual user-boundary path already pinned byte-identical-idempotent by `test_render_idempotent_byte_identical`; (b) ADR-003 elected **fully advisory** CI (no PR merge block) — maintainer prioritized avoiding false-positive blocks; (c) ADR-004 v2 placed the safety net in `release.yml` `boundary-advisory` job (no secret needed because pure parser tests have zero LLM call) AND the CLAUDE.md release runbook — both visible failure paths. **Honest test outcome**: ZERO production-template issues found on first LIVE render — every recent fix referenced in RESEARCH (yaml-empty-list-renders-null, phantom-key, toml-section-header-injection, yaml-colon-in-unquoted-description) was already closed; Layer 1 confirms them empirically rather than discovers them. Real catches happened in two other places: (1) my own parser logic during dev (the `.mdc` `lstrip("-")` bug; the `harness.yaml` `targets is not None` not catching explicit null; the meta-test regex needed adjustment after seeing pytest's actual collect-only output format) — all caught in 1-2 iter; (2) review caught TWO P1 bugs in my OWN release.yml additions — `tee | $?` always returns 0 not pytest's exit (consensus-passed P1 between code-reviewer + security-reviewer; without `${PIPESTATUS[0]}` every release would silently misreport PASS) AND `gh release view` without an error guard would overwrite Release notes on transient API failure (manual-only P1). Both fixed in review round 2. Meta-irony: the Layer-1 advisory infrastructure I wrote was itself broken in two ways, but the review stage caught both before merge — living proof that the multi-layer defense matters. Non-obvious code-reviewer hallucination: it confidently claimed my meta-test regex was broken because pytest 9.x supposedly doesn't emit file-summary lines; I verified against the actual test output (the meta-test PASSED earlier in the same execute stage) and dropped the finding as false positive. See [[fail:review:reviewer-hallucinates-tool-output-format]]. Production code in `src/harness_maker/` was 0 lines changed — release-safe for the 0.17.0 user-base. See [[wiki:pattern:boundary-parse-test-layer]] + [[wiki:gotcha:pipestatus-vs-dollar-question-mark]].
