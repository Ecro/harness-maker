# Session log — 2026-06-02

## [decision:spoton-codex-rm-stash-rootcause] split 3 spoton symptoms into 1 shipped fix + 2 local-only remediations | 14:30 UTC | stage:wrapup
Three symptoms in `~/spoton` (codex second-opinion skipped, `rm` permission denied, recurring worktree stash) root-caused to three *different* classes — the key insight that drove the whole plan. (1) codex skip = a **live template bug** (`tools:` omits `Bash` so the `Bash(codex exec:*)` permission is inert — the hard-gate gotcha), fixed in harness-maker source and shipped via 0.28.5. (2) `rm` deny + (3) stash = **"upgrade-can't-undo"**: `render._merge_permissions` unions deny lists by design so a retired baseline `Bash(rm:*)` survives forever; `.claude/observability/` was committed at 0.23.2 before being gitignored so it's tracked-yet-ignored and the finalize stash keeps sweeping it. Neither (2) nor (3) is fixable by re-rendering — both were remediated **manually in spoton only** (commit `4cfae0b`), deliberately NOT by hardening harness-maker (a subtractive-deny mechanism + pathspec-scoped finalize stash are precedent-setting changes deferred to their own PLANs). ADR-002.

## [decision:dogfood-rerender-skipped] dogfood .claude/ re-render is local-only, out of the shipped change | 14:32 UTC | stage:wrapup
harness-maker gitignores its own `.claude/` (and `work-docs/`), so the dogfood agents' re-render neither ships nor enters the commit. The 0.28.5 commit is exactly the 12 source files (3 templates + 2 tests + 5 version files + uv.lock + CHANGELOG) plus the tracked memory updates. To give harness-maker's *own* codex agents Bash, run `uv run harness-maker make . --update` anytime — it's a local convenience, not release-critical.

## [decision:release-tag-gated-on-probe] 0.28.5 tag push deferred behind the codex permission probe | 14:34 UTC | stage:wrapup
REVIEW surfaced (after the plan's "full release now" decision) that `tests/manual/CODEX_PERMISSION_PROBE.md` is the only runtime check that Claude Code's permission evaluator actually honors `Bash(codex exec:*)` given a bare `Bash` tool + interpreter denies — statically undeterminable. Firing the tag publishes 0.28.5 to PyPI/marketplaces immutably, so `main` was pushed (reversible backup) but the tag was held pending the probe + explicit go. New evidence justified pausing the otherwise-locked full-release decision.

## [decision:codex-schema-strict-fix-and-deferrals] codex-finding schema fixed for strict mode; re-render + smoke deferred | 15:10 UTC | stage:wrapup
Fixed `codex-finding.schema.json` for OpenAI/Codex strict structured-output mode (all-properties-required + nullable optionals + constraints dropped; 0.28.7). Two execute-time deviations from the PLAN, both forced by reality not preference: (1) the dogfood `.claude/` re-render (PLAN Phase 3) was re-scoped to a **post-release chore** — the live schema is gitignored, `make --update` in a worktree is the `[fail:snapshot-regen-inside-worktree]` footgun, and re-rendering in base via local `uv run harness-maker` uses an installed package copy lacking the staged fix + flips maintainer-local `--with` paths. The shippable fix is the source template only. (2) ADR-003's codex smoke gate is permission-gated (the `codex exec` Bash call was denied), so it's a user-run step — `main` pushed (backup), but `v0.28.7` tag held until the user runs the smoke. Review additionally hardened the new guard (structural walker; was scanning property names as keywords + missing the additionalProperties:false requirement).

## [decision:reconcile-schemas-replace-shipped] reconcile force-REPLACE for schemas/*.json (0.28.8) | 16:05 UTC | stage:wrapup
Root-caused why the 0.28.7 codex-schema fix didn't reach the dogfood: `reconcile()` KEEPs frontmatter-less renders, so existing installs froze. Fixed by a forced-REPLACE branch for `.claude/schemas/*.json`, reusing `render._is_schemas_json` (reconcile already imports render → no cycle, no second predicate → drift impossible). Scope kept surgical: mcp.json/.mdc are the same KEEP-forever class but carry user content → need MERGE, deferred. Both reviewers 0 findings; placement confirmed the only correct slot (one line later = the freeze). Boundary test mirrors cli.py:367-371 with real reconcile+render to prove on-disk overwrite (a reconcile-only unit test wouldn't). The reconcile fix itself reaches existing installs because it's Python re-fetched on `/plugin update`, not a rendered artifact.

## [decision:codex-exec-flag-fix-0.28.9] dropped invalid --ask-for-approval from codex recipe | 16:55 UTC | stage:wrapup
The codex_second_opinion call had a 3rd silent-skip cause: `codex exec` rejects `--ask-for-approval` (interactive-only). Dropped the line (0.28.9); `--sandbox read-only` is the isolation. The throughline across 0.28.6→0.28.9: warn-and-proceed silently hid each of three distinct breakages (no-Bash-tool, invalid-schema, invalid-flag). Lesson recorded: any warn-and-proceed-wrapped feature needs a positive happy-path probe. The fix was empirically verified by this session's release smoke (the exact corrected flag set returned schema-conforming JSON). Releasing as v0.28.9.
