# Session 2026-06-12

## [decision:worktree-deliverable-blocks-create] guard the CREATE boundary, not finalize; SHA-validated landed marker | 00:00 UTC | stage:wrapup
Two coupled worktree fixes. Key non-obvious constraints: (1) the create-guard deliverable exemption is the UNSAFE direction (forgiving more = guard fires less, per the documented fail-safe heuristic), so it had to be bounded — per-line (code WIP still blocks), anchored full-match with `[^/]+` (no nested over-match), and finalize filter left UNCHANGED so deliverables stay stash-preserved (no data-loss path). (2) The landed-marker had to record the branch TIP SHA and the sweep validate `tip == marker_SHA`, NOT bare marker-presence — plan-validator caught that bare-presence could delete a re-created same-named branch holding unlanded work (a regression of the 3x orphan-WIP incident). (3) Marker write co-located pre-cleanup on BOTH clean+dirty base, because the obvious `handed_off` handshake block is stash-conditional and skips the common clean-base finalize. (4) `git status --porcelain` collapses untracked dirs → needed `-uall` for the per-line filter (found in execute, not plan). Review (grade A) additionally tightened `.+`→`[^/]+` and added a finalize-marker producer test (the sole producer was untested = absent-case black hole). Codex second-opinion was permission-gated in this environment across plan + review — surfaced as skip, not blocked.
