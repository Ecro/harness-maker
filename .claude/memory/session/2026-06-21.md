# Session log — 2026-06-21

## [decision:autopilot-merge-gate-stop-before-wrapup] auto-advance stops BEFORE wrapup, not at its terminal | 07:30 UTC | stage:wrapup
Fixed the fullreview P1-1 (autopilot auto-advancing INTO wrapup and running the irreversible squash-land before the merge gate). User chose "stop before wrapup" over "guard Step 7.7": `autopilot_caps._HUMAN_GATED_STAGES={wrapup}` makes the boundary CLI refuse to auto-enter wrapup (`halt_kind=merge_gate`, record `gate_blocked`, clear marker → human invokes `/hm:wrapup`). Non-obvious constraint surfaced: the never-auto guard does NOT block the local `task-land` merge (only `git push`/`reset --hard`/`stash drop`), so the prompt-layer gate is the sole protection — hence it must be airtight. See [[gate-the-one-way-door-before-entering-its-stage]].

## [decision:guard-rm-shlex-tokenizer] rm-escape detection moved from prefix-regex to shlex operand check | 07:30 UTC | stage:wrapup
fullreview P1-2: the prefix-char regex missed mid-token traversal (`rm -rf build/../../etc`). Rewrote to shlex-tokenize operands and flag absolute/`~`/any-`..`-component/`$`/brace-list. Round-2 review caught a residual brace-expansion gap (`rm -rf {/etc,/home}`) — closed. Accepted over-block: ANY `..` component is treated as escape even when it resolves in-worktree (static containment is undecidable; block-biased is the documented fail-safe direction).

## [decision:in-cwd-no-isolation-due-to-concurrent-base] worked in-cwd, not a worktree, because a concurrent session held the base dirty | 07:30 UTC | stage:wrapup
A second session was committing the worktree task-land work to main (394f86e, 3c428a4) AND held base files dirty + a finalize stash. `worktree create` blocked on the dirty base; rather than `--allow-dirty-base` (which would stash into a queue that already had 2 stale finalize stashes), worked in-cwd and staged only autonomy files, leaving the concurrent WIP untouched. The concurrent commit's `{{ wt_prefix }}` addition to wrapup.md.j2 broke an integration-test regex (`test_wrapup_template_git_add_line_extractable`) — fixed the 1-line regex (test-only, clean file) with the user's approval. Snapshot-regen flakiness from the same area: see [[snapshot-regen-must-render-each-fixture-in-process-isolation]].

## [decision:loop-marker-session-scoping-p1-p4-functional] /hm:loop made parallel-safe (P1-P4 shipped functional, reviewed A; P5/P6 deferred) | 09:50 UTC | stage:wrapup
Closed the user-reported gap: the global `.hm-loop-active` was session-blind, so parallel `/hm:loop` sessions interfered (A's `rm` killed B's Stop guard; B's standalone `/hm:plan` saw A's marker → skipped its interview). Fix: Claude `session_id` in the marker CONTENT header (filename stays worktree-keyed → registry untouched), SessionStart hook exposes `HM_SESSION_ID` via `CLAUDE_ENV_FILE`, Stop-hook + `loop-mode-active` CLI content-match it; **only loop.md.j2 passes `--claude-session-id`** so standalone execute is never scoped. PLAN status = **in-progress** (NOT complete): P1-P4 are functional + reviewed Grade A across two k-of-3 rounds, but P5 (CLAUDE.md `## Multi-session` + `/hm:health` loud smoke) and P6 (two-session integration test) remain — deliberate honest partial per repo precedent. Non-obvious turns: (a) a 4th unenumerated marker reader (`_session_worktrees`) broke finalize — see [[new-marker-content-field-must-update-every-reader]]; (b) the re-review REVERSED the first review's Codex-H2 (degraded-loop self-guard) in favor of C1 (never block a peer) — parallel-safety wins, see [[degraded-fallback-can-reintroduce-the-very-bug-it-guards]]; (c) worked on the shared base (no worktree) because a concurrent session had finalized P1-P3 there already, and the user accepted `--allow-dirty-base` after a 30-min wait for the peer to land; the peer's dirty `session/2026-06-20.md` is preserved in a deferred finalize-stash restored at post-commit-pop.

## [decision:loop-marker-session-scoping-p5-p6-complete] P5+P6 landed → feature COMPLETE (P1-P6, Grade A) | 10:30 UTC | stage:execute
Closed the deferred hardening. P5: CLAUDE.md `## Multi-session` gained a loop-marker-session-scoping subsection; `readiness._dim_guardrails` gained the `sessionid_envfile_registered` loud-smoke signal (fires when a rendered `hooks.json` HAS SessionStart but lost the hook → silent degradation made visible by `/hm:health`). P6: `tests/integration/test_loop_parallel_session.py` (HM_RUN_PARALLEL_SESSION-gated) drives the REAL CLI/hook subprocess boundaries for 4 two-session scenarios — markers coexist, Stop-hook blocks only the owner, loop-mode detection is session-scoped, finalize leaves the peer's marker. Non-obvious P6 debugging turn: the CLI `worktree create` writes NO marker unless `harness.yaml worktree.scope` includes the stage (`_scope_includes` → empty no-isolation path); the direct `create()` Python call skips that gate, so the test fixture MUST commit a `.claude/harness.yaml` with `worktree.scope:[execute]` or every marker assertion fails. PLAN → status:complete. Worked on shared base again (peer's `2026-06-20.md` still restored-dirty, excluded from this commit by explicit-path staging — never `git add -A` on a shared base).

## [decision:multisession-10-fleet-hardening] 4 concurrency fixes for ~10-session fleet; Fix-4 was dead-on-arrival | 04:57 UTC | stage:wrapup
PLAN-multisession-10-fleet-hardening shipped 4 hardenings (same-slug hard-fail / drift auto-refresh+land-block / O_EXCL pid-nonce reaper / readiness live HM_SESSION_ID hard-gate). Three non-obvious turns the layered process caught vs missed: (1) execute found the land drift-block must sit AFTER the empty-squash no-op, not just after the `already` convergence check, else a change-already-in-head branch blocks instead of converging. (2) k-of-3 review split — code-reviewer passed the `_unlink_if_unchanged` reaper "clean" while concurrency+Codex flagged a real successor-removal TOCTOU (P1); consensus correctly sided 2/3, fixed via atomic os.replace-to-quarantine. (3) THE BIG ONE: Fix 4 gated the live probe on `CLAUDE_ENV_FILE`, which Claude Code exposes ONLY to hooks, NOT to command Bash — so the hard-gate was permanently N-A (dead-on-arrival), and ALL THREE review layers passed it because they read code logic, not the runtime env. Caught only by a user-requested `/hm:health` env probe; re-fixed to gate on `CLAUDECODE`. Lesson: runtime-env assumptions need a live probe. DEFERRED: integration-suite extension under INTEGRATION=1, and the prune-rmtree-vs-create race (real work-loss path, out of this scope). Pre-existing unrelated failure test_loop_codex_render_keeps_marker_on_non_convergence left as-is (loop template, untouched). Shared base again — explicit-path staging only, never git add -A.

## [decision:worktree-prune-create-race] lock-free pre-create reservation closes prune-vs-create work-loss + prune-crashes-add | 06:42 UTC | stage:wrapup
PLAN-worktree-prune-create-race shipped the lock-free fix (user rejected lock-serialization): `create()` writes a `.hm-creating-<name>` reservation (written-once mtime as the freshness signal — leaf-dir mtime is unreliable per Codex) before `git worktree add`; the prune scan skips fresh-reserved + no-`.git` dirs, and `prune_stale` skips `git worktree prune` entirely while any peer is mid-create. Three non-obvious turns: (1) PLAN-stage Codex refuted the original age+`.git` mechanism (nested-checkout mtime staleness) AND the marker-prune deferral (strands deferred stash) — forcing a Round-2 pivot to reservation + targeted marker fix. (2) the Phase-4 concurrent TEST (RED) caught a MORE SEVERE facet than the rmtree the PLAN targeted: concurrent `git worktree prune` crashes a peer's `git worktree add` by removing its half-written gitdir — fixed by gating the prune on `_any_fresh_reservation`. (3) REVIEW Codex (single-source, both Claude reviewers missed) caught an immortal-marker deadlock in the strand fix. Accepted residuals (documented): the half-written-gitdir instant is unclosable without a lock (P2, recoverable-retry), and multi-repo sibling bases get no reservation (deferred). 2 pre-existing unrelated failures (autopilot, plugin_live) not in my diff. Shared base — explicit-path staging only.

## [decision:fleet-10-20-c1-floor-shipped-c3-reverted] full R→P→E→Rev→W cycle: C1 floor ships, C3 reverted as unsafe | 08:30 UTC | stage:wrapup
PLAN-fleet-10-20-parallel-safety ran a full research→plan→execute→review→wrapup cycle on "are 10-20 multi-session worktree ops safe?". Net SHIPPED = the C1 loud floor only: a degraded `/hm:loop` on WSL2 (empty `HM_SESSION_ID`) self-stops after one iteration, and the two existing surfaces (`loop.md.j2` degraded guard + `readiness.sessionid_envfile_live`) mis-described it as "peers block each other" — corrected to the accurate self-stop symptom (CLAUDECODE-branched). Three non-obvious turns: (1) Phase-0 SPIKE killed the planned self-heal — the Stop payload cwd is project root with no worktree field, so a degraded marker can't be attributed to the stopping session (see [[loop-stop-payload-cwd-is-project-root]]); the floor is the only sound fix. (2) C3 (per-session queue-guard) went DONE→reverted across TWO review rounds: round 1 found the mtime-liveness was create-age not activity (near-no-op); the Option-A auto-fix (unconditional foreign exclusion) then PASSED a Grade-A review but a fresh re-review caught Codex P0 — it re-opens the count:3 contamination because Layer 3 (`post-commit-pop` owned-set) is all-markers, making Layer 1's foreign-counting LOAD-BEARING (see [[friction-looking-guard-was-load-bearing-safety]] + [[owned-session-uuids-is-all-markers-not-per-session]]). C3 fully reverted; real fix = harden Layer 3, a separate plan. (3) Process lesson: an auto-fix applied inside `/hm:review` MUST be re-reviewed by fresh eyes — the Grade-A on Option A was on the author's own un-reviewed change; the re-invocation is what caught the P0. Committed to main, explicit-path staging. Highest-value follow-up surfaced: Layer-3 per-session ownership hardening.

## [decision:layer3-per-session-ownership-shipped-grade-B] Layer 3 made per-session via slug crumb; same-slug residual accepted | 11:30 UTC | stage:wrapup
PLAN-layer3-per-session-ownership (the C3-unblocking follow-up) ran plan→execute(×2, in-cwd because Phase-1 was staged-not-committed so a fresh worktree would lack it)→review→wrapup. SHIPPED: `post-commit-pop`'s owned-set now comes from a slug-keyed crumb `.claude/.hm-owned-uuids-<slug>` (execute writes via `owned-crumb-add $(wt-uuid <WT>)`, wrapup reads by its slug arg → standalone-recovery works) instead of all-markers `owned-uuids`; the `:3266` guard dropped `owned_uuids and` so empty-set fail-safe-SKIPs uuid'd refs. Plan took THREE validation cycles (MAJOR→NEEDS→resolved): validator caught that the wrapup `<WT>` is the WRONG worktree (slug, not the execute-`<uuid>` that owns the ref) AND the standalone-wrapup absent-case (→ the crumb fix). Review grade B: k-of-3 (Codex+concurrency) caught a same-slug crumb peer-pop — the crumb UNIONs and is keyed by SLUG not session, so two sessions on one slug pop each other (see [[per-session-key-that-unions-by-a-non-session-key]]); code-reviewer mis-cleared it trusting my wrong "last-writer-wins" docstring. Auto-fix: corrected docstring + empty-slug CLI guard + dropped `_current_session_uuid` fallback (it stranded a legacy owner's own stash — a derivation that `wt-uuid` couldn't reproduce) + rewired the missed `worktree-isolator/SKILL.md` producer + extended the render-gate to scan skills. ACCEPTED-RISK: same-slug-same-task peer-pop (bounded: flag-on SharedSlug blocks, flag-off discipline, no worse than pre-fix); SharedSlug-on-crumb is the follow-up. Distinct-slug fleet (the goal + user's config) fully fixed; C3 re-enablement unblocked. Lessons: a "per-session" key must partition by session; when changing a consumer contract grep EVERY producer (commands AND skills); heterogeneous review again caught what same-model mis-cleared. Committed to main + push.

## [decision:h1-memory-tier-lock-shipped] H1 closed for participating writers via flock'd memory_md CLI | 14:00 UTC | stage:wrapup
Shipped the H1 fix (concurrent memory-tier writes losing data across a 10-20 fleet): new `memory_md` CLI does RMW inside `exclusive_lock`, flush_session adopts the same base-rooted lock, wrapup template routes Step 5.1/5.2/5.5 through it. Dogfooded the CLI in THIS wrapup (these very memory writes) — real e2e validation. Non-obvious turns: (1) the k-of-3 review's sharpest finding was 2-source consensus (security+code) on heading-shaped body lines (`## [tier:slug]`) injecting a phantom heading that truncates entries — the marker-string guard alone was insufficient; (2) Codex (single source) caught a markerless-but-non-empty file being silently split into unmanaged+managed halves — fixed to fail-closed; (3) honored the documented lesson that auto-fixes inside /hm:review need FRESH eyes — a Codex re-review of the fix diff confirmed no new hole (it found only the pathological `_base_root`-inside-`.worktrees` edge, accepted). Scope was deliberately H1-only: flag-OFF H4/H3 friction and multi-repo C2 stay documented-risk in RESEARCH. Guarantee is "closes H1 for PARTICIPATING writers" — a stale-rendered harness keeps the old Edit path (no runtime detection shipped, per user scope).
