# Session Log — 2026-06-24

> Auto-generated by harness-maker.
> Format: `## [category:slug] description | HH:MM UTC | stage:name`

## [decision:mutmut-3x-pin] Stay on mutmut 2.x via pin<3 + runtime 3.x guard | 12:48 UTC | stage:wrapup
Chose tightening the pin to `<3` over a 3.x wrapper rewrite (ADR-001) — mutmut is dev-group-only and the gate already degrades when absent, so the cost of staying on 2.x is low while it preserves two 2.x-tuned runbook wiki entries; a 3.x migration is its own deliberate effort, not a pin-fix rider. Added a runtime guard (ADR-002) because the pin only governs uv installs — the wrapper calls the bare `mutmut` from PATH, so a global 3.x bypasses it. The non-obvious bit: the genuinely-absent contract stays single-sourced at the existing run-FileNotFoundError handler (precheck FNF falls through, never re-implements absent) — the "absent-case = feature black hole" failure mode the plan-validator flagged as W1. Codex (k-of-3 voter) caught that the precheck ignored `proc.returncode`, a real ambiguity-contract gap both Claude reviewers missed.
