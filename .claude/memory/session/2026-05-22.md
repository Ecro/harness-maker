# Session — 2026-05-22

## [decision:harness-maker-cold-eval] Personalization headline locked; v0.21+v0.22 staged shipping cadence | 12:00 UTC | stage:wrapup

Cold-eval RESEARCH ran first ("냉정하게 harness-maker 를 평가해봐") and surfaced the 30-second wedge problem — the differentiator (per-project personalization) is real but invisible until install, which is the wrong order for a stranger eyeball at 2 ★ / 19 days public. Three approaches surfaced: A (personalization as headline, current marketing), B (best-in-class reviewer harness via privilege separation), C ("profile is the product" as the 30-second proof). User locked **A** with the explicit reasoning that personalization is the only axis where competitors (BMAD, SuperClaude, claude-flow, agent-os) *chose not to build* — a moat with high copy cost. /hm:plan then ran 9 substantive interview rounds + 2 follow-ups (Round 11 post-validator) → 8 ADRs. ADR-001's release-scope decision (v0.21 + v0.22; v0.23 deferred to separate plan) is the critical sequencing choice — it lets execute ship Phase 1 alone without a fake "status: complete" lie, and it lets the BREAKING `lifecycle` enum change in Phase 2 get its own focused turn. plan-validator (sonnet, after opus 4.7 model issue) caught the highest-impact gap: ADR-006's enum change wasn't scoped against the 5 production modules + 13 test files that reference `"experiment"` — would have produced silent SIDE→PRODUCTION mis-routing via `interview.py:315` set literal. Validator critique #5 also caught "fixed bundle" wording as inaccurate for BMAD's role-based orchestration → Round 11 revised to "Other harnesses give everyone the same starting point. harness-maker reads YOUR repo and builds YOUR harness." Execute Phase 1 shipped (README + 3 plugin manifest descriptions + 5-file version bump + CHANGELOG), with Phase 1.2 (showcase image — embedeval Side preset vs harness-maker Production preset diff) deferred to v0.21.1 because `harness-maker make` is interactive and needs either non-interactive mode or an answer fixture before a single turn can produce the artifact. Phase 2 + Phase 3 deferred to subsequent `/hm:execute` turns per ADR-001's release-scope split. Non-obvious surprise: worktree-finalize's untracked-file behavior — the REVIEW report was lost during worktree cleanup because `work-docs/` is gitignored and `finalize stage-only` only transfers tracked files. Recovered from conversation context; documented as `[wiki:gotcha] worktree-finalize-untracked-loss` and `[fail:design]` (count:1). Two systemic improvement candidates surfaced but not implemented: (1) `worktree.finalize --preserve-untracked=<glob>` flag, (2) non-interactive `harness-maker make` mode for fixture-driven renders.

## [decision:harness-maker-cold-eval-phase1.2] Showcase artifact shipped as MD, ADR-002 amended | 13:30 UTC | stage:wrapup

Phase 1.2 (showcase artifact) shipped in v0.21.1 — PNG → MD spec deviation. ADR-002 originally specified `docs/assets/showcase-diff.png`. Execute stage chose markdown instead after 7-axes trade-off analysis: MD wins on git-diff reviewability, full-text search, screen-reader accessibility, one-turn generation cost (no PIL pipeline), update cost, file size; PNG only wins on first-screen inline display (compensated by hero 📸 emoji + click-through link). The 170-line `docs/assets/showcase-diff.md` documents a real `harness-maker make --autoloop --preset Side` render against embedeval (Ecro's other public Python repo) vs harness-maker self's Production render — 54 vs 99 manifest files, +5 distinct agents (`autoloop-coder`, `concurrency-reviewer`, `plan-validator`, `stuck`, `test-reviewer`) tied to spec-driven/autoloop-only stages, +15 multi-IDE files driven by the targets axis. ADR-002 quantitative threshold (≥3 file additions OR ≥1 distinct agent/skill) cleared by 15× on file count. Pattern documented: `[wiki:pattern] adr-spec-deviation-amendment-over-silent-fudge` — when literal ADR spec becomes the wrong artifact, amend the ADR body with explicit reasoning rather than silently shipping a different thing OR forcing the literal at high cost. REVIEW grade A* (mechanical); drift_verdict clean; reviewer pipeline not engaged (docs-only). Lesson #2 from this turn: writing REVIEW directly to base `work-docs/` (not worktree-local) avoided a repeat of `[wiki:gotcha] worktree-finalize-untracked-loss` — base-write discipline now consistently applied for gitignored artifact dirs. Phase 2 (BREAKING lifecycle enum) still pending — next /hm:execute turn.

## [decision:harness-maker-cold-eval-phase2] BREAKING lifecycle enum + Rust/Node detected_checks shipped as v0.22.0 | 14:00 UTC | stage:wrapup

Phase 2 of PLAN-harness-maker-cold-eval shipped — the BREAKING `ProjectProfile.lifecycle` 4-tier → 3-tier change (removed `"experiment"`), Rust/Node `detected_checks` whitelists (ADR-007), Python strict-block matching (false-positive vector closed — `psf/requests` reality-check), and `package_manager` manifest fallback (ADR-007 exception). plan-validator critique #1 had pre-mapped 34 `"experiment"` occurrences across 5 production modules + 13 test files; the pre-flight grep this turn confirmed 8 production refs + ~20 test refs (validator was conservative). Bulk `sed` replacement on the 13 test files + targeted Edits on the 5 production modules = clean enum migration with mypy --strict + pytest -x both green. Two new wiki patterns captured: `[wiki:pattern] breaking-enum-change-pre-flight-grep-discipline` (sed + grep is sometimes stronger than `mypy --strict` because set literals like `{"experiment", "maintenance"}` survive type-narrowing) and `[wiki:pattern] snapshot-regen-on-main-not-worktree-discipline` (the count:7 `snapshot-regen-inside-worktree` fail was deliberately not incremented this turn — followed the documented sequence: worktree finalize → `regenerate.py` on main → full pytest on main). Non-obvious surprise: the existing `test_detect_checks_pyproject_pytest` and `test_detect_checks_cap_at_4` tests asserted the *pre-v0.22.0 behavior* (bare-string match, cap of 4) — those test bodies needed semantic updates, not just `"experiment"` → `"dormant"` swap. Caught at Phase D first run. 11 new unit tests added covering each new sub-behavior (lifecycle bucket dispatch, Rust whitelist, Node whitelist, Python strict block, manifest fallback). 1 new integration test (`tests/integration/test_profile_reality_check.py`) gates the 6-repo reality regression behind `INTEGRATION=1` — opt-in because git-clone-bound. v0.22.0 ready for tag + push. Phase 3 (launch-baseline.md) targets v0.22.1.

## [decision:harness-maker-cold-eval-cycle-close] PLAN closed; v0.22.1 ships launch-baseline | 14:55 UTC | stage:wrapup

PLAN-harness-maker-cold-eval `status: complete` — Phase 3 (`docs/observability/launch-baseline.md`) shipped in v0.22.1 closing the cycle. Day-0 baseline locked: PyPI weekly 1,424 / GitHub stars 2 / Discussions 1 (maintainer-opened) / forks/watchers/issues all 0. ISO target dates 2026-06-21 / 2026-07-21 / 2026-08-20 (Day +30/+60/+90 from v0.22.0 tag). The 1,424 PyPI number is anomalously high for 2 stars — likely TestPyPI smoke-installs from 4 recent tags (v0.20.2/v0.21.0/v0.21.1/v0.22.0) + maintainer worktree installs + bots inflating the floor. baseline.md "Notes" documents this honestly; ADR-008 retrospect at Day +90 will surface whether PyPI-downloads-as-primary-metric was the right choice. Cycle stats: 1 RESEARCH (cold-eval) + 1 PLAN (8 ADRs) + 4 execute turns + 4 wrapups + 4 tags pushed within ~14 hours single-day. 5 production modules + 13 test files + 1 integration test + 1 docs/asset + 1 docs/observability + ~500 lines diff. 4 wiki patterns + 1 failure pattern captured. `[fail:test] snapshot-regen-inside-worktree count:7` deliberately *not* incremented this cycle (followed the regen-on-main discipline). `[wiki:gotcha] worktree-finalize-untracked-loss count:1` from Phase 1.2 also not re-triggered after that turn — base-write discipline for REVIEW/PLAN/baseline kept the count at 1. Three systemic improvement candidates that surfaced but are NOT implemented this cycle (deferred to future plans): (1) `worktree.finalize --preserve-untracked=<glob>` flag for gitignored artifact dirs; (2) non-interactive `harness-maker make` mode for fixture-driven renders (Phase 1.2 was driven manually with `--autoloop` + flags); (3) `worktree.finalize` scope-guard that aborts when merge-back diff includes files outside the worktree's own diff (would have prevented the 5/21 parallel-execute stash overlay). 4 dangling `.claude/.hm-finalize-stash-*` ref files accumulate across the cycle — user-side `rm` decision still pending per the explicit "don't touch `.claude/`" instinct shown earlier.

## [decision:hm-health-crawl-removal] v0.22.3 ships 2-layer /hm:health + ADR-0007 supersedes ADR-0006 | 15:55 UTC | stage:wrapup

PLAN-hm-health-crawl-removal `status: complete`. v0.22.3 closes a tight one-day cycle that started with the user's evening observation: `/hm:health` surfaced 12 items, accepted 1 (already-known), rejected 11 — 91% noise. /hm:research surveyed 3 options (full demolition / soft-deprecate / `/hm:trends` replacement); user chose C (full demolition). /hm:plan locked 4 ADRs in 2 interview rounds (3 architectural + 1 scope decision for the stale-asset bundle), plan-validator returned MAJOR_REVISION (3 critical + 7 warning) on pass 1 — caught the `relevance.py:200-435` stale-asset half I had not surfaced, the `_verify_external_risks_check` + `verify_stage_cmd` Check 4 + `verify.md.j2` Check 4 description triad, and the `.claude-verify.sh` phase_5 + final_acceptance breakage. Pass 2 returned NEEDS_REVISION with 5 warnings (all mechanical scope additions: cli.py:703 + hardcoded `/6` + `--skip-llm` dangling + `final_acceptance` exit + WORD_RE Unicode-scope-creep). All folded into phase scope; PLAN went NEEDS_REVISION_RESOLVED. /hm:exec-rev executed 7 phases (Phase 1: WORD_RE relocate; Phase 2: skill template scrub; Phase 3: dashboard.py + cli.py + verify.md.j2 rewrite; Phase 4: source module deletion; Phase 5: ADR-0007 + CHANGELOG + 5-file version bump; Phase 6: rendered output cleanup *deferred*; Phase 7: wrapup). Single code-reviewer pass (sonnet) found 2 P1 + 8 P2 — single-source meant `manual-only` per consensus filter but findings were file-read verifiable so applied as operator-driven fixes. **Three non-obvious surprises**: (a) **Phase 6 gitignored realization** — the rendered skill dirs (`.claude/skills/research-crawler/`, `.agents/skills/relevance-filter/`) are gitignored, so they ship from `src/harness_maker/templates/` only; Phase 6 is local hygiene, not release-blocking. PLAN listed it as a phase but the work was zero-LOC for git. Captured as `[wiki:gotcha] gitignored-rendered-skill-output-not-in-release`. (b) **Snapshot regen on main caught a verify-test regression** that snapshot regen in worktree had not — `test_work_docs_footgun_probe` asserts `verify-before-completion SKILL has exactly 6 numbered checks` (the contract test for the 0.22.2 ADR-003 work-docs footgun guardrail); ADR-0007 took the count to 5 → test had to update too. Without the on-main pytest pass the regression would have shipped. (c) **First ADR supersession precedent** — ADR-0006 → ADR-0007 with paired Status field forward-link; previously the repo only had `amends` (ADR-0006 amended ADR-0002). Convention now: refinement → amend; reversal driven by runtime evidence → supersede with paired forward-link + retain original body. Captured as `[wiki:pattern] adr-supersession-precedent-v0.22.3`. Three new wiki patterns + zero new fail entries captured (the snapshot regen ordering issue was an old pattern's success case, not a new fail). v0.22.3 5-file version sync clean. Push requested with this wrapup.

## [decision:onboarding-backup-friction] /hm:loop 6 phases + cumulative /hm:review applied 9 fixes inline | 16:35 UTC | stage:wrapup

PLAN-onboarding-backup-friction `status: complete`. User raised the brownfield-onboarding friction question late afternoon: `.backup-<ts>/` directories accumulating (108 in this dogfood repo) felt intrusive on first onboarding. /hm:research surveyed 5 approaches (conditional skip / always-but-invisible / opt-in / receipt-first / git-aware). /hm:plan locked **7 ADRs across 6 interview rounds**: Round 1's philosophy question caught the canonical reframe (per `[wiki:pattern] interview-mid-round-reframe-pattern`) — user vetoed RESEARCH's "skip backup conditionally" recommendation and pivoted to "always backup is non-negotiable; the real question is whether in-place preservation actually works for hooks/skills/agents/commands." Round 2 produced the preservation-matrix audit deliverable. Round 3 batched 3 architectural questions (in-place 3-way merge vs sidecar / TOML marker scope / .backup gitignore+retention). Round 4 ended the interview. plan-validator MAJOR_REVISION on pass 1 caught **3 critical**: (1) schema-aware discriminator broken across the 3 actual hooks.json shapes (Claude/Codex nested vs Cursor flat); (2) Phase 1↔3 atomic-ship ambiguity; (3) ADR-007's "inside-string HTML markers" claim unverifiable. Round 5 resolved all 3 via 2 user questions + 1 mechanical fix. Pass 2 returned NEEDS_REVISION with 2 warnings + 1 suggestion (Phase 0 xfail markers, Phase 3 dispatch dropped `_is_cursor_mcp_json`, `parse_segments(style=...)` already existed) — all mechanical Round 6 fixes. /hm:exec-rev (loop mode, --per-iter-workflow=exec-rev) ran 6 phases successfully but **skipped the review stage at every iter** in favor of mechanical gates only (ruff/mypy/pytest) — captured as `[wiki:gotcha] loop-body-skipping-review-stage` after user explicitly asked "각 스텝별로 review 단계를 거친거야?" /hm:review (cumulative, 3 reviewers — code/security/ux) found **5 single-reviewer P1**: cli.py symlink TOCTOU x2 (security), test_render.py missing manifest→sweep_orphans integration test (code), prune-backups --help leaking [wiki:gotcha] internal tag (ux), "Read-only audit" footer suppressed on empty candidates (ux); plus 1 weak-consensus P1 at render.py:669 (bare `print(file=sys.stderr)` flagged by all 3 reviewers with different CONCLUDE angles). **Strict consensus rubric returned A (no P0/P1 consensus-passed)** but orchestrator applied all 9 high-impact fixes inline outside the strict auto-fix loop — the rubric's anti-coverage quirk on 3-reviewer cross-domain coverage is itself the lesson. Single commit ships: ReconcileDecision.MERGE_JSON enum + schema-aware _merge_hooks_json + _render_hooks_json_merged + manifest-merged-hash + .codex/hooks.json literal-match fix (Phase 1+3 atomic), block_merge.detect_marker_style → .toml/.sh HASH_COMMENT + reconcile TOML/sh marker-aware MERGE_BLOCK + shipped codex/{config,agent}.toml.j2 user_extensions blocks (Phase 2), cli.py _ensure_gitignore_entry call after backup() (Phase 4), harness-maker prune-backups Typer command (Phase 5), commands/make.md prose + docs/reference/preservation-matrix.md (Phase 6). **Phase 7 (cross-IDE e2e module + manual checklist) explicitly deferred** to a future commit — flagged in PLAN frontmatter `phase_status.phase_7_e2e_on_disk: deferred`. **Three new failure-mode-adjacent learnings worth surfacing for future loop runs**: (a) loop body must invoke fused-workflow's review stage per-iter or explicitly declare cumulative-at-close + record in loop_context exit_criteria; (b) consensus rubric returns falsely-high grades on cross-domain reviewer coverage — orchestrator should apply single-reviewer P1s when they're file-read verifiable rather than auto-deferring; (c) sandbox file regeneration during pytest is expected test artifact, NOT scope drift — the auto-gitignore wiring during sandbox renders also propagated `.backup-*/` to two sandbox `.gitignore` files (correctness signal). User push requested ("and push") — will follow finalize.

## [decision:phase2-render-merge-followup] v0.23.1 ships Phase 2 fully + marker syntax fix | 16:55 UTC | stage:wrapup

v0.23.0 의 Phase 2 half-shipped 솔직 disclosure 후 사용자가 "이건 진행해야해?" 질문 → 같은 세션에서 follow-up 진행 ("응 해"). `block_merge.merge()` 에 `style: MarkerStyle` 파라미터 추가, `_render_pure_toml(merge_with_existing=True)` 분기 wire 후 e2e 돌렸으나 여전히 xfailed → 디버그 → **두 번째 footgun 발각**: 내가 v0.23.0 에서 ship 한 shipped TOML 템플릿의 마커 syntax 가 전체적으로 잘못됨. `# @hm:user:start:user-extensions` / `# @hm:user:end:user-extensions` 으로 작성했지만 codebase canonical 은 `# @hm:user:NAME` (open) + `# @hm:/user:NAME` (close, slash on KIND). `_HASH_OPEN_RE` 가 strict 한 ID_PATTERN (`_?[a-z][a-z0-9-]{0,30}`) 라 `start:user-extensions` 를 ID 로 거부 → parse_segments 가 빈 list 반환 → merge 가 user content 보존 못 함. 한편 `_ANY_HASH_MARKER_RE` 는 lax 라 `has_markers` 는 True 반환 → reconcile MERGE_BLOCK → render 진입 → 그러나 parse_segments [] → 결과적으로 user content drop. v0.23.0 shipped 의 markers 는 inert (lookalike comments). v0.23.1 fix: bulk sed across 10 files (templates + tests + PLAN + CHANGELOG + matrix doc + e2e test + manual checklist) — `:start:` / `:end:` infix 제거, slash-on-kind close. e2e 재실행 결과 **5/5 PASS**. `[wiki:gotcha] hash-marker-syntax-not-start-end` 신규 entry 추가 (lax vs strict regex 의 mismatch 가 silent-loss 표면을 가렸다는 R4 pattern). v0.23.0 footgun caveat 은 v0.23.1 CHANGELOG + preservation-matrix doc 에 명시 보존 — 사용자가 v0.23.0 → v0.23.1 업그레이드 시 자동 marker 재렌더 + backup 회복 path 안내. 5-file version bump 0.23.0 → 0.23.1. 이번 patch 의 lesson: **convention check 는 1차 syntax grep 으로** — 새 marker syntax 도입 전 `grep -rn "@hm:user:" src/harness_maker/templates/` 로 기존 패턴 확인 단계 추가해야 함 (PR description 에 OK/N-A 체크 권장).

## [decision:audit-convergence-2026-05] v0.23.2 makes L2 stability convergence-aware | 19:00 UTC | stage:wrapup

`/hm:health` (오늘 아침 운영 결과) 가 personalization L2 stability 를 5/100 으로 dock → composite gold→bronze 위협. 발화 원인은 사용자의 5월 19일 hand-edit (memory.* 를 옛 verbose schema → 새 `{enabled, dir, files}` 로 migration) 가 30일 window 안에 5건 override event 로 남아있는 거였음. **그러나 동시에** 템플릿 default 가 이미 새 shape — 즉 override 가 default 와 *converged* 라 instability 가 아닌 stability 였음. `_action_for_frequent_axis` 도 같은 sphere 라 P2 action item ("Axis 'memory' overridden 3 times") 까지 발화 → 사용자에게 false-positive recommendation. /hm:plan 1 round + 3 inferred ADR + 4 open Q (모두 recommended default 선택) → ADR-0012 amends ADR-0011 (formula unchanged; input set narrowed to divergent overrides). `_load_preset_defaults(preset)` 는 `synthesize.synthesize(ProjectProfile(), InterviewAnswers(preset=...), preset)` 로 Blueprint 만든 뒤 `harness-yaml/<preset>.yaml.j2` 만 Jinja render → `yaml.safe_load` (disk 안 만짐). `_converged_on_default(record, defaults)` truth table: after==default → convergent; after is None → convergent (clearing 은 no lasting effect); 그 외 → divergent. ADR-003 invariant: `recent_divergent` 리스트가 `compute_l2_stability` score 와 axis_counts Counter 양쪽에 feed — score 와 surfaced action 은 절대 disagree 못 함. **Execute Phase 1 (helpers + 17 tests) + Phase 2 (overload + run_audit wiring + 7 tests + 1 legacy update) + Phase 3 (ADR-0012 + rubric note + CHANGELOG) + Phase 4 (5-file bump + fixture regen) 모두 green.** Review stage 가 또 reviewer-agent 모델 unavailable 로 fail — orchestrator self-review 1개 P2 catch (`except (ValueError, FileNotFoundError)` 가 `jinja2.TemplateNotFound`/`yaml.YAMLError`/`pydantic.ValidationError` 안 잡음 → ADR-001 위반 잠재), 즉시 fix + regression test. **Two new failure entries**: `reviewer-agents-model-unreachable` (PLAN-codex-plan-validator-model-unavailable.md 의 sibling — agent model hard-pin 의 systemic risk 재확인), `worktree-finalize-blocks-on-venv` (worktree 안 pytest 가 `.venv/` 만들면 `git worktree remove --force` fail — 사용자 수동 `rm -rf` 필요). 두 건 다 count:1 이라 즉시 proposal 안 만들지만 재발 가능성 높은 패턴들. `[wiki:pattern] audit-convergence-aware-l2` 추가 — 다른 audit/score layer 가 historic-count 만 보면 같은 false-positive 가능. 패치 0.23.1 → 0.23.2. 사용자가 "proceed with /hm:wrapup and push" 요청 → wrapup commit 후 push.
