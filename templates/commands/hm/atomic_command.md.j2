# /hm:{{ stage }}

> Atomic stage command — runs only the `{{ stage }}` stage.

## Usage

```
/hm:{{ stage }} <task description>
```

## Arguments

`$ARGUMENTS` — task description (kebab-case slug auto-generated).

## Behavior

Phase 5에서 stage runner 본문 채워집니다.
