{#- Claude Code hooks schema: PascalCase event keys + nested
    {matcher, hooks: [{type, command}]} shape.

    Cursor uses a different schema (lowercase camelCase + version: 1, flat
    {matcher, command}) rendered to .cursor/hooks.json by
    templates/cursor/hooks.json.j2. Each IDE reads its own file; the
    dual-render is intentional, not a bug.

    See tests/cursor-compat/results-2026-05-08.md for the kairos-derived
    empirical evidence proving Cursor reads .cursor/hooks.json natively.
-#}
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {"type": "command", "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.telemetry", "timeout": 5}
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {"type": "command", "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.hooks.post_write_reminder", "timeout": 5}
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {"type": "command", "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.gates.permission_gate"}
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {"type": "command", "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.gates.worktree_gate"}
        ]
      }{% if config.dev_mode == "spec-driven" %},
      {
        "matcher": "Write|Edit",
        "hooks": [
          {"type": "command", "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.gates.spec_gate"}
        ]
      }{% endif %}
    ],
    "PreCompact": [
      {
        "matcher": "auto",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.hooks.flush_session",
            "timeout": 30,
            "statusMessage": "Snapshotting autoloop progress..."
          }
        ]
      },
      {
        "matcher": "manual",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.hooks.flush_session",
            "timeout": 30,
            "statusMessage": "Snapshotting autoloop progress..."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {"type": "command", "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.hooks.sessionstart_drift"}
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "uv run --with {{ harness_maker_src_path }} python -m harness_maker.hooks.loop_gate --mode stop-hook",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "preset": "{{ preset }}"
}
