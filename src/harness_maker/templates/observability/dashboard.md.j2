# Health

> Latest-snapshot dashboard. `/hm:health` **overwrites** this file on every run
> (`observability/dashboard.py:write_dashboard`, plain atomic write — not
> append-only and not block-merged). Do not add hand-maintained panels here;
> they are lost on the next health run. The 2-section schema below mirrors
> exactly what the writer emits so `/hm:verify` Check 3 can read the
> `## Structural` `score:` line.

## Structural

Run `/hm:health` to populate the structural score.

## Personalization

Run `/hm:health` to populate the personalization score.
