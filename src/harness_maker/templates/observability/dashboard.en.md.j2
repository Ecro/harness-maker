# Dashboard — {{ preset | default('Side') }} preset

**Project:** {{ project_name | default('unknown') }}
**Preset:** {{ preset | default('Side') }}

## Efficiency
- Cache hit: {{ cache_hit | default(0) }}%
- Cumulative tokens: {{ total_tokens | default(0) }}

## Health (6-dim)
{% for dim, score in (health_dims | default({})).items() -%}
- {{ dim }}: {{ score }}/100
{% endfor %}

## Agent Quality
{% for agent, tier in (agent_tiers | default({})).items() -%}
- {{ agent }}: {{ tier }}
{% endfor %}

## Refresh

Run `/hm:monitor` to refresh.
