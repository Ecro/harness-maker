# Dashboard — {{ preset | default('Side') }} preset

**프로젝트:** {{ project_name | default('unknown') }}
**Preset:** {{ preset | default('Side') }}

## 효율
- Cache hit: {{ cache_hit | default(0) }}%
- 누적 토큰: {{ total_tokens | default(0) }}

## Health (6-dim)
{% for dim, score in (health_dims | default({})).items() -%}
- {{ dim }}: {{ score }}/100
{% endfor %}

## Agent Quality
{% for agent, tier in (agent_tiers | default({})).items() -%}
- {{ agent }}: {{ tier }}
{% endfor %}

## Refresh

`/hm:monitor` 로 갱신.

<!-- @hm:user:extensions -->
<!-- 프로젝트 고유 대시보드 패널 (KPI, 외부 대시보드 링크 등). harness-maker 업데이트 시 보존. -->
<!-- @hm:/user:extensions -->
