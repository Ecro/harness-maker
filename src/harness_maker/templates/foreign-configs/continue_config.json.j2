{
  "_hm_harness": {
    "_comment": "This top-level _hm_harness key is managed by harness-maker. harness-maker performs a top-level JSON key merge: this single key is REPLACED on every re-render; every other top-level key you add is preserved byte-for-byte.",
    "generated_by": "harness-maker",
    "locale": "{{ locale }}",
    "preset": "{{ preset }}",
    "targets": {{ targets | tojson }},
    "default_model": "{{ default_model }}"
  }
}
