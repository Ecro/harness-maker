# Aider configuration generated by harness-maker.
# Content INSIDE `# @hm:harness:settings` ... `# @hm:/harness:settings` is
# managed by harness-maker (REPLACED on every re-render); everything outside
# is yours and preserved byte-for-byte. YAML treats `#` as comment, so the
# hash-comment markers are inert to aider's YAML parser yet detected by
# harness-maker's block-merge engine.

# @hm:harness:settings
# generated_by: harness-maker
# locale: {{ locale }}
# preset: {{ preset }}
# targets: {{ targets | join(", ") }}
model: {{ default_model }}
# @hm:/harness:settings

# Your own aider settings below — preserved across re-render.
