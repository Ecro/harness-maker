{
  "permissions": {
    "allow": ["Read", "Bash(uv:*)", "Bash(pytest:*)", "Bash(git:*)"],
    "deny": {% if config.permissions.deny_dangerous %}["Bash(rm:*)", "Bash(curl * | sh)", "Write(/etc/**)", "Write(~/.ssh/**)"]{% else %}[]{% endif %}
  },
  "preset": "{{ preset }}"
}
