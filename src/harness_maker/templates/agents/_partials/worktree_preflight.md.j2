{#- Phase 5 (ADR-002/004/006): flag-on per-task worktree preflight.
    Shared across every /hm: stage template. The INCLUDING stage owns the
    `worktree.feature_branch_workflow` gate (a `{%- if … %}` wrapper) so this
    partial is only rendered when the flag is on — its own whitespace therefore
    never affects the flag-OFF snapshot fixtures. Inherits is_codex +
    harness_maker_src_path from the parent stage context. Substitute <slug> with
    the task slug and treat the command's stdout as <WT>.

    PLAN-token-economy-step-pruning Phase 4: in a FUSED command the shared prose
    (intro + tail) is hoisted to the workflow preamble and rendered once; each stage
    keeps its heading and its own `--stage hm:<stage>` invocation, which is per-stage
    by construction and IS the mechanism, not a duplicate. `workflow_context` already
    discriminates fused from atomic (the workflow name in `workflow_fuse.fuse()`, ""
    in `synthesize`), so no new flag was introduced. The atomic branch below renders
    byte-identically to the pre-Phase-4 output — atomic renders are AC-007's
    differential control and must not move. -#}
### Task worktree preflight (feature-branch workflow)

{% if workflow_context %}Claim this stage's task worktree (preamble: **Task worktree preflight — shared rules**):
{% else %}{% include "agents/_partials/pf_intro.md.j2" %}{% endif %}
{% if is_codex %}
```
Bash("uv run --with {{ harness_maker_src_path }} python -m harness_maker.worktree task-preflight <slug> \"$(pwd)\" --stage hm:{{ hm_stage }}")
```
{% else %}
```bash
!uv run --with {{ harness_maker_src_path }} python -m harness_maker.worktree task-preflight <slug> "$(pwd)" --stage hm:{{ hm_stage }}
```
{% endif %}
{% if not workflow_context %}
{% include "agents/_partials/pf_tail.md.j2" %}{% endif -%}
