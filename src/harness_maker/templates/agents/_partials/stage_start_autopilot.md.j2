<!-- @hm:pipeline-active-crumb (PLAN-autopilot-guard-interactive-scope) -->

**Autopilot guard scope.** This harness runs `guard_when: pipeline_only`, so the autopilot guard stays dormant in plain interactive sessions. A pipeline stage is now starting — stamp the leading `.hm-pipeline-active` crumb FIRST (before any stage work) so the guard is armed for the rest of this session. No-op-safe (no marker armed → harmless; cleared with the marker at the pipeline's terminal point):

!uv run --with {{ harness_maker_src_path }} python -m harness_maker.autopilot pipeline-active --root .
