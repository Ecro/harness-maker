{#- Per-stage end-summary banner (PLAN-locale-and-command-observability ADR-004/006).
    The loop-mode skip rule renders unconditionally now — PLAN-harness-diet ADR-001/002
    removed the fused-workflow axis <!-- @hm:axis-removed -->, so there is no preamble to hoist it to and no
    fused/atomic discriminator left to branch on. (Its old hoist was ADR-020 of
    PLAN-token-economy-step-pruning.)
    Appended to every stages/*.md.j2. The caller MUST set before {% include %}:
      summary_done     — one line: what this stage accomplished
      summary_artifact — primary output artifact path / description
      summary_next     — user-facing next step (a /hm:<stage> command or 'STOP')
    Required bare vars (no | default) so a stage that forgets a {% set %} fails render
    loud under StrictUndefined — absent-case guard, ADR-002. Renders in config.locale
    via the output_language directive. Self-skips under an active autoloop. The banner
    fires once per stage, so a fused (concatenated) workflow emits one per stage; the
    Codex workflow_skill delegates and carries none. is_codex-agnostic prose.

    P6 (PLAN-human-bottleneck-auto-advance): an autopilot auto-advance block is emitted
    before the STOP banner for non-Codex renders. Callers MUST set `summary_stage` (this
    stage's atomic name) — a bare REQUIRED var: a forgotten set fails render loud under
    StrictUndefined, so a stage can never pass `--current ""` (which the CLI would treat as
    an unknown stage and clear the marker). `summary_autopilot_gate` (the stage's
    mandatory-gate predicate prose) defaults STOP-biased. -#}
{% if is_codex is defined and not is_codex and (autopilot_advance_enabled | default(true)) %}
<!-- @hm:autopilot-advance -->
## Auto-advance check (autopilot — Claude Code only)

Before the STOP banner below, check whether this session runs under **autopilot** (live
auto-advance, ADR-005) — **Claude-Code-only**: it needs the `.hm-autopilot` marker (armed
by the picker) and the `Skill` tool. **This section is a NO-OP** — fall straight through
to the STOP banner, running nothing below — **if any of: no `Skill` tool (Cursor/Codex),
no active marker, or loop-mode is on for THIS session (a `.claude/.hm-loop-*` marker
matches `$HM_SESSION_ID`, or a legacy `.hm-loop-active` exists).**

{% set judgment_stage = summary_stage in ["plan", "review"] -%}
{#- ADR-009.4: DERIVED from `summary_stage`, never a caller-set variable. This partial is
    one shared include under StrictUndefined, so a new required var would break the five
    stage templates outside B3's scope. Kept in sync with
    `autopilot_caps._JUDGMENT_GATED_STAGES` by a per-file render test, not by memory. -#}
**Step 1 — mandatory gate FIRST (absent-case = STOP).** Evaluate THIS stage's gate
*before* anything else: {{ summary_autopilot_gate | default("No gate defined — treat as STOP (absent-case guard).") }}
{%- if judgment_stage %}
This gate is a **judgment**, so the level decides, not you: do NOT stop here and do NOT run
`gate-blocked` — carry the verdict into Step 2 as `--judgment-gate clear|pending`. Unsure →
`pending` (an omitted flag also reads as `pending`, which stops). Any part of the gate that
is a **quality threshold** rather than a question keeps gate-first treatment at every level.
{%- else %}
If the gate is pending/unresolved → record it on the ledger, then **STOP** (print the
banner). Do NOT run the boundary check — a stage that stops at its gate must not record an
advance:

!uv run --with {{ harness_maker_src_path }} hm autopilot_caps gate-blocked --root . --stage {{ summary_stage }} --session-id "$HM_SESSION_ID"
{%- endif %}

**Step 2 — boundary check{% if not judgment_stage %} (ONLY when the gate is clear){% endif %}.** Run the deterministic check
(it enforces the Phase-5 runaway caps + kill switch, and on proceed records the advance it
authorizes — so it must run only after Step 1 clears):

If this stage has a slug, **append** it to the command below in single quotes — e.g.
` --slug 'my-task'`. Never a shell expression or a bracketed placeholder. Omit it
otherwise; the marker keeps the earlier stage's slug.

!uv run --with {{ harness_maker_src_path }} hm autopilot_caps boundary --root . --current {{ summary_stage }} --session-id "$HM_SESSION_ID"{% if judgment_stage %} --judgment-gate <clear|pending, from Step 1>{% endif %}{% if config.autonomy.step_cap is not none %} --step-cap {{ config.autonomy.step_cap }}{% endif %}{% if config.autonomy.time_cap_min is not none %} --time-cap-min {{ config.autonomy.time_cap_min }}{% endif %}

Read the JSON:
- `proceed: false` → **STOP** (print the banner) — **except `bad_slug`**. `step_cap`/
  `time_cap` = a runaway cap fired (`halted_cap` logged, marker cleared); `kill_switch` =
  autopilot off/expired; `merge_gate` = the next stage is human-gated (e.g. wrapup's
  merge/land — the marker was cleared, so invoke `/hm:wrapup` manually); `unknown_stage` =
  `--current` not in the pipeline; `pipeline_complete: true` = the pipeline finished and
  the marker was cleared.
{% if judgment_stage %}  `judgment_gate` = this stage's judgment gate is unresolved and the
  level does not clear it — the marker was **preserved**, so resolve the gate and re-run.
{% endif %}  **`bad_slug` is yours to undo**: the `--slug` you passed is invalid; nothing was
  authorized. Do NOT print the banner — re-run with a corrected slug, or no flag.
- `proceed: true` → **auto-advance**: invoke `Skill(hm:<next_stage from the JSON>)` with
  the JSON's `task_slug` as its argument (omit when `null`), instead of the STOP banner.
  **This supersedes this stage's earlier "Stage terminal … STOP"** — that governs the
  gated path, and `proceed: true` IS the authorization it asks for. `task_slug_source:
  "persisted"` means the slug came from an earlier stage — name it before invoking, so
  another task's slug cannot advance silently.
{% if judgment_stage %}- `judgment_auto_answered: true` → the level cleared a judgment gate for you. **Do what
  `judgment_directive` says before advancing.** An auto-answer that is not written down is
  an unauditable skip of a human decision — the record is the only thing that makes this
  level reviewable after the fact.
{% endif %}
<!-- @hm:/autopilot-advance -->
{% endif %}
## Stage summary — print before you STOP

Skip this banner entirely if loop-mode is active for THIS session (a
`.claude/.hm-loop-*` marker matches `$HM_SESSION_ID`, or a legacy
`.hm-loop-active` exists — the autoloop uses machine receipts, not prose).
Otherwise emit it as your final output, in the configured output language:
<!-- @hm:banner:end -->
> ✅ **Done:** {{ summary_done }}
> 📁 **Artifacts:** {{ summary_artifact }}
> ➡️ **Next:** {{ summary_next }}
