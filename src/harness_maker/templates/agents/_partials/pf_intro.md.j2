{#- Preflight intro sentence. Single source for the atomic block and the fused
    preamble; `in_preamble` switches only the subject of the sentence, because a
    preamble sits above every stage rather than inside one. Carries the AC-006
    fingerprint ("Claim/refresh it and surface concurrent work + drift"), so it must
    render exactly once per fused command. -#}
`harness.yaml worktree.feature_branch_workflow` is **on**: {% if in_preamble is defined and in_preamble %}every stage below opens with its own `task-preflight` call and operates{% else %}this stage operates{% endif %} inside the persistent per-task worktree `.worktrees/<slug>/` on branch `hm/<slug>` — shared by every `/hm:` stage for this task — NOT an ephemeral `execute-<uuid>` worktree. Claim/refresh it and surface concurrent work + drift{% if in_preamble is defined and in_preamble %}. This is what that call's output means{% endif %}:
