#!/usr/bin/env bash
# User's prior statusLine command, captured at /harness-maker:make time when
# the "combine" policy was chosen. harness-maker NEVER re-renders this file
# after first creation — edit it freely to change what the user-side of the
# combined statusline emits.
#
# To revert to harness-maker-only, change settings.json statusLine.command
# back to: "bash .claude/lib/run-statusline.sh"

{{ user_statusline_command | default('# (no original command captured)') }}
