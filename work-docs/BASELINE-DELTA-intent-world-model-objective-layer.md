# BASELINE-DELTA — intent-world-model-objective-layer

**Status: DECLARED before the change, measured at P0.** This document exists so that the
growth Phase 4 will add to four rendered commands is declared through a PLAN
`surface_allowance` rather than laundered through a regenerated `surface_baseline.json`. The
rule it satisfies is `PLAN-surface-ratchet`'s **ADR-010** — a ratchet is never rebaselined by
its own subject (`ratchet-rebaselined-by-its-own-subject`). `surface_baseline.json` is **not**
regenerated by this task; its `aggregate_chars` stay at **`claude` 427 369, `codex` 362 326**.

**Owning phase: Phase 4 (render).** Phase 0 measured the numbers below from the unchanged
render; Phase 4 adds the `surface_allowance` block to the PLAN frontmatter and only then edits
`plan.md.j2`, `wrapup.md.j2`, `review.md.j2` and `help.*.md.j2`. Nothing else in this task
touches a rendered command.

### Direction, stated out loud

**Four command surfaces will get larger on each arm** — `plan` (Step 0.5 objective context +
revisit loop), `wrapup` (two answer-gated blocks), `review` (Step 3.3 objective drift) and `help`
(one skill line). The SPEC caps them at 25 / 25 / 25 / 3 lines **per variant**. The skill body
(`intent-layer`) is not a command and is outside the ratchet.

### Measured at P0 (unchanged render, both variants)

| Variant key | chars | round_trips |
|---|---|---|
| `plan` | 63 813 | 26 |
| `wrapup` | 46 531 | 26 |
| `review` | 85 923 | 33 |
| `help` | 1 883 | 0 |
| `hm-plan` | 58 967 | 14 |
| `hm-wrapup` | 44 703 | 24 |
| `hm-review` | 82 319 | 28 |
| `hm-help` | 2 110 | 0 |

Counting rule: `round_trips` = `^!` lines on the claude variant, `Bash(` call sites on the codex
variant, plus each variant's dispatch form (`tests/structural/_surface_baseline.py`).

### Allowance to declare at Phase 4

Increments, not baselines. The declared values are the SPEC ceilings converted at the measured
average of ~70 chars per rendered line, rounded up, and are **re-measured after the edit**:

| Key | chars (max) | round_trips (max) |
|---|---|---|
| `plan` / `hm-plan` | 1 800 each | 3 each (status load, revisit loop call, objective show) |
| `wrapup` / `hm-wrapup` | 1 800 each | 2 each (assume observe, objective close) |
| `review` / `hm-review` | 1 800 each | 1 each (objective show) |
| `help` / `hm-help` | 250 each | 0 |

Aggregate `chars` ceiling: 11 300 across both variants. If the measured growth after Phase 4
exceeds a ceiling, the template is cut, not the ceiling raised.
