---
type: review
task_slug: help-command
status: APPROVED
created: 2026-05-21
reviewers_invoked: [code-reviewer, ux-reviewer]
consensus_method: cross-check
drift_verdict:
  result: clean
  scope_violations: []
  scenario_misses: []
  task_slug: help-command
  computed_at: 2026-05-21T05:30:00+00:00
---

## 🎯 Round 1 Summary

**Grade: A** (0 P0 / 0 P1 consensus-passed) — threshold A met → APPROVED. No auto-fix loop entered (gate exited on first branch).

**Notes:**
- 14 raw findings (6 code-reviewer / 8 ux-reviewer). After consensus filter, 0 strong-consensus, 1 weak-consensus pair, 13 manual-only.
- 2 manual-only findings classified as direct **ADR-001 (locale parity) violations** in this PLAN — auto-applied before report write (recorded below).
- All remaining manual-only findings are surfaced for user judgment; none are auto-applied (no consensus, no PLAN-ADR violation).

**Auto-applied fixes (ADR-001 enforcement, not auto-fix loop):**

| # | Finding | File | Action |
|---|---|---|---|
| 1 | U4 — `## 💡 Next steps` heading remained English in `.ko` template | `templates/commands/hm/help.ko.md.j2:66` | Changed to `## 💡 다음 단계`. Snapshot fixtures regenerated. |
| 2 | U2 — trailing space `⭐ (기본) ` in `.ko` workflow table cell (parity drift from `.en`) | `templates/commands/hm/help.ko.md.j2:26` | Removed trailing space inside `{% endif %}`. Snapshot fixtures regenerated. |

Both fall under PLAN-help-command ADR-001 "Help text must reliably display in the user's configured locale" — a Korean header left in English is a direct ADR contradiction, not subjective UX preference. Applied without consensus per CLAUDE.md "PLAN's ADRs are binding."

---

## 🔍 Drift Findings

`drift_verdict.result: clean`. All substantive changes match PLAN scope. Auto-regen consequences (e2e sandbox fixtures, `tests/fixtures/*/CLAUDE.md` from `regenerate.py` side effect, `uv.lock` from version bump, `.gitignore` from `finalize` tool itself) excluded as mechanical.

---

## ✅ Consensus Findings (strong)

**None.** Cross-check (2/N) consensus requires two reviewers to converge on the same execution risk at the same file+line region; the 2-reviewer set (code + ux) produced no strong-consensus pairs.

---

## ⚠️ Weak Consensus

| # | Severity | File:Line | Summary |
|---|---|---|---|
| W1 | P2 (mid) | `help.{en,ko}.md.j2:60-65` | **OBSERVE matches** (lines 60-65, Cursor + Codex blockquotes back-to-back), **CONCLUDE diverges**: code-reviewer (C5/C6) flags literal `/hm:*` in Cursor block as leak into Codex SKILL body for 3-target users; ux-reviewer (U3) flags the two consecutive blockquotes as contradictory for multi-target users. Same surface, different risk model → weak consensus per Step 4b. Not auto-fixable; needs design decision (see Manual-Only items U3, C5, C6 below). |

---

## 📝 Manual-Only Findings

### P1 (3 items)

| # | File:Line | Summary | Suggestion |
|---|---|---|---|
| C1 | `tests/e2e/sandbox/.claude/commands/hm/help.md` (absent) | E2e sandbox fixture missing the new `help.md`; CHANGELOG claim "all 8 fixtures regenerated" is technically true for `tests/snapshot/` but does not extend to `tests/e2e/sandbox/` which is regenerated by its own e2e test path. | Run `uv run python -m harness_maker.cli make tests/e2e/sandbox --update` (and same for `sandbox-plugin-test`) before commit so the e2e fixture isn't stale. |
| C2 | `tests/e2e/test_dogfood_sandbox.py:26` | `REQUIRED_COMMANDS` list omits `help`. New rendered command not e2e-verified. | Add `'help'` to `REQUIRED_COMMANDS`. |
| U1 | `help.en.md.j2:27` / `help.ko.md.j2:26` | Workflow table first-column width is uneven across rows (only the `default_workflow` row gets ` ⭐ (your default)` / ` ⭐ (기본)` suffix); some terminal renderers display jagged pipes. | Add a third column `| Default? |` and emit `⭐` there. Or move the marker to a separate line below the table. **Subjective** — `claude-code` terminal renders the existing layout cleanly in practice; surface for user judgment. |
| U3 | `help.en.md.j2:60-65` (+ `.ko.md.j2:59-64`) | Two consecutive blockquotes (Cursor + Codex) confuse multi-target users by reading like contradictory advice on `/hm:*` vs `@hm-*`. | Add blank line between the two blocks and prefix each with `If you are in <IDE>:` to make the conditional explicit. |
| U4-applied | `help.ko.md.j2:66` | (See "Auto-applied fixes" above — ADR-001 violation, fixed.) | — |

### P2 (8 items)

| # | File:Line | Summary | Suggestion |
|---|---|---|---|
| C3 | `synthesize.py:521` | Dead branch: `if config_dump else "en"` is unreachable — `config_dump` is set non-None at lines 510-513 before reaching 521. | Simplify to `str(config_dump.get("locale", "en"))`. |
| C4 | `test_help_command.py:73` | `test_codex_skill_body_uses_at_hm_prefix` asserts `"/hm:help" not in body` but uses `targets=[CLAUDE_CODE, CODEX]` only — does not catch the latent `/hm:*` literal in the Cursor blockquote (which only fires when targets ALSO includes cursor). | Add a 3-target variant asserting `"/hm:" not in body` (not just `/hm:help`). Pair with C5/C6 fix. |
| C5 | `help.en.md.j2:61` | Hardcoded `/hm:*` literal inside the `{% if "cursor" in config.targets %}` block leaks into Codex SKILL body when `targets=[cursor, codex, claude-code]` — the SKILL is pre-rendered with `is_codex=True` but the Cursor block doesn't know that. | Replace `/hm:*` with `{{ cmd }}*`, or guard the entire Cursor block with `{% if not is_codex %}`. |
| C6 | `help.ko.md.j2:60` | Same `/hm:*` leak in Korean Cursor block. | Same fix as C5. |
| U2-applied | `help.ko.md.j2:26` | (See "Auto-applied fixes" — ADR-001 violation, fixed.) | — |
| U5 | `help.en.md.j2:43` (+ `.ko.md.j2:42`) | ASCII flow diagram positions `verify` branching off `execute` visually, but prose at line 20 describes `verify` as a post-wrapup check. Misleads first-time users. | Move the branch to hang off `wrapup`, or replace branch with a footnote: `(verify: optional post-wrapup check)`. |
| U6 | `help.en.md.j2:4` | Intro blockquote hardcodes `.claude/commands/hm/<name>.md` — wrong path for Codex users reading the `.agents/skills/hm-help/SKILL.md` rendering of this body. | Branch on `is_codex`: `{% if is_codex %}.agents/skills/hm-<name>/SKILL.md{% else %}.claude/commands/hm/<name>.md{% endif %}`. |
| U7 | `help.ko.md.j2:5` | Same as U6 in Korean. | Same fix. |
| U8 | `help.en.md.j2:69` (+ `.ko.md.j2:67`) | "New to this harness?" → defaults to `config.default_workflow`. For Production preset, that's `exec-rev-wrap-ver` (4-stage execute), which is NOT an orientation. | Rephrase: "Start your next task?" + add tip "First time? Run `/hm:research` first." |

---

## 🤝 Disagreements

None. Reasoning aligned in all cross-reviewer overlaps (only W1 had aligned OBSERVE).

---

## Review Iteration Summary

| Iteration | Grade | Fixes Applied | Remaining | New |
|-----------|-------|---------------|-----------|-----|
| 1 (init)  | A     | 2 (ADR-001 enforcement, not auto-fix loop) | 12 manual-only + 1 weak-consensus | — |

**Final grade:** A
**Iterations used:** 1 / 3
**Status:** APPROVED
**human_review_needed:** false (grade met threshold) — BUT 12 manual-only items surfaced for user discretion.

---

## Recommended User Actions (manual triage)

Order by what would most affect end-user experience:

1. **P1-C1+C2 (e2e fixture gap)** — `make --update` on both e2e sandboxes; add `'help'` to `REQUIRED_COMMANDS`. ~2 min.
2. **P2-C5+C6+U3 (Cursor/Codex block layout + `/hm:*` leak)** — coordinated fix: guard Cursor block with `{% if not is_codex %}` AND restructure the dual-block to prefix `If you are in <IDE>:`. ~5 min.
3. **P2-U6+U7 (intro path is_codex branch)** — ~1 min mechanical.
4. **P2-C3 (dead branch cleanup)** — ~30s.
5. **P2-U5 (ASCII diagram verify placement)** — ~1 min.
6. **P2-U8 ("New to this harness?" rephrasing)** — ~30s.
7. **P2-C4 (3-target test coverage)** — ~2 min after C5/C6 fix.
8. **P1-U1 (table column alignment)** — defer until visual issue confirmed in real terminal use.

Total: ~12-15 min of follow-up work if user wants A+ polish. None are blockers — current grade is A and `/hm:help` works correctly for the user's 3-target config (verified by Phase 4 grep checks during execute).
