slug: plan-harness-gap-cot-2026-05
source: work-docs/plans/PLAN-harness-gap-cot-2026-05.md
created_at: "2026-05-08"
updated_at: "2026-05-08"
context:
  purpose: >
    harness-maker 0.7.0 에 Reliability Stack 7대 기능 + spec strength rubric + TDAD 를 단일
    릴리스로 추가. drift detection, hallucination prevention, cost observability, episodic memory,
    tool cascade recovery, prod-name guard, spec quality gate, 코드↔테스트 의존성 힌트 구현.
    호출자: harness-maker 플러그인을 사용하는 Claude Code / Cursor IDE 사용자.
  invariants:
    - "ADR-001~010 binding (단일 릴리스, 3-layer memory, 다층 fallback drift, self JSONL, scope-aware consensus, spec-driven only 강제, Anthropic-only, deny+sequence prod guard, LLM 에러 분류, hybrid drift)"
    - "CLAUDE.md 기술 결정: Python only, Anthropic only, 100% 로컬, atomic file write, uv+pyproject"
    - "기존 wiki.md/failures.md 파일 포맷 미변경 (backward compat)"
    - "기존 5-gate security scanner 미변경 (확장만)"
    - "외부 추가 의존 없음 (stdlib ast + 기존 anthropic, httpx 만)"
  priority: "reliability(correctness) > LLM-first design > simplicity(dependencies minimal) > cost"
  test_reliability: >
    각 phase 별 exit criterion 에 구체적 pytest 명령 포함. Unit test 는 mock 우선
    (mock_anthropic_client fixture). Integration 은 INTEGRATION=1 env 가드.
    Phase-exit 대응 테스트 매핑표 완비 (PLAN Testing Strategy 참조).
    현재 기존 코드 pytest 통과 상태.
  stopping_criteria: >
    Success Criteria 22개 항목 전체 pass: 모든 unit test pass + uv run pytest 전체 pass +
    ruff check + ruff format --check + mypy --strict 0 error.
    각 feature 별 구체적 검증 항목 (PLAN Success Criteria 참조).
  notes:
    - "Phase 0 ablation 결과에 따라 Phase 6 도입/거부 결정"
    - "Phase 3→4 순서 의존 (drift monitor 가 episodic memory 참조 가능)"
    - "Phase 5→6 선후관계 권장 (consensus → 2-pass review)"
